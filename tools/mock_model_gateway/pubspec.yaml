name: mock_model_gateway
description: Multi-protocol mock model gateway for TeamPilot CLI matrix tests
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.7.0

dev_dependencies:
  test: ^1.25.0
  lints: ^5.0.0
