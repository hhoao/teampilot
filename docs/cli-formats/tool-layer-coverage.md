# 工具层覆盖矩阵

**日期:** 2026-08-12（审计）；2026-08-13（Task 6 收尾回填）
**外部证据源:** [asgeirtj/system_prompts_leaks](https://github.com/asgeirtj/system_prompts_leaks)（CC0） @ `93c999115b300a6faac567830b0450a5478800cd`（下文记 `spl@93c9991`）
**状态:** 完成（Task 6 收尾：缺口全部落结论，见「缺口清单状态」；矩阵结论为最终版）

> 四方证据源定义：**src** = 本仓库源码（`client/lib/`，行号可 grep 回验）；**fixture** = `client/test/fixtures/session_history/<cli>/` 夹具（工具行实测）；**本机** = 本机实测数据（`~/.local/share/opencode/opencode.db`、`~/.codex/sessions/`、`~/.cursor/projects/*/agent-transcripts/`、`~/.flashskyai/projects/` 只读扫描，见 [opencode.md](opencode.md)）；**spl** = system_prompts_leaks 固定 commit 快照（raw URL 以 `spl@93c9991` 为引用锚点，禁止"最新版"表述）。
>
> 四家提示的工具定义段结论（Step 2 提取）：
> - **claude**（`Anthropic/Claude Code/claude-code-sonnet-5.md`，2888 行）：✅ **含完整 JSON 工具 schema**——33 个工具段，每节带 `$schema`（draft 2020-12）块（Edit/Read/Write/Bash/Agent 等）。注意：本快照为原生 macOS/Linux 构建，**无 Glob/Grep 工具**（`agents/Explore.md` 注释说明内嵌搜索构建才有）。
> - **codex**（`OpenAI/Codex/gpt-5.5.md`，138 行）：❌ **提示中无工具 JSON schema**——纯行为指令，散文提及 `apply_patch`（freeform）、`exec_command`、`multi_tool_use.parallel`、`commentary`/`final` 通道。同 commit 的 `codex-full.md` 含恢复的内置工具 schema（`# <BUILTIN_TOOLS>`），本矩阵作旁证引用。
> - **opencode**（`OpenCode/opencode.md`，329 行）：⚠️ **无 JSON schema**——`# Available Tools` 节为 markdown 定义（11 工具：bash/edit/glob/grep/question/read/skill/task/todowrite/webfetch/write），工具名与参数 key（`workdir`、`filePath` 等）均明确可归因。
> - **cursor**（`Cursor/cursor.md`，322 行）：⚠️ **无 JSON schema**——`## Available Tools` 节为 markdown 定义（20 工具），参数 key 散见于散文（`block_until_ms`、`old_string`、`replace_all`、`allow_multiple`、`subagent_type`）。

## 矩阵（5 CLI × 5 类别）

| CLI | Edit | File | Shell | Subagent | Category | 缺口数 |
|-----|------|------|-------|----------|----------|--------|
| claude | ✅ 完整 + spl:Edit{file_path,old_string,new_string,replace_all}(claude-code-sonnet-5.md:1282-1312)/NotebookEdit(:1725)/Write(:2868-2888) 与 str-replace/write codec 键集一致（src:shared_tool_call_resolvers.dart:25-31）；NotebookEdit 键 `notebook_path`/`new_source` 已入共享键集（G-3 修复 commit 99e4fc69） | ✅ 完整 + spl:Read{file_path,offset,limit,pages}(:1829-1860) 命中 read 规则 useOffsetLimit（src:shared_tool_call_resolvers.dart:86-97）；Write{file_path,content} 命中 write codec | ✅ 完整 + spl:Bash{command,timeout,description,...}(:882-912) 命中共享 shell 名集（src:shared_tool_call_resolvers.dart:55-59，bash/shell_command/exec_command）+ commandKeys（src:tool_call_resolvers.dart:115）；fixture:truncated_bash.jsonl 实测 `description` | ✅ 完整 + spl:Agent{description,prompt,subagent_type,model,run_in_background,isolation}(:451-455) ∈ subagentToolNames {'agent','task','workflow'}（src:claude ai_history_capability.dart:32）；匹配 lowercase（src:subagent_attachment_inflater.dart:44-45） | ✅ 完整 + G-3 修复（commit 99e4fc69）：enterplanmode→plan、taskget/tasklist/taskoutput/taskstop→task 入共享类别表；Cron*/DesignSync/SendMessage 等仍→other（可接受）；Glob/Grep→read 条目在表（G-6 观察项，见下） | 1（G-6 观察项） |
| codex | ✅ 完整 + G-1 修复（commit 99e4fc69）：apply_patch FREEFORM 经 unified-diff codec freeform 分支 + `*** File:` 头解析提取 hunk（spl:codex-full.md:547 + :550 grammar；src:unified_diff_edit_hunk_codec.dart:30-33,128-150） | ✅ 不适用：spl:codex-full.md 内置工具无 read/write 文件工具（文件操作走 exec_command/apply_patch）；共享 read/write 规则与 codex 无交集（无害空转） | ✅ 完整 + fixture:basic.jsonl 实测 exec_command{cmd}、reasoning_and_tools.jsonl 实测 shell_command{command}；spl:codex-full.md:463-475 functions.exec_command 含 cmd；commandKeys 含 'cmd'（src:tool_call_resolvers.dart:115） | ✅ 完整 + spl:codex-full.md:1269 `multi_agent_v1.spawn_agent`（defer_loading）∈ {'spawn_agent','agent','task'}（src:codex ai_history_capability.dart:30）；**I1 复核（Task 6）**：本机 `~/.codex/sessions/` 扫描 273 条真实 `spawn_agent` 工具调用**全部为裸名**（`"name":"spawn_agent"`），`multi_agent_v1.` 前缀零命中（src:codex ai_transcript.dart:210-214 取 `payload['name']` 原样）→ 裸名精确匹配与真实数据一致，**已验证非缺口** | ✅ 基本完整：exec_command/shell_command/apply_patch/spawn_agent 在共享表（src:tool_call_categories.dart:31-108）；update_plan（spl:codex-full.md:498）/request_user_input（spl:codex-full.md:506）与 write_stdin/view_image/tool_search 一样不在表内→other（可接受） | 0 |
| opencode | ✅ 完整 + 本机/夹具实测 edit{filePath,oldString,newString}（src:opencode tool_call_resolvers.dart:21-34 加 'filePath'/'oldString'/'newString' 追加键）；spl:opencode.md edit 定义一致 | ✅ 完整 + 本机实测 read{filePath,offset,limit}、write{filePath,content}；read 规则 useOffsetLimit（src:opencode tool_call_resolvers.dart:55-69）；spl:opencode.md 同 | ✅ 完整 + fixture:prt_tool1.json 实测 bash{command}、本机实测 {command,timeout,workdir}；spl:opencode.md bash 定义（含 `workdir`） | ✅ 完整 + spl:opencode.md task 工具（description/prompt/subagent_type/task_id/command）∈ {'task'}（src:opencode ai_history_capability.dart:38） | ✅ 完整 + G-3 修复（commit d8b14868）：bash/read/edit/write/grep/glob/task/todowrite/webfetch 全在表内；skill→other 显式化；**question→askUser（Task 6 决策，跨 CLI 统一，见「Task 6 决策项」节）** | 0 |
| cursor | ✅ 完整 + G-4 重校（Task 6）：本机实测真实键形 StrReplace{path,old_string,new_string}（25839 次，`file_path` 零命中）、Write{path,contents}（3902 次）、ApplyPatch 字符串 FREEFORM input（84 次）——Task 3「无 key 缺口」判定被推翻（原按 spl 散文假设 file_path/content 键形），Task 6 修复：cursor resolver 追加 `path`/`contents` 键（src:cursor tool_call_resolvers.dart:31-46）+ adapter 字符串 input 保留 argsText（src:cursor ai_transcript.dart:256-262，同 codex freeform 路径）；夹具 chat-strreplace-write.jsonl 全链路实测 | ✅ 覆盖 + fixture:agent_transcript_no_tool_id.jsonl 实测 Read{path} 命中 read 规则默认 pathKeys 含 'path'（src:ai_message_core tool_file_target.dart:22）；Read{path,offset,limit} 本机实测（18627 次）；Write 见左格 | ✅ 完整 + fixture:chat-aaaa…jsonl / chat-shell-missing-result.jsonl 实测 Shell{command,description}；cursor shellResolver 名集（src:cursor tool_call_resolvers.dart:91-101）含 'execute'（spl:codex 快照未见，**前瞻条目——Task 6 本机扫描 4040 条真实 transcript 零命中，维持前瞻**） | ✅ 完整 + spl:cursor.md Task 工具 + 7 种子代理类型(:271-282)；lowercase 'task' ∈ {'agent','task'}（src:cursor ai_history_capability.dart:33）；'agent' 为前瞻（本机扫描零命中，维持）；**观察项**：真实 transcript 出现过 `Subagent` 工具名（6 次，lowercase 'subagent' 在类别表→subagent，但不在 subagentToolNames {'agent','task'} 内，run 侧不解析） | ✅ 完整 + G-3 修复（commit 7259d414）：AskQuestion→askUser 入共享表（本机实测 60 次 AskQuestion 佐证）；SwitchMode(20)/SemanticSearch(340)/Delete(350)/GenerateImage(3) 等→other（接受差异，断言固化）；TodoWrite/WebSearch/WebFetch/Glob/Grep 已覆盖；mcp__ 前缀规则生效 | 0 |
| flashskyai | ✅ 解析器覆盖 / ⚠️ G-5 证据缺口（接受差异）：复用共享 codec（src:flashskyai tool_call_resolvers.dart:4-5）可解析 Edit/Write；断言固化可解析性（task-2 report）；夹具（basic/streamed_tools.jsonl）无 Edit/Write 工具行，**夹具待补**（Task 6 判定：本机 `~/.flashskyai/projects/` 扫描仅 Read×3/Bash×1，无 Edit/Write 发射证据，无法构造真实夹具） | ⚠️ 部分 + fixture:streamed_tools.jsonl 实测 Read{file_path} 命中 read 规则；Write 无夹具证据（G-5，同上） | ✅ 完整 + fixture:streamed_tools.jsonl 实测 Bash{command,description}（description 实测 `"List working directory contents"`） | ⚠️ G-2 接受差异（断言固化）：subagentToolNames {'agent','task'}（src:flashskyai ai_history_capability.dart:32），无 workflow 解析（flashskyai.md:81-83 文档化；本机无 workflow 发射证据；ClaudeWorkflowResolver 依赖 Claude 专属磁盘产物）——类别表 workflow→subagent 仍在，类别/run 侧差异为已知 | ✅ 完整：Bash/Read/agent/task 全在共享表（lowercase 匹配，flashskyai.md:52-55） | 2（G-2、G-5，均接受差异） |

## 缺口清单状态（Task 6 收尾回填）

| # | CLI | 类别 | 缺口 | 状态 | 结论 |
|---|-----|------|------|------|------|
| G-1 | codex | Edit | `apply_patch` 是 FREEFORM 工具（patch 为 `arguments` 原始文本，非 JSON map）：codex adapter 把非 JSON 的 arguments 放 `argsText`（src:codex ai_transcript.dart:426-432），`UnifiedDiffEditHunkCodec` 只经 `toolCallArgsMap`（args 或 argsText 的 jsonDecode）取 patchKey（src:tool_args.dart:7-20、unified_diff_edit_hunk_codec.dart:36-38）→ 解码失败、hunk 永远提取不出 | **已修复**（commit `99e4fc69`） | unified-diff codec 增加 freeform 分支（argsText 非 JSON 直接按 patch 文本解析）+ `*** Update/Add/Delete File:` 头提取 path（方案 A，任务 2 报告 §G-1 方案选择）；codec 层 7 条新断言 + resolver 20 断言全绿 |
| G-2 | flashskyai | Subagent | 复用 Claude 兼容链路但无 workflow 解析：`subagentToolNames` 仅 `{'agent','task'}`（src:flashskyai ai_history_capability.dart:32），capability 不引用 `workflow_resolver.dart`；Claude 兼容格式会话若产生 Workflow 工具调用，run 侧解析缺失（flashskyai.md:81-83 已文档化） | **接受差异**（断言固化，commit `99e4fc69`） | 修复方向原文 =「先用真实会话/夹具确认 flashskyai 是否发射 Workflow 工具调用」。证据：夹具无 workflow 行；本机 `~/.flashskyai/projects/` 扫描无 workflow 工具调用；`ClaudeWorkflowResolver` 依赖 Claude 专属磁盘产物（`<task-notification>`、`workflows/wf_*.json`），无证据 flashskyai 会写这些文件——盲加 'workflow' 反而类别/run 侧不一致。固化断言：`subagentToolNames == {'agent','task'}`；类别表 workflow→subagent 仍在（已知差异） |
| G-3 | claude / opencode / cursor | Category | 真实工具未显式归类（落 `other`）：claude `EnterPlanMode`、`TaskGet`/`TaskList`/`TaskOutput`/`TaskStop`；opencode `question`/`skill`；cursor `AskQuestion` | **已修复**（commits `99e4fc69` / `7259d414` / `d8b14868` + Task 6 决策） | ① claude 部分（99e4fc69）：`enterplanmode→plan`、`taskget/tasklist/taskoutput/taskstop→task` 入共享表；② cursor 部分（7259d414）：`askquestion→askUser` 入共享表（本机实测 60 次 AskQuestion 佐证）；③ opencode 部分（d8b14868）：`question`/`skill`→other 显式化（11 工具全表显式）；④ **Task 6 决策**（本任务）：`question` 跨 CLI 统一为 **askUser**（与 cursor AskQuestion / claude askuserquestion 一致），见「Task 6 决策项」 |
| G-4 | cursor | Edit / File | StrReplace/Write/EditNotebook 参数 key 仅有 SPL 散文级证据（spl:cursor.md:244-254），夹具无 StrReplace/Write 工具行 | **已修复**（Task 6 重校 + 修复，本分支最终 commit） | Task 3 判「无 key 缺口」基于 spl 散文假设键形 {file_path, content}；**Task 6 本机扫描推翻该假设**：真实 StrReplace 用 `path`（25839 次，`file_path` 零命中）、真实 Write 用 `path`/`contents`（3902 次，`content` 零命中）、真实 ApplyPatch input 为字符串 FREEFORM（84 次，adapter 此前丢弃）。修复：cursor resolver 追加 `path`/`contents` 键（src:cursor tool_call_resolvers.dart:31-46，追加语义未替换共享键）+ cursor adapter 字符串 input 保留 argsText（src:cursor ai_transcript.dart:256-262）。夹具补入 `client/test/fixtures/session_history/cursor/projects/home-me-proj/agent-transcripts/chat-strreplace-write/chat-strreplace-write.jsonl` + 全链路断言（adapter→resolver 3 工具行全解析） |
| G-5 | flashskyai | Edit / File | 夹具只有 Bash/Read 工具行，无 Edit/Write/Glob/Grep（解析器共享层已覆盖，缺实测证据） | **接受差异**（断言固化）+ **夹具待补** | 断言固化共享配置对 Edit/Write 的可解析性（Task 2）；夹具增补**待补**：Task 6 判定本机 `~/.flashskyai/projects/` 真实会话仅 Read×3/Bash×1，**无 Edit/Write 发射证据**，无法构造真实夹具（避免人工捏造非证据数据）；待未来真实 flashskyai Edit/Write 会话后按 streamed_tools.jsonl 同格式补入 |
| G-6 | claude | File / Category | spl@93c9991 快照（原生构建）无 Glob/Grep 工具（`agents/Explore.md:10` 注释：npm/Windows 构建含 Glob/Grep）——共享类别表已有 glob/grep 条目，**无解析缺口**，仅记录构建间工具面差异 | **观察项**（维持，无代码改动） | 断言固化 `Glob`/`Grep`→read（npm/Windows 构建工具面，共享表已有条目，任务 2）；若 Teams 会话跑 npm/Windows 构建出现 Glob/Grep 工具行，解析已就绪 |

## Task 6 必查项结论（本机真实数据复核）

| 必查项 | 来源 | 扫描范围 | 结论 |
|--------|------|---------|------|
| I1（Task 2 遗留）：codex `spawn_agent` 前缀 `multi_agent_v1.` vs 裸名 `subagentToolNames` 匹配风险 | 本机 `~/.codex/sessions/` | 12 个会话 jsonl（2026-03~2026-05） | **已验证非缺口**：273 条真实 `"name":"spawn_agent"` 全部为裸名，`multi_agent_v1.` 前缀零命中；codex adapter 取 `payload['name']` 原样（src:codex ai_transcript.dart:210-214），裸名精确匹配成立。spl:codex-full.md:1269 的命名空间前缀形态**未出现在真实 transcript**——矩阵 codex Subagent 格「✅ 完整」∈ 判定实证通过 |
| Task 3 遗留：cursor `agent`/`execute` 前瞻条目 | 本机 `~/.cursor/projects/*/agent-transcripts/` | 37 个项目 4040 条 transcript | **前瞻维持**：`execute` 零命中、`agent` 零命中（spl:cursor.md 快照亦未见）→ 断言固化行为与真实数据无冲突，维持前瞻条目。**新观察**：真实 transcript 出现 `Subagent` 工具名 6 次（lowercase 'subagent' 类别表已覆盖→subagent 显示，但不在 subagentToolNames {'agent','task'}，run 侧子代理解析不触发——影响面极小，记录不修） |
| Task 3 遗留：cursor StrReplace/Write/EditNotebook 真实键形 | 同上 | 同上 | **G-4 重校**（见缺口清单 G-4）：真实键形与 Task 3 假设不符，Task 6 已修复 + 夹具补入 |
| G-4/G-5 夹具增补 | 本机 `~/.cursor/` + `~/.flashskyai/projects/` | 见缺口清单 | cursor：已补（chat-strreplace-write.jsonl，真实键形）；flashskyai：**待补**（本机无 Edit/Write 发射证据，理由见 G-5） |

## Task 6 决策项：question 跨 CLI 归类统一

**问题**（Task 4 遗留）：opencode `question` 工具 → other（Task 4 显式化），而 cursor `AskQuestion` → askUser（Task 3）、claude `askuserquestion`/`ask_user_question`/`ask_user` → askUser——同类语义（向用户提问）跨 CLI 归类不一致，历史视图「Ask user」折叠组不统一。

**裁决**：**统一为 askUser**（采纳 controller 方向，非否决）。理由：
1. opencode `question` 工具语义即「向用户提问」（`questions[]` 数组含 header/options），与 askUser 类别定义吻合；opencode 运行时 `question.asked` hook 已驱动 ask-user 提问卡（`agent_attention_cubit_test.dart`、`ask_user_question.dart` 实测路径）。
2. 与 cursor AskQuestion（60 次本机实测发射）、claude askuserquestion 三族一致，历史视图折叠组统一。
3. 半径小且自洽：类别表 1 行值变更（other→askUser，`tool_call_categories.dart`）+ 2 处断言同步（opencode resolver 测试、类别映射契约测试）+ 矩阵/opencode.md 行更新；行为变更已伴随测试全绿（见验证节）。

**影响面**：`tool_call_categories.dart` `'question': other` → `'question': askUser`；`opencode_tool_call_resolvers_test.dart`、`tool_call_category_mapping_test.dart` 断言更新；opencode.md:91 行更新。

## Task 5 治理同步（矩阵回填修订）

1. **shellToolNames 收窄显式化**：Task 2 判定「shellToolNames 全量保持共享」在 5 CLI 全证据下被 Task 5 逐项治理**收窄**——`shell`/`run_shell_command`/`run_terminal_cmd` 无 ≥2 CLI 发射证据，仅由 cursor 专属覆写持有（src:cursor tool_call_resolvers.dart:91-101，Task 3 固化测试全绿）；共享 `shellToolNames = {bash, shell_command, exec_command}`（src:shared_tool_call_resolvers.dart:55-59）。矩阵 claude/codex/opencode/flashskyai Shell 格引用共享名集，cursor Shell 格引用专属覆写。
2. **共享键集行号引用**（Task 5 重构后）：键集常量 `shared_tool_call_resolvers.dart:24-60`（editToolNames:25-28 / editPathKeys:29 / editNewStringKeys:31 / writeToolNames:33-35 / writeContentKeys:37 / diffToolNames:42 / fileReadToolNames:45 / fileEditToolNames:49-53 / shellToolNames:55-59）；read 规则 `:86-97`；codec 实例 `:67-84`。
3. **0 证据项移除**（Task 5）：writefile/write_file/create/create_file/readfile/read_file/contents/file/target_file/start_line/startLine 从共享 resolver 移除（类别表仍显示，union 语义不受影响）；`opencode.md` 兜底别名表已同步重写。

## 新 CLI 预研（非承诺）

> 以下为预研要点，不是接入计划；「未来接入」仅指潜在接入点。证据源固定为 spl@93c9991 的 `Google/gemini-cli.md`（254 行）与 `Google/antigravity-cli.md`（451 行），两者均无工具 JSON schema（与 codex 同属散文级证据）。

### Gemini CLI（`Google/gemini-cli.md`）

1. **工具命名风格**：snake_case + 语义化——`grep_search` / `read_file` / `write_file` / `replace` / `run_shell_command` / `update_topic` / `save_memory` / `activate_skill` / `ask_user` / `web_fetch`（散文列举，无 schema）。`read_file` 兼负编辑前校验（"read_file fails if old_string is ambiguous"），`replace` 对同文件**禁止并行**编辑——与现有 5 CLI 的 edit 模型（str-replace codec 的 old_string/new_string）概念同源，接入时 str-replace codec 键集可复用。
2. **子代理即工具**：subagent 以同名工具暴露（`codebase_investigator` / `cli_help` / `generalist` / `browser_agent`，「You MUST delegate tasks to the sub-agent with the most relevant expertise」）——与 TeamPilot `AiHistoryCapability.subagentToolNames` 的「工具名集合」模型天然吻合，接入时只需把子代理名加入集合 + 配 side resolver。
3. **参数形态**：`grep_search(context/before/after/include_pattern/exclude_pattern/total_max_matches)`、`read_file(start_line/end_line)`、`run_shell_command(is_background/wait_for_previous)`、`save_memory(scope: "global"\|"project")`、`update_topic(title,summary)`——read 类工具的 `start_line/end_line` 与 claude `offset/limit` 语义等价但 key 不同，若接入需新增 startLine/endLine 键别名。
4. **证据缺口**：提示中无 transcript 存储位置与工具 JSON schema（代码路径仅见 `~/.gemini/GEMINI.md` 的上下文片段）——未来接入需走「源码 + 本机实测」路径补证据（同本矩阵 codex 的降级路径）。

### Antigravity CLI（`Google/antigravity-cli.md`）

1. **transcript 位置精确已知（最大潜在接入点）**：`<appDataDir>/brain/<conversation-id>/.system_generated/logs/transcript.jsonl`——JSONL，每行含 `step_index` / `source`（USER_EXPLICIT/MODEL/SYSTEM）/ `type`（USER_INPUT/PLANNER_RESPONSE/VIEW_FILE 等）/ `status` / `content` / `tool_calls`（数组，**含工具参数**）；提示明确可用于 grep 回溯历史（`grep '"type":"USER_INPUT"' …`）。未来接入的 transcript 定位落点可直接套 `AiTranscriptTailReader` 家族（JSONL 行式）。
2. **子代理通信模型不同**：`invoke_subagent`（按名调用或 `define_subagent` 会话内定义）→ 返回 conversation ID → `send_message` 按 conversation ID 通信；系统自动唤醒（无需轮询）。与 TeamBus 的成员收件箱语义不同，若接入需在 side resolver 上做 conversation-ID 映射，而非复用现有 agent/task 的父子 transcript 约定。
3. **工具名/数据形态**：snake_case（`invoke_subagent`/`define_subagent`/`send_message`/`view_file`/`generate_image`/`ask_question` + `ArtifactMetadata.request_feedback`）；planning 三件套 artifact（`task.md` / `implementation_plan.md` / `walkthrough.md`）落在 `<appDataDir>/brain/<conversation-id>/`——即 seat transcript 之外的**同一数据目录内**补充证据源，可用于 cross-check 工具调用序列。
