# 工具层覆盖矩阵

**日期:** 2026-08-12
**外部证据源:** [asgeirtj/system_prompts_leaks](https://github.com/asgeirtj/system_prompts_leaks)（CC0） @ `93c999115b300a6faac567830b0450a5478800cd`（下文记 `spl@93c9991`）
**状态:** 审计中（Task 6 回填结论）

> 四方证据源定义：**src** = 本仓库源码（`client/lib/`，行号可 grep 回验）；**fixture** = `client/test/fixtures/session_history/<cli>/` 夹具（工具行实测）；**本机** = 本机实测数据（`~/.local/share/opencode/opencode.db` 只读查询，见 [opencode.md](opencode.md)）；**spl** = system_prompts_leaks 固定 commit 快照（raw URL 以 `spl@93c9991` 为引用锚点，禁止"最新版"表述）。
>
> 四家提示的工具定义段结论（Step 2 提取）：
> - **claude**（`Anthropic/Claude Code/claude-code-sonnet-5.md`，2888 行）：✅ **含完整 JSON 工具 schema**——34 个工具每节带 `$schema` 块（Edit/Read/Write/Bash/Agent 等均有 `input_schema`）。注意：本快照为原生 macOS/Linux 构建，**无 Glob/Grep 工具**（`agents/Explore.md` 注释说明内嵌搜索构建才有）。
> - **codex**（`OpenAI/Codex/gpt-5.5.md`，138 行）：❌ **提示中无工具 JSON schema**——纯行为指令，散文提及 `apply_patch`（freeform）、`exec_command`、`multi_tool_use.parallel`、`commentary`/`final` 通道。同 commit 的 `codex-full.md` 含恢复的内置工具 schema（`# <BUILTIN_TOOLS>`），本矩阵作旁证引用。
> - **opencode**（`OpenCode/opencode.md`，329 行）：⚠️ **无 JSON schema**——`# Available Tools` 节为 markdown 定义（11 工具：bash/edit/glob/grep/question/read/skill/task/todowrite/webfetch/write），工具名与参数 key（`workdir`、`filePath` 等）均明确可归因。
> - **cursor**（`Cursor/cursor.md`，322 行）：⚠️ **无 JSON schema**——`## Available Tools` 节为 markdown 定义（20 工具），参数 key 散见于散文（`block_until_ms`、`old_string`、`replace_all`、`allow_multiple`、`subagent_type`）。

## 矩阵（5 CLI × 5 类别）

| CLI | Edit | File | Shell | Subagent | Category | 缺口数 |
|-----|------|------|-------|----------|----------|--------|
| claude | ✅ 完整 + spl:Edit{file_path,old_string,new_string,replace_all}(claude-code-sonnet-5.md:1282-1312)/NotebookEdit(:1725)/Write(:2868-2888) 与 str-replace/write codec 键集一致（src:shared_tool_call_resolvers.dart:18-37） | ✅ 完整 + spl:Read{file_path,offset,limit,pages}(:1829-1860) 命中 read 规则 useOffsetLimit（src:shared_tool_call_resolvers.dart:93-97）；Write{file_path,content} 命中 write codec | ✅ 完整 + spl:Bash{command,timeout,description,...}(:882-912) 命中 shell 名集（src:shared_tool_call_resolvers.dart:58-65）+ commandKeys（src:tool_call_resolvers.dart:115）；fixture:truncated_bash.jsonl 实测 `description` | ✅ 完整 + spl:Agent{description,prompt,subagent_type,model,run_in_background,isolation}(:451-455) ∈ subagentToolNames {'agent','task','workflow'}（src:claude ai_history_capability.dart:32）；匹配 lowercase（src:subagent_attachment_inflater.dart:44-45） | ⚠️ 轻微缺口 G-3：EnterPlanMode→other（表仅 exitplanmode）；TaskGet/TaskList/TaskOutput/TaskStop→other（表仅 taskcreate/taskupdate）；Cron*/DesignSync/SendMessage 等→other | 2（G-3、G-6） |
| codex | ⚠️ 缺口 G-1：apply_patch 为 FREEFORM（spl:codex-full.md:547 `type apply_patch = (FREEFORM) => any` + :550 grammar；gpt-5.5.md:61 明文件编辑走 apply_patch），patch 文本进 argsText（src:codex ai_transcript.dart:426-432），unified-diff codec 经 toolCallArgsMap 只认 args/patchKey（src:tool_args.dart:7-20、unified_diff_edit_hunk_codec.dart:36-38）→ 无法提取 hunk | ✅ 不适用：spl:codex-full.md 内置工具无 read/write 文件工具（文件操作走 exec_command/apply_patch）；共享 read/write 规则与 codex 无交集（无害空转） | ✅ 完整 + fixture:basic.jsonl 实测 exec_command{cmd}、reasoning_and_tools.jsonl 实测 shell_command{command}；spl:codex-full.md:463-475 functions.exec_command 含 cmd；commandKeys 含 'cmd'（src:tool_call_resolvers.dart:115） | ✅ 完整 + spl:codex-full.md:1269 `multi_agent_v1.spawn_agent`（defer_loading）∈ {'spawn_agent','agent','task'}（src:codex ai_history_capability.dart:30） | ✅ 基本完整：exec_command/shell_command/apply_patch/spawn_agent/update_plan/request_user_input 全在共享表（src:tool_call_categories.dart:31-107）；write_stdin/view_image/tool_search→other 可接受 | 1（G-1） |
| opencode | ✅ 完整 + 本机/夹具实测 edit{filePath,oldString,newString}（src:opencode tool_call_resolvers.dart:20-23 加 'filePath'）；spl:opencode.md edit 定义一致 | ✅ 完整 + 本机实测 read{filePath,offset,limit}、write{filePath,content}；read 规则 useOffsetLimit（src:opencode tool_call_resolvers.dart:45-50）；spl:opencode.md 同 | ✅ 完整 + fixture:prt_tool1.json 实测 bash{command}、本机实测 {command,timeout,workdir}；spl:opencode.md bash 定义（含 `workdir`） | ✅ 完整 + spl:opencode.md task 工具（description/prompt/subagent_type/task_id/command）∈ {'task'}（src:opencode ai_history_capability.dart:38） | ⚠️ 轻微缺口 G-3：question/skill→other（共享表无条目；本机实测，opencode.md:91-92 已标注）；bash/read/edit/write/grep/glob/task/todowrite/webfetch 全在表内 | 1（G-3） |
| cursor | ✅ 覆盖（证据等级注）+ spl:cursor.md StrReplace 散文 old_string/replace_all(:244-245)、EditNotebook(:250-254) 命中 codec 名集（src:shared_tool_call_resolvers.dart:18-23）；⚠️ 夹具未实测 StrReplace/Write 参数（G-4） | ✅ 覆盖（证据等级注）+ fixture:agent_transcript_no_tool_id.jsonl 实测 Read{path} 命中 read 规则 pathKeys 含 'path'（src:shared_tool_call_resolvers.dart:24）；Write 命中 write codec；⚠️ 夹具未实测 Write（G-4） | ✅ 完整 + fixture:chat-aaaa…jsonl / chat-shell-missing-result.jsonl 实测 Shell{command,description}；cursor shellResolver 名集（src:cursor tool_call_resolvers.dart:11-23）含 'execute'（spl:cursor.md 快照未见，前瞻条目） | ✅ 完整 + spl:cursor.md Task 工具 + 7 种子代理类型(:271-282)；lowercase 'task' ∈ {'agent','task'}（src:cursor ai_history_capability.dart:33）；'agent' 为前瞻 | ⚠️ 轻微缺口 G-3：AskQuestion→other（共享表仅 askuserquestion/ask_user_question/ask_user，src:tool_call_categories.dart:93-95）；SwitchMode/SemanticSearch/Delete/GenerateImage 等→other；TodoWrite/WebSearch/WebFetch/Glob/Grep 已覆盖；mcp__ 前缀规则生效 | 1（G-3、G-4） |
| flashskyai | ✅ 解析器覆盖 / ⚠️ 证据缺口 G-5：复用共享 codec（src:flashskyai tool_call_resolvers.dart:4-5）可解析 Edit/Write；夹具（basic/streamed_tools.jsonl）无 Edit/Write 工具行 | ⚠️ 部分 + fixture:streamed_tools.jsonl 实测 Read{file_path} 命中 read 规则；Write 无夹具证据（G-5） | ✅ 完整 + fixture:streamed_tools.jsonl 实测 Bash{command,description}（description 实测 `"List working directory contents"`） | ⚠️ 缺口 G-2：subagentToolNames {'agent','task'}（src:flashskyai ai_history_capability.dart:32），无 workflow 解析（flashskyai.md:81-83 文档化；capability 不引用 workflow_resolver.dart） | ✅ 完整：Bash/Read/agent/task 全在共享表（lowercase 匹配，flashskyai.md:52-55） | 2（G-2、G-5） |

## 缺口清单（Task 2-4 的修复依据）

| # | CLI | 类别 | 缺口 | 涉及配置 | 修复方向 |
|---|-----|------|------|---------|---------|
| G-1 | codex | Edit | `apply_patch` 是 FREEFORM 工具（patch 为 `arguments` 原始文本，非 JSON map）：codex adapter 把非 JSON 的 arguments 放 `argsText`（src:codex ai_transcript.dart:426-432），`UnifiedDiffEditHunkCodec` 只经 `toolCallArgsMap`（args 或 argsText 的 jsonDecode）取 patchKey（src:tool_args.dart:7-20、unified_diff_edit_hunk_codec.dart:36-38）→ 解码失败、hunk 永远提取不出 | codex `capabilities/history/ai_transcript.dart`；`ai_history/edit_codecs/unified_diff_edit_hunk_codec.dart` | unified-diff codec 增加 freeform 分支：argsText 非 JSON 时直接按 patch 文本解析（`**` hunk 头提取 path）；或 codex 专属 codec 配置（Task 2-4 选型） |
| G-2 | flashskyai | Subagent | 复用 Claude 兼容链路但无 workflow 解析：`subagentToolNames` 仅 `{'agent','task'}`（src:flashskyai ai_history_capability.dart:32），capability 不引用 `workflow_resolver.dart`；Claude 兼容格式会话若产生 Workflow 工具调用，run 侧解析缺失（flashskyai.md:81-83 已文档化） | flashskyai `capabilities/history/ai_history_capability.dart` | 先用真实会话/夹具确认 flashskyai 是否发射 Workflow 工具调用；确认后加 'workflow' 并复用 `ClaudeWorkflowResolver` |
| G-3 | claude / opencode / cursor | Category | 真实工具未显式归类（落 `other`，可显示但不细分）：claude `EnterPlanMode`（表仅 exitplanmode/exit_plan_mode）、`TaskGet`/`TaskList`/`TaskOutput`/`TaskStop`（表仅 taskcreate/taskupdate）；opencode `question`/`skill`（本机实测，opencode.md:91-92 已标注）；cursor `AskQuestion`（表仅 askuserquestion/ask_user_question/ask_user） | `ai_history/tool_call_categories.dart` 共享表 | 补条目：`enterplanmode→plan`、`taskget/tasklist/taskoutput/taskstop→task`、`askquestion→askUser`、`skill→other` 显式化（question 同理）；同步契约测试 |
| G-4 | cursor | Edit / File | StrReplace/Write/EditNotebook 参数 key 仅有 SPL 散文级证据（spl:cursor.md:244-254），夹具无 StrReplace/Write 工具行（Read/Shell 已有夹具实测） | `client/test/fixtures/session_history/cursor/` | 增补 cursor 夹具（StrReplace{file_path,old_string,new_string}、Write 行），消除证据降级 |
| G-5 | flashskyai | Edit / File | 夹具只有 Bash/Read 工具行，无 Edit/Write/Glob/Grep（解析器共享层已覆盖，缺实测证据） | `client/test/fixtures/session_history/flashskyai/` | 增补 flashskyai 夹具（Edit/Write 工具行），与 streamed_tools.jsonl 同格式 |
| G-6 | claude | File / Category | spl@93c9991 快照（原生构建）无 Glob/Grep 工具（`agents/Explore.md:10` 注释：npm/Windows 构建含 Glob/Grep）——共享类别表已有 glob/grep 条目，**无解析缺口**，仅记录构建间工具面差异，防误判 | —（观察项） | 跟踪 claude 版本差异；若 Teams 会话跑 npm/Windows 构建出现 Glob/Grep 工具行，解析已就绪 |

## 新 CLI 预研（非承诺）

> 以下为预研要点，不是接入计划；「未来接入」仅指潜在接入点。证据源固定为 spl@93c9991 的 `Google/gemini-cli.md`（254 行）与 `Google/antigravity-cli.md`（451 行），两者均无工具 JSON schema（与 codex 同属散文级证据）。

### Gemini CLI（`Google/gemini-cli.md`）

1. **工具命名风格**：snake_case + 语义化——`grep_search` / `read_file` / `write_file` / `replace` / `run_shell_command` / `update_topic` / `save_memory` / `activate_skill` / `ask_user` / `web_fetch`（散文列举，无 schema）。`read_file` 兼负编辑前校验（"read_file fails if old_string is ambiguous"），`replace` 对同文件**禁止并行**编辑——与现有 5 CLI 的 edit 模型（str-replace codec 的 old_string/new_string）概念同源，接入时 str-replace codec 键集可复用。
2. **子代理即工具**：subagent 以同名工具暴露（`codebase_investigator` / `cli_help` / `generalist` / `browser_agent`，「You MUST delegate tasks to the sub-agent with the most relevant expertise」）——与 TeamPilot `AiHistoryCapability.subagentToolNames` 的「工具名集合」模型天然吻合，接入时只需把子代理名加入集合 + 配 side resolver。
3. **参数形态**：`grep_search(context/before/after/include_pattern/exclude_pattern/total_max_matches)`、`read_file(start_line/end_line)`、`run_shell_command(is_background/wait_for_previous)`、`save_memory(scope: "global"\|"project")`、`update_topic(title,summary)`——read 类工具的 `start_line/end_line` 与 claude `offset/limit` 语义等价但 key 不同，若接入需新增 startLine/endLine 键别名。
4. **证据缺口**：提示中无 transcript 存储位置与工具 JSON schema（代码路径仅见 `~/.gemini/GEMINI.md` 的上下文片段）——未来接入需走「源码 + 本机实测」路径补证据（同本矩阵 codex 的降级路径）。

### Antigravity CLI（`Google/antigravity-cli.md`）

1. **transcript 位置精确已知（最大潜在接入点）**：`<appDataDir>/brain/<conversation-id>/.system_generated/logs/transcript.jsonl`——JSONL，每行含 `step_index` / `source`（USER_EXPLICIT/MODEL/SYSTEM）/ `type`（USER_INPUT/PLANNER_RESPONSE/VIEW_FILE 等）/ `status` / `content` / `tool_calls`（数组，**含工具参数**）；提示明确可用于 grep 回溯历史（`grep '"type":"USER_INPUT"' …`）。未来接入的 transcript 定位落点可直接套 `AiTranscriptTailReader` 家族（JSONL 行式）。
2. **子代理通信模型不同**：`invoke_subagent`（按名调用或 `define_subagent` 会话内定义）→ 返回 conversation ID → `send_message` 按 conversation ID 通信；系统自动唤醒（无需轮询）。与 TeamBus 的成员收件箱语义不同，若接入需在 side resolver 上做 conversation-ID 映射，而非复用现有 agent/task 的父子 transcript 约定。
3. **工具名/数据形态**：snake_case（`invoke_subagent`/`define_subagent`/`send_message`/`view_file`/`generate_image`/`ask_question` + `ArtifactMetadata.request_feedback`）；planning 三件套 artifact（`task.md` / `implementation_plan.md` / `walkthrough.md`）落在 `<appDataDir>/brain/<conversation-id>/`——即 seat transcript 之外的**同一数据目录内**补充证据源，可用于 cross-check 工具调用序列。
