# 消息层差异矩阵

**日期:** 2026-08-12
**来源:** 格式参考库各页 + 5 个 adapter 源码 + `ai_message_core`（message.dart / subagent_attachment.dart）
**状态:** 审计中（Task 8 回填结论）

行号约定：`compatible_jsonl.dart` = `client/lib/services/cli/claude/capabilities/history/compatible_jsonl.dart`（claude/flashskyai 共享解析器）；各 CLI 的 `ai_transcript.dart` = `client/lib/services/cli/<cli>/capabilities/history/ai_transcript.dart`；`message.dart` = `client/packages/ai_message_core/lib/src/message.dart`；`subagent_attachment.dart` = `client/packages/ai_message_core/lib/src/subagent_attachment.dart`。交叉校验：本文所有行号均已 grep 回源码确认。

## 矩阵

| 维度 | claude | codex | opencode | cursor | flashskyai | 语义一致？ |
|------|--------|-------|----------|--------|------------|-----------|
| 消息 id 来源与优先级 | `message.id` → 事件 `uuid` → 惰性 fallback `claude-{seq}`（`_logicalMessageId` compatible_jsonl.dart:152-164；fallback 前缀 ai_transcript.dart:74） | 无原生 id，恒 fallback `codex-{seq}`（appendCodexJsonlEvent ai_transcript.dart:95） | db 行 `data.id`，空则整条消息跳过、无 fallback（ai_transcript.dart:534-536,576） | `uuid` → `id` → `message.id` → 惰性 fallback `cursor-{seq}`（`_messageId` ai_transcript.dart:346-355；fallback 前缀 :163） | `message.id` → 事件 `uuid` → 惰性 fallback `flashskyai-{seq}`（共享 `_logicalMessageId` compatible_jsonl.dart:152-164；前缀 ai_transcript.dart:77） | **不一致**（G1） |
| tool part 的 args 形态（Map / 字符串） | `_asArgs(input)`：仅 Map → args，非 Map → args=null（compatible_jsonl.dart:86,214-219） | `_parseArgs(arguments)`：Map 或 **JSON 字符串** → Map，非 JSON 字符串 → args=null（ai_transcript.dart:223,400-422）；custom_tool_call：input Map → args / String → argsText（:254-255） | `_asArgs(state['input'])`：仅 Map → args，非 Map → null（ai_transcript.dart:673,684-689） | `_asArgs(input)`：仅 Map → args，非 Map → null（ai_transcript.dart:256,357-362） | 同 claude（共享 compatible_jsonl.dart） | **不一致**（G2） |
| argsText 保留策略 | 构造时不传 argsText → 恒 null（compatible_jsonl.dart:82-88） | `_argsText`：arguments 为字符串（含非 JSON）原样保留；custom_tool_call input 为 String 时写入 argsText（ai_transcript.dart:254,424-430） | 构造时不传 argsText → 恒 null（ai_transcript.dart:670-677） | 构造时不传 argsText → 恒 null（ai_transcript.dart:252-258） | 同 claude → 恒 null | **不一致**（G2） |
| reasoning 映射（加密 thinking 处理） | `thinking` 块 → AiReasoningPart，trim 空丢弃（compatible_jsonl.dart:73-77） | `response_item.reasoning`（`summary[].type=summary_text`，\n\n 连接）+ `event_msg.agent_reasoning` 双写、相邻去重先到先得（ai_transcript.dart:188-196,271-280,337-364,386-398） | part `reasoning` → AiReasoningPart，trim 空丢弃（ai_transcript.dart:634-637） | **无映射**：block switch 只认 text/tool_use/tool_result，thinking 静默 continue；思考占位 `[REDACTED]` 整块丢弃/尾部剥离（ai_transcript.dart:232-269,321-344） | 解析器同 claude（夹具未实测 thinking） | **不一致**（G3） |
| tool part 挂载角色（是否仅 assistant） | 仅 assistant（compatible_jsonl.dart:113-116） | function_call/custom_tool_call 恒挂 assistant（ai_transcript.dart:215-229,246-260） | 仅 assistant，user 消息的 tool part 丢弃（ai_transcript.dart:639） | 仅 assistant（ai_transcript.dart:285） | 仅 assistant（同 claude） | **一致** |
| toolCallId 来源与 fallback | `blockMap['id']` 必填，空则跳过该 tool（compatible_jsonl.dart:79-80） | `call_id` 必填 string，空则整事件丢弃（ai_transcript.dart:212-213,242-243） | `callID ?? part.id`，两者都空则跳过（ai_transcript.dart:641） | `id` 空 → assistant 事件合成 `{messageId}-tool-{seq}`、user 事件 `user-tool-{seq}`（ai_transcript.dart:246-250） | 同 claude | **不一致**（G4） |
| result 回填机制（enricher） | tool_result 块 → `applyAiToolResult`（compatible_jsonl.dart:89-96,102-111,237-249）+ `ClaudeCompatibleToolResultEnricher` 截断占位回填（`_truncationSentinel` compatible_tool_result_enricher.dart:10,16,33-49；capability 配置 ai_history_capability.dart:15） | function_call_output / custom_tool_call_output → `applyAiToolResult`（ai_transcript.dart:231-239,262-270）；**无截断回填** `NoOpToolResultEnricher`（codex ai_history_capability.dart:13） | result 内联在 part `state`——completed→`state.output`、error→`state.error`，无事件关联（ai_transcript.dart:651-661）；**无截断回填** `NoOpToolResultEnricher`（opencode ai_history_capability.dart:13） | tool_result → `applyAiToolResult`（ai_transcript.dart:259-266,272-281）+ `CursorTerminalToolResultEnricher` 用 `terminals/*.txt` 回填缺失结果（requiresFilesystem=true terminal_tool_result_enricher.dart:19；capability 配置 cursor ai_history_capability.dart:39） | 同 claude（共享 enricher） | **不一致**（G5） |
| status / isError 规范化 | 解析期不设 status（默认 incomplete）；isError = tool_result `is_error`；finalize 兜底（compatible_jsonl.dart:89-96；message.dart:173-194） | 解析期不设 status；**isError 恒 false**（`_applyToolResult` 两处调用硬编码 `isError: false`，ai_transcript.dart:238,269）；`applyAiToolResult` 置 complete | 显式映射：`completed`→complete、`error`→complete+isError=true、`pending`/`running`/空→incomplete、其他→incomplete（ai_transcript.dart:651-668） | 解析期不设；isError = `is_error`（ai_transcript.dart:265）；`applyAiToolResult` 置 complete | 同 claude | **不一致**（G6） |
| 子代理附件（agentId 提取） | `subagents/agent-*.meta.json` 的 toolUseId→agentId 索引 → 回退 `subagentAgentIdFromPart`（part args/result 的 agentId/agent_id）（compatible_side_resolver.dart:30-33；subagent_attachment.dart:159-168,197-207） | 仅 `subagentAgentIdFromPart`（side_resolver.dart:22） | `opencodeChildSessionId`：result Map `sessionId`/`metadata.sessionId` → 文本正则 `<task id="ses_[^"]+">`；运行中 discovery 扫 `session.parent_id`（side_resolver.dart:15-42,352-355） | args `resume`/`agentId`/`agent_id` → `subagentAgentIdFromPart` 回退 → prompt 文本启发式（side_resolver.dart:37-42,138-141） | 同 claude（共享 resolver） | **不一致**（G7） |
| 增量/全量 id 一致性 | lineAppend=`appendClaudeJsonlEvent` + `tailFallbackPrefix='claude'`，fallback 惰性求值 → 增量与全量同 id 序列（claude ai_history_capability.dart:26,29） | 全量/增量共用 `appendCodexJsonlEvent` + `'codex'`（codex ai_history_capability.dart:24,27） | 无 fallback id：消息 id 恒 = db 行 id，增量 locate `id > ?` 与全量同源同 id（ai_transcript.dart:242-303；lineAppend=null opencode ai_history_capability.dart:27） | 全量/增量共用 `appendCursorJsonlEvent` + `'cursor'`（cursor ai_history_capability.dart:27,30） | 同 claude + `'flashskyai'`（flashskyai ai_history_capability.dart:26,29） | **一致**（各 CLI 内部序列稳定） |

## Gap 清单（Task 3-6 的修复依据）

| # | CLI | 维度 | 现状 | 应达到的语义 | 涉及函数 |
|---|-----|------|------|-------------|---------|
| G1 | claude / cursor（两族）+ codex + opencode | 消息 id 来源与优先级 | claude/flashskyai 以 `message.id` 优先（compatible_jsonl.dart:158），cursor 以事件 `uuid` 优先（ai_transcript.dart:347）；codex 恒 fallback `codex-{seq}` 无原生 id（ai_transcript.dart:95）；opencode 消息 id 恒 = db 行 id、空则整条跳过、无 fallback（ai_transcript.dart:534-536） | 统一优先级：事件稳定 id（message.id/uuid）→ CLI 原生 id → 惰性 fallback `{cli}-{seq}`；对「无 id 消息」采取统一策略（丢弃 or 合成），并在消息层文档化该约定 | `_logicalMessageId`（compatible_jsonl.dart:152）、`_messageId`（cursor ai_transcript.dart:346）、`appendCodexJsonlEvent`（codex ai_transcript.dart:119）、`OpencodeAiTranscriptAdapter.parse`（opencode ai_transcript.dart:524） |
| G2 | codex（差异方） | args 形态 / argsText | codex `_parseArgs` 对字符串参数做 `jsonDecode` 兜底、`_argsText` 保留原始字符串、custom_tool_call input 双形态（ai_transcript.dart:254-255,400-430）；其余 4 家 `_asArgs` 仅接受 Map、非 Map 直接 args=null、从不填 argsText（compatible_jsonl.dart:214、cursor ai_transcript.dart:357、opencode ai_transcript.dart:684） | 统一约定：args 恒为 Map（字符串先 jsonDecode，失败则 args=null 且 argsText 保留原串）；argsText 作为字符串输入的忠实副本通道 | `_parseArgs`（codex ai_transcript.dart:400）、`_argsText`（codex ai_transcript.dart:424）、`_asArgs`（compatible_jsonl.dart:214 / cursor ai_transcript.dart:357 / opencode ai_transcript.dart:684） |
| G3 | cursor | reasoning 映射 | block switch 只认 text/tool_use/tool_result，thinking 类块静默 continue；思考位置被 `[REDACTED]` 占位并剥离（ai_transcript.dart:232-269,321-344） | 与其余 CLI 对齐：若父向 transcript 存在 thinking 类块 → AiReasoningPart；`[REDACTED]` 等占位符明确为「不产生 reasoning 输出」的既定语义并文档化（区别于 claude 的空 thinking 丢弃） | `appendCursorJsonlEvent`（cursor ai_transcript.dart:229）、`_cursorVisibleText`（cursor ai_transcript.dart:321） |
| G4 | cursor（有）/ claude、codex（无） | toolCallId fallback | cursor 对无 id tool_use 合成 `{messageId}-tool-{seq}`（assistant）或 `user-tool-{seq}`（user，随即被丢弃）（ai_transcript.dart:246-250）；claude/flashskyai 空 id 跳过该 tool（compatible_jsonl.dart:79-80）；codex 空 call_id 整事件丢弃（ai_transcript.dart:212-213,242-243） | 统一策略：tool_use 缺 id 不静默丢弃——参照 cursor 合成 `{messageId}-tool-{seq}`，保证 tool_result 仍可按合成 id 关联 | `appendCursorJsonlEvent`（cursor ai_transcript.dart:246-250）、`appendClaudeJsonlEvent`（compatible_jsonl.dart:79-80）、`_appendFromResponseItem`（codex ai_transcript.dart:210-213） |
| G5 | claude / cursor / codex+opencode | result 回填机制 | 三族并存：claude/flashskyai 事件块关联 + 截断 sentinel enricher（compatible_tool_result_enricher.dart:10,16）；cursor 事件块关联 + terminals/*.txt 终端回填（terminal_tool_result_enricher.dart:19）；codex/opencode 事件块/state 内联 + `NoOpToolResultEnricher` 无回填（codex/opencode ai_history_capability.dart:13）；opencode 结果内联 part state 而非事件关联（ai_transcript.dart:651-661） | 统一约定：result 关联以 parse 路径为第一通道（事件块/state 内联），enricher 仅作「缺失结果」回填层；截断占位回填语义对齐或文档化 NoOp 差异 | `ClaudeCompatibleToolResultEnricher`（compatible_tool_result_enricher.dart:33-49）、`CursorTerminalToolResultEnricher`（terminal_tool_result_enricher.dart:19）、`NoOpToolResultEnricher`（tool_result_enricher.dart:24） |
| G6 | codex | status / isError 规范化 | `_applyToolResult` 两处调用硬编码 `isError: false`（function_call_output / custom_tool_call_output，ai_transcript.dart:238,269），失败输出以纯文本进 result（如 `Exit code: 1`，夹具实测），isError 恒 false；status 解析期不设 | 与 claude/cursor 一致：错误形态可推导时置 isError（如 output 文本前缀/退出码形态），保留失败标记通道而非恒 false | `_appendFromResponseItem`（codex ai_transcript.dart:231-239,262-270）、`_applyToolResult`（codex ai_transcript.dart:432） |
| G7 | cursor / opencode（差异方） | 子代理 agentId 提取 | claude/flashskyai：meta.json 索引 + part 字段回退（compatible_side_resolver.dart:30-33）；codex：仅 part 字段（side_resolver.dart:22）；cursor：args resume/agentId/agent_id + prompt 文本启发式（side_resolver.dart:138-141）；opencode：result sessionId + `parent_id` 表扫描（side_resolver.dart:17-42,352-355） | 统一优先级：part args/result 显式 id（`subagentAgentIdFromPart`）→ CLI 专属通道（meta 索引 / parent_id 表 / 目录指纹）→ prompt 启发式为最后回退且须可验证 | `subagentAgentIdFromPart`（subagent_attachment.dart:159）、`ClaudeCompatibleSideResolver.resolve`（compatible_side_resolver.dart:30-33）、`_resumeUuidFromPart`（cursor side_resolver.dart:138）、`opencodeChildSessionId`（opencode side_resolver.dart:17） |
