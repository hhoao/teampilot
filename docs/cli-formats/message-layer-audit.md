# 消息层差异矩阵

**日期:** 2026-08-12
**来源:** 格式参考库各页 + 5 个 adapter 源码 + `ai_message_core`（message.dart / subagent_attachment.dart）
**状态:** ✅ 已完成（Task 8 回填结论；Task 3-7 修复 + 裁决全部落账）

行号约定：`compatible_jsonl.dart` = `client/lib/services/cli/claude/capabilities/history/compatible_jsonl.dart`（claude/flashskyai 共享解析器）；各 CLI 的 `ai_transcript.dart` = `client/lib/services/cli/<cli>/capabilities/history/ai_transcript.dart`；`message.dart` = `client/packages/ai_message_core/lib/src/message.dart`；`subagent_attachment.dart` = `client/packages/ai_message_core/lib/src/subagent_attachment.dart`。交叉校验：本文所有行号均已 grep 回源码确认。

## 矩阵

| 维度 | claude | codex | opencode | cursor | flashskyai | 语义一致？ |
|------|--------|-------|----------|--------|------------|-----------|
| 消息 id 来源与优先级 | `message.id` → 事件 `uuid` → 惰性 fallback `claude-{seq}`（`_logicalMessageId` compatible_jsonl.dart:152-164；fallback 前缀 ai_transcript.dart:74） | 无原生 id，恒 fallback `codex-{seq}`（appendCodexJsonlEvent ai_transcript.dart:95） | db 行 `data.id`，空则整条消息跳过、无 fallback（ai_transcript.dart:534-536,576） | `uuid` → `id` → `message.id` → 惰性 fallback `cursor-{seq}`（`_messageId` ai_transcript.dart:346-355；fallback 前缀 :163） | `message.id` → 事件 `uuid` → 惰性 fallback `flashskyai-{seq}`（共享 `_logicalMessageId` compatible_jsonl.dart:152-164；前缀 ai_transcript.dart:77） | ✅ 接受差异（G1 既定语义，唯一性/一致性契约断言 + 4 家固化断言） |
| tool part 的 args 形态（Map / 字符串） | `_asArgs(input)`：仅 Map → args，非 Map → args=null（compatible_jsonl.dart:86,214-219） | `_parseArgs(arguments)`：Map 或 **JSON 字符串** → Map，非 JSON 字符串 → args=null（ai_transcript.dart:223,400-422）；custom_tool_call：input Map → args / String → argsText（:254-255） | `_asArgs(state['input'])`：仅 Map → args，非 Map → null（ai_transcript.dart:673,684-689） | `_asArgs(input)`：仅 Map → args，非 Map → null（ai_transcript.dart:256,357-362） | 同 claude（共享 compatible_jsonl.dart） | ✅ 已修复（G2：codex custom_tool_call String JSON 解码，commit f20e6972；其余既定语义 + 固化断言） |
| argsText 保留策略 | 构造时不传 argsText → 恒 null（compatible_jsonl.dart:82-88） | `_argsText`：arguments 为字符串（含非 JSON）原样保留；custom_tool_call input 为 String 时写入 argsText（ai_transcript.dart:254,424-430） | 构造时不传 argsText → 恒 null（ai_transcript.dart:670-677） | 构造时不传 argsText → 恒 null（ai_transcript.dart:252-258） | 同 claude → 恒 null | ✅ 接受差异（G2 既定语义：argsText 仅 codex 作为字符串输入忠实副本通道，其余恒 null；契约断言 args 为 Map 或 null） |
| reasoning 映射（加密 thinking 处理） | `thinking` 块 → AiReasoningPart，trim 空丢弃（compatible_jsonl.dart:73-77） | `response_item.reasoning`（`summary[].type=summary_text`，\n\n 连接）+ `event_msg.agent_reasoning` 双写、相邻去重先到先得（ai_transcript.dart:188-196,271-280,337-364,386-398） | part `reasoning` → AiReasoningPart，trim 空丢弃（ai_transcript.dart:634-637） | **无映射**：block switch 只认 text/tool_use/tool_result，thinking 静默 continue；思考占位 `[REDACTED]` 整块丢弃/尾部剥离（ai_transcript.dart:232-269,321-344） | 解析器同 claude（夹具未实测 thinking） | ✅ 接受差异（G3 既定语义：cursor 真实数据无 thinking 块，`[REDACTED]` 不产出 reasoning；claude/flashskyai 加密 thinking 丢弃已固化断言） |
| tool part 挂载角色（是否仅 assistant） | 仅 assistant（compatible_jsonl.dart:113-116） | function_call/custom_tool_call 恒挂 assistant（ai_transcript.dart:215-229,246-260） | 仅 assistant，user 消息的 tool part 丢弃（ai_transcript.dart:639） | 仅 assistant（ai_transcript.dart:285） | 仅 assistant（同 claude） | ✅ 一致 |
| toolCallId 来源与 fallback | `blockMap['id']` 必填，空则跳过该 tool（compatible_jsonl.dart:79-80） | `call_id` 必填 string，空则整事件丢弃（ai_transcript.dart:212-213,242-243） | `callID ?? part.id`，两者都空则跳过（ai_transcript.dart:641） | `id` 空 → assistant 事件合成 `{messageId}-tool-{seq}`、user 事件 `user-tool-{seq}`（ai_transcript.dart:246-250） | 同 claude | ✅ 接受差异（G4 既定语义：各 CLI 数据形态差异，契约要求 toolCallId 非空 + 固化断言，claude/codex 空 id 属损坏数据） |
| result 回填机制（enricher） | tool_result 块 → `applyAiToolResult`（compatible_jsonl.dart:89-96,102-111,237-249）+ `ClaudeCompatibleToolResultEnricher` 截断占位回填（`_truncationSentinel` compatible_tool_result_enricher.dart:10,16,33-49；capability 配置 ai_history_capability.dart:15） | function_call_output / custom_tool_call_output → `applyAiToolResult`（ai_transcript.dart:231-239,262-270）；**无截断回填** `NoOpToolResultEnricher`（codex ai_history_capability.dart:13） | result 内联在 part `state`——completed→`state.output`、error→`state.error`，无事件关联（ai_transcript.dart:651-661）；**无截断回填** `NoOpToolResultEnricher`（opencode ai_history_capability.dart:13） | tool_result → `applyAiToolResult`（ai_transcript.dart:259-266,272-281）+ `CursorTerminalToolResultEnricher` 用 `terminals/*.txt` 回填缺失结果（requiresFilesystem=true terminal_tool_result_enricher.dart:19；capability 配置 cursor ai_history_capability.dart:39） | 同 claude（共享 enricher） | ✅ 接受差异（G5 既定语义：parse 为第一通道 + enricher 仅作缺失回填层；opencode 内联 state 与 NoOp 差异文档化，既有断言覆盖） |
| status / isError 规范化 | 解析期不设 status（默认 incomplete）；isError = tool_result `is_error`；finalize 兜底（compatible_jsonl.dart:89-96；message.dart:173-194） | 解析期不设 status；**isError 恒 false**（`_applyToolResult` 两处调用硬编码 `isError: false`，ai_transcript.dart:238,269）；`applyAiToolResult` 置 complete | 显式映射：`completed`→complete、`error`→complete+isError=true、`pending`/`running`/空→incomplete、其他→incomplete（ai_transcript.dart:651-668） | 解析期不设；isError = `is_error`（ai_transcript.dart:265）；`applyAiToolResult` 置 complete | 同 claude | ✅ 接受差异（G6 既定语义：codex 真实 rollout 无错误标志字段，isError 恒 false 固化断言；消息级 status 属 G6b 裁决——app 管理流式标志，不改） |
| 子代理附件（agentId 提取） | `subagents/agent-*.meta.json` 的 toolUseId→agentId 索引 → 回退 `subagentAgentIdFromPart`（part args/result 的 agentId/agent_id）（compatible_side_resolver.dart:30-33；subagent_attachment.dart:159-168,197-207） | 仅 `subagentAgentIdFromPart`（side_resolver.dart:22） | `opencodeChildSessionId`：result Map `sessionId`/`metadata.sessionId` → 文本正则 `<task id="ses_[^"]+">`；运行中 discovery 扫 `session.parent_id`（side_resolver.dart:15-42,352-355） | args `resume`/`agentId`/`agent_id` → `subagentAgentIdFromPart` 回退 → prompt 文本启发式（side_resolver.dart:37-42,138-141） | 同 claude（共享 resolver） | ✅ 已修复 + 接受差异（G7：opencode `<task id>` 正则容忍属性修复，commit 10d36f78；其余既定语义 + 固化断言） |
| 增量/全量 id 一致性 | lineAppend=`appendClaudeJsonlEvent` + `tailFallbackPrefix='claude'`，fallback 惰性求值 → 增量与全量同 id 序列（claude ai_history_capability.dart:26,29） | 全量/增量共用 `appendCodexJsonlEvent` + `'codex'`（codex ai_history_capability.dart:24,27） | 无 fallback id：消息 id 恒 = db 行 id，增量 locate `id > ?` 与全量同源同 id（ai_transcript.dart:242-303；lineAppend=null opencode ai_history_capability.dart:27） | 全量/增量共用 `appendCursorJsonlEvent` + `'cursor'`（cursor ai_history_capability.dart:27,30） | 同 claude + `'flashskyai'`（flashskyai ai_history_capability.dart:26,29） | ✅ 已验证（D10 运行时闭环，见下） |

## Gap 清单（Task 3-6 的修复依据 + Task 8 状态回填）

状态标注：**已修复** = 有生产代码改动 + commit；**既定语义 + 固化断言** = 核验确认无真实缺口，以测试断言锁定既有语义（契约测试自 Task 2 起 5/5 全绿）；**裁决（不改）** = 公共层裁决，见 G6b。

| # | CLI | 维度 | 现状 | 应达到的语义 | 涉及函数 | 状态 |
|---|-----|------|------|-------------|---------|------|
| G1 | claude / cursor（两族）+ codex + opencode | 消息 id 来源与优先级 | claude/flashskyai 以 `message.id` 优先（compatible_jsonl.dart:158），cursor 以事件 `uuid` 优先（ai_transcript.dart:347）；codex 恒 fallback `codex-{seq}` 无原生 id（ai_transcript.dart:95）；opencode 消息 id 恒 = db 行 id、空则整条跳过、无 fallback（ai_transcript.dart:534-536） | 统一优先级：事件稳定 id（message.id/uuid）→ CLI 原生 id → 惰性 fallback `{cli}-{seq}`；对「无 id 消息」采取统一策略（丢弃 or 合成），并在消息层文档化该约定 | `_logicalMessageId`（compatible_jsonl.dart:152）、`_messageId`（cursor ai_transcript.dart:346）、`appendCodexJsonlEvent`（codex ai_transcript.dart:119）、`OpencodeAiTranscriptAdapter.parse`（opencode ai_transcript.dart:524） | **既定语义 + 固化断言**：契约要求性质（id 唯一、增量/全量一致）而非相同优先级；5 家（claude/flashskyai/codex/cursor/opencode）均已加断言锁定各自优先级链（Task 3/4/5/6 各 commit） |
| G2 | codex（差异方） | args 形态 / argsText | codex `_parseArgs` 对字符串参数做 `jsonDecode` 兜底、`_argsText` 保留原始字符串、custom_tool_call input 双形态（ai_transcript.dart:254-255,400-430）；其余 4 家 `_asArgs` 仅接受 Map、非 Map 直接 args=null、从不填 argsText（compatible_jsonl.dart:214、cursor ai_transcript.dart:357、opencode ai_transcript.dart:684） | 统一约定：args 恒为 Map（字符串先 jsonDecode，失败则 args=null 且 argsText 保留原串）；argsText 作为字符串输入的忠实副本通道 | `_parseArgs`（codex ai_transcript.dart:400）、`_argsText`（codex ai_transcript.dart:424）、`_asArgs`（compatible_jsonl.dart:214 / cursor ai_transcript.dart:357 / opencode ai_transcript.dart:684） | **已修复（部分）**：codex custom_tool_call String JSON 解码（`args: _parseArgs(input)`，commit `f20e6972`）+ 3 条固化断言；其余 4 家「非 Map → args=null」为既定语义 + 断言锁定（Task 3/4/5） |
| G3 | cursor | reasoning 映射 | block switch 只认 text/tool_use/tool_result，thinking 类块静默 continue；思考位置被 `[REDACTED]` 占位并剥离（ai_transcript.dart:232-269,321-344） | 与其余 CLI 对齐：若父向 transcript 存在 thinking 类块 → AiReasoningPart；`[REDACTED]` 等占位符明确为「不产生 reasoning 输出」的既定语义并文档化（区别于 claude 的空 thinking 丢弃） | `appendCursorJsonlEvent`（cursor ai_transcript.dart:229）、`_cursorVisibleText`（cursor ai_transcript.dart:321） | **既定语义 + 固化断言**：真实 100+ transcript 扫描 thinking 块出现 0 次，`[REDACTED]` 为数据形态本身，无 thinking 可映射；断言锁定「thinking 块不产出 reasoning + `[REDACTED]` 不浮现」（Task 4，commit 42d61860） |
| G4 | cursor（有）/ claude、codex（无） | toolCallId fallback | cursor 对无 id tool_use 合成 `{messageId}-tool-{seq}`（assistant）或 `user-tool-{seq}`（user，随即被丢弃）（ai_transcript.dart:246-250）；claude/flashskyai 空 id 跳过该 tool（compatible_jsonl.dart:79-80）；codex 空 call_id 整事件丢弃（ai_transcript.dart:212-213,242-243） | 统一策略：tool_use 缺 id 不静默丢弃——参照 cursor 合成 `{messageId}-tool-{seq}`，保证 tool_result 仍可按合成 id 关联 | `appendCursorJsonlEvent`（cursor ai_transcript.dart:246-250）、`appendClaudeJsonlEvent`（compatible_jsonl.dart:79-80）、`_appendFromResponseItem`（codex ai_transcript.dart:210-213） | **既定语义 + 固化断言**：cursor 合成 id 是真实数据常态（240k tool_use 全无 id，断言锁定格式+唯一）；claude/codex 空 id 属损坏数据（真实扫描 0 例），单侧合成无法恢复关联，跳过/丢弃为既定语义（Task 3/4/6） |
| G5 | claude / cursor / codex+opencode | result 回填机制 | 三族并存：claude/flashskyai 事件块关联 + 截断 sentinel enricher（compatible_tool_result_enricher.dart:10,16）；cursor 事件块关联 + terminals/*.txt 终端回填（terminal_tool_result_enricher.dart:19）；codex/opencode 事件块/state 内联 + `NoOpToolResultEnricher` 无回填（codex/opencode ai_history_capability.dart:13）；opencode 结果内联 part state 而非事件关联（ai_transcript.dart:651-661） | 统一约定：result 关联以 parse 路径为第一通道（事件块/state 内联），enricher 仅作「缺失结果」回填层；截断占位回填语义对齐或文档化 NoOp 差异 | `ClaudeCompatibleToolResultEnricher`（compatible_tool_result_enricher.dart:33-49）、`CursorTerminalToolResultEnricher`（terminal_tool_result_enricher.dart:19）、`NoOpToolResultEnricher`（tool_result_enricher.dart:24） | **既定语义 + 固化断言**：parse 为第一通道、enricher 仅回填缺失结果（契约断言「有 result 非 running」三族全部满足）；claude 补 `status == complete` parse 路径锁定、cursor/opencode 状态机断言（Task 4/5/6） |
| G6 | codex | status / isError 规范化 | `_applyToolResult` 两处调用硬编码 `isError: false`（function_call_output / custom_tool_call_output，ai_transcript.dart:238,269），失败输出以纯文本进 result（如 `Exit code: 1`，夹具实测），isError 恒 false；status 解析期不设 | 与 claude/cursor 一致：错误形态可推导时置 isError（如 output 文本前缀/退出码形态），保留失败标记通道而非恒 false | `_appendFromResponseItem`（codex ai_transcript.dart:231-239,262-270）、`_applyToolResult`（codex ai_transcript.dart:432） | **既定语义 + 固化断言**：真实 rollout 的 function_call_output/custom_tool_call_output 只有 call_id + output 两字段、无错误标志字段，失败形态以纯文本进 output 不做解析——isError 恒 false 为数据形态决定；断言锁定（Task 3，commit f20e6972） |
| G7 | cursor / opencode（差异方） | 子代理 agentId 提取 | claude/flashskyai：meta.json 索引 + part 字段回退（compatible_side_resolver.dart:30-33）；codex：仅 part 字段（side_resolver.dart:22）；cursor：args resume/agentId/agent_id + prompt 文本启发式（side_resolver.dart:138-141）；opencode：result sessionId + `parent_id` 表扫描（side_resolver.dart:17-42,352-355） | 统一优先级：part args/result 显式 id（`subagentAgentIdFromPart`）→ CLI 专属通道（meta 索引 / parent_id 表 / 目录指纹）→ prompt 启发式为最后回退且须可验证 | `subagentAgentIdFromPart`（subagent_attachment.dart:159）、`ClaudeCompatibleSideResolver.resolve`（compatible_side_resolver.dart:30-33）、`_resumeUuidFromPart`（cursor side_resolver.dart:138）、`opencodeChildSessionId`（opencode side_resolver.dart:17） | **已修复（部分）**：opencode `<task id>` 正则容忍 `state` 等属性（`[^>]*`，真实数据 47/47 命中原先 0/47），commit `10d36f78`；其余既定语义 + 既有 inflater 覆盖确认（Task 5/6） |
| G6b | 全部（ai_message_core 公共层） | 消息级 status 推导（finalize 只规范 part 级 status） | `finalizeAiMessagesForHistory`（message.dart:173-194）只规范化 part 级 status；adapter 构造 `AiMessage` 不设 status，恒走默认 `complete`（message.dart:110），finalize 原样保留 | **既定语义，不改（Task 7 裁决）**：消息级 status 是 **app 管理的流式标志**——UI streaming 判断 `status == incomplete`（ai_message_view.dart:86，驱动 text_part_view.dart:29-31 的 80ms markdown 节流）、非 complete 渲染错误风格「incomplete」横幅（ai_message_view.dart:162-170,370）；app 仅在合成 running 占位消息显式置 `incomplete`（session_history_thread.dart:30）。finalize 注释明示「不发明磁盘 transcript 的 running 状态」（message.dart:171-172）；part 级 status 已忠实承载未配对 tool 信号（running/无 result 归一为 incomplete，message.dart:184-188），tool 卡片按 part 级渲染（tool_call_part_view.dart:549）。若在 finalize 推导，截断 transcript（如 truncated_bash.jsonl）已完成的历史消息将永久呈现 streaming 节流 + incomplete 错误横幅 | `finalizeAiMessagesForHistory`（message.dart:173） | **裁决（不改）**：commit `17d73cc4`；契约测试 finalize 段改为严格断言 `status == AiMessageStatus.complete`；生产代码零改动 |

## D10 增量/全量 id 一致性 — 运行时验证闭环（Task 8）

Task 1 遗留的运行时验证（`client/test/services/cli/registry/capabilities/history/` 下）：

| CLI | 验证内容 | 测试文件 | 结果 |
|-----|---------|---------|------|
| claude | 契约测试 + lineAppend 重放 = 全量 parse 零分叉 | `message_layer_contract_test.dart`、`line_append_test.dart` | ✅ 通过 |
| codex | 契约测试 + lineAppend 重放 = 全量 parse 零分叉 | `message_layer_contract_test.dart`、`line_append_test.dart` | ✅ 通过 |
| cursor | 契约测试 + lineAppend 重放 = 全量 parse 零分叉 | `message_layer_contract_test.dart`、`line_append_test.dart` | ✅ 通过 |
| opencode | 契约测试 + 增量 locate `id > ?` 产出 = 全量窗口子集（同源同 id） | `message_layer_contract_test.dart`、`opencode_ai_transcript_test.dart`（`full and incremental sqlite locate share the same message ids (G1)`，:273） | ✅ 通过 |
| flashskyai | 契约测试 + fallback 序列（`flashskyai-0/-1`）严格顺序、noise 行不占号（与 claude 共享 `appendClaudeJsonlEvent`） | `message_layer_contract_test.dart`、`flashskyai_ai_transcript_test.dart`（`G1: shared parser assigns flashskyai fallback ids only to consumed events`） | ✅ 通过 |

结论：**D10 一致为运行时验证确认**——5 家增量路径与全量 parse 产出同一 id 序列（jsonl 族共用 lineAppend 函数 + 惰性 fallback；opencode 恒 db 行 id 天然一致），契约断言「id 非空 + 唯一」在增量/全量两侧同时满足。

## 已知陷阱（版本 caveat，Task 4/6 遗留补注）

以下「真实数据核验」结论基于本机快照，**未来 CLI 版本升级后需重开核验**（测试注释未注扫描版本，仅本矩阵留档）：

| CLI | 扫描范围 | 快照版本 | 结论受版本影响项 |
|-----|---------|---------|-----------------|
| cursor | `~/.cursor/projects/**/agent-transcripts/*.jsonl` 全部 100+ 文件 | 当前本机 cursor 版本（版本号未记录） | G1（事件 uuid/id 恒缺）、G3（thinking 块 0 次）、G4（tool_use id 恒缺 → 合成 id 常态） |
| claude | `~/.claude/projects` 103 文件 / 11320 事件 | **claude 2.1.156** | G3（加密 thinking 为 null/空，389/2297）、G4（id-less tool_use 0 例）、G1（fallback 恒不触发） |
| flashskyai | `~/.flashskyai/projects` 26 文件 / 140 事件 | **flashskyai 1.0.18** | G3/G4 共享 claude 解析器；**无 `agent-*.meta.json`**——子代理 inflate 在真实数据上恒走 degrade/part 回退（非 resolver 缺口，数据布局现状） |
| codex | `~/.codex/sessions` 46+ rollout 文件 | 2026-03~05 版本 | G4（空 call_id 0 例）、G6（无错误标志字段 → isError 恒 false） |
| opencode | `~/.local/share/opencode/opencode.db` 5578 条 message | 当前本机 opencode 版本（版本号未记录） | G1（data.id 恒缺 → db 行 id 唯一通道）、G7（task 输出 `<task id state>` 包裹形态 47/47，正则已容忍） |

若未来版本引入新字段形态（如 cursor 父向 transcript 出现 thinking 块、codex 增加 is_error 标志、opencode 改变 task 包裹形态），对应 Gap 的既定语义需重开核验。
