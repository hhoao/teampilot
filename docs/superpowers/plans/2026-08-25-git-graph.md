# Git Graph（浮动工作区）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 TeamPilot 内置 IDE 的浮动工作区中提供 Git 提交图（DAG 可视化）、提交详情/diff、分支/标签/stash/提交级操作与历史搜索，对齐 neo-git-graph 能力。

**Architecture:** 解析 `git log --graph --date-order --pretty=format:%x1e…%x1f…` 的 ASCII 拓扑为结构化行模型（git 保证拓扑正确性），CustomPainter 渲染 lane；新 `GitHistoryService`（读）/`GitHistoryActions`（写）经现有 `GitCommandRunner` 三后端执行；`GitGraphCubit` 由 `GitRepoStore` LRU 托管；UI 以 `GitGraphFloatingSurface` 注册进浮动面板。

**Tech Stack:** Flutter + flutter_bloc、现有 git CLI 执行层（零新依赖）、shared_ui Tp 组件、DiffViewer。

**Spec:** `docs/superpowers/specs/2026-08-25-git-graph-design.md`

## Global Constraints

- 所有命令在 `client/` 下执行。全量验证：`flutter analyze --no-fatal-infos --no-fatal-warnings && dart run tool/run_tests.dart`；单测：`flutter test test/<path>_test.dart`。
- UI 层禁止 IO/Process.run/裸路径；状态只用 flutter_bloc；用户可见文案全部进 `client/lib/l10n/app_en.arb` 与 `app_zh.arb`（两份都要加）；诊断日志用 `AppLogger`，禁止 print。
- 文件软限：pages ~400 行 / cubits ~500 行 / services ~600 行；超限拆分文件。
- git 命令一律经 `GitCommandRunner.runInDirectory(dir, args)`；非零退出抛 `GitException(stderr)`。
- 测试不 spawn 真实 git：服务层注入 fake `ProcessRunner`（模式见 `test/services/git/git_service_test.dart:12` 的 `_FakeRunner`）。
- 每个 Task 结束必须 commit；消息风格 `feat(git-graph): …`。

## File Structure

```
Create:
  client/lib/models/git_graph.dart                           # 全部纯模型
  client/lib/services/git/parser/git_decoration_parser.dart   # %d → decorations
  client/lib/services/git/parser/git_graph_parser.dart        # ASCII → 行模型
  client/lib/services/git/git_history_service.dart            # 只读历史查询
  client/lib/services/git/git_history_actions.dart            # 历史写操作
  client/lib/cubits/git_graph_cubit.dart                      # 核心状态/刷新/分页/选中/搜索
  client/lib/cubits/git_graph_cubit_actions.dart              # cubit 写操作扩展
  client/lib/services/floating_workspace/surfaces/git_graph_floating_surface.dart
  client/lib/pages/git_graph/open_git_graph.dart              # 打开入口 helper + 命令注册
  client/lib/pages/git_graph/git_graph_pane.dart              # 面板骨架(工具条+列表+详情)
  client/lib/pages/git_graph/git_graph_toolbar.dart           # 工具条(分支过滤/fetch/pull/push/stash/搜索)
  client/lib/pages/git_graph/git_graph_row_tile.dart          # 单行(图形片元+文本列)
  client/lib/pages/git_graph/git_graph_lane_painter.dart      # CustomPainter
  client/lib/pages/git_graph/git_graph_detail_pane.dart       # 详情栏+内嵌 diff
  client/lib/pages/git_graph/git_graph_menus.dart             # 右键菜单+确认对话框
Modify:
  client/lib/cubits/workbench/workbench_tab.dart:3,7          # 新 kind + surfaceIdFor + factory
  client/lib/pages/floating_workspace/floating_workspace_tab_bar.dart:16,129 # identity/icon
  client/lib/services/floating_workspace/floating_surface_registry.dart:7     # withDefaults 参数
  client/lib/app/app_shell.dart:2103                          # surface 装配
  client/lib/services/git/git_repo_store.dart                 # graphCubitFor 托管
  client/lib/widgets/git/git_source_control_panel.dart:732    # _Header 入口按钮
Test:
  client/test/services/git/parser/*_test.dart
  client/test/services/git/git_history_*_test.dart
  client/test/cubits/git_graph_cubit_test.dart
  client/test/pages/git_graph/*_test.dart
```

---

### Task 1: 数据模型 + decoration 解析器

**Files:**
- Create: `client/lib/models/git_graph.dart`
- Create: `client/lib/services/git/parser/git_decoration_parser.dart`
- Test: `client/test/services/git/parser/git_decoration_parser_test.dart`

**Interfaces (Produces，后续任务按此引用):**

```dart
enum GitSearchMode { message, author, hash }
enum GitResetMode { soft, mixed, hard }
enum GitRefDecorationKind { head, localBranch, remoteBranch, tag }

class GitRefDecoration extends Equatable {
  const GitRefDecoration(this.kind, this.name);
  final GitRefDecorationKind kind; // head 时 name 可为 ''(detached)
  final String name;
}

class GitGraphNode extends Equatable {
  const GitGraphNode(this.lane, this.colorIndex);
  final int lane; final int colorIndex;
}

/// 一行内一段连线：from=行顶 lane，to=行底 lane；相等画竖线，不等画 S 曲线。
class GitGraphEdge extends Equatable {
  const GitGraphEdge(this.fromLane, this.toLane, this.colorIndex);
  final int fromLane; final int toLane; final int colorIndex;
  bool get isStraight => fromLane == toLane;
}

sealed class GitGraphRow extends Equatable {
  const GitGraphRow({required this.edges});
  final List<GitGraphEdge> edges;
}

class GitCommitRow extends GitGraphRow {
  const GitCommitRow({ required super.edges, required this.node,
    required this.hash, required this.parents, required this.authorName,
    required this.authorEmail, required this.authorDate, required this.subject,
    required this.refs });
  final GitGraphNode node; final String hash; final List<String> parents;
  final String authorName; final String authorEmail; final DateTime authorDate;
  final String subject; final List<GitRefDecoration> refs;
}

/// 无提交记录的纯拓扑行（merge 的 |\ 、|/ 等），UI 渲染为半高连线行。
class GitGraphSpacerRow extends GitGraphRow {
  const GitGraphSpacerRow({required super.edges});
}

List<GitRefDecoration> parseGitDecorations(String raw,
    {Set<String> remotePrefixes = const {'origin/'}});
```

- [ ] **Step 1: 写失败测试**

```dart
// client/test/services/git/parser/git_decoration_parser_test.dart
import 'package:flutter_test/flutter_test.dart';
import 'package:teampilot/models/git_graph.dart';
import 'package:teampilot/services/git/parser/git_decoration_parser.dart';

void main() {
  group('parseGitDecorations', () {
    test('parses HEAD branch, remote and tag', () {
      final refs = parseGitDecorations(
        '(HEAD -> main, origin/main, tag: v1.0)',
        remotePrefixes: const {'origin/'},
      );
      expect(refs, const [
        GitRefDecoration(GitRefDecorationKind.head, 'main'),
        GitRefDecoration(GitRefDecorationKind.remoteBranch, 'origin/main'),
        GitRefDecoration(GitRefDecorationKind.tag, 'v1.0'),
      ]);
    });

    test('detached HEAD alone', () {
      expect(parseGitDecorations('(HEAD)'), const [
        GitRefDecoration(GitRefDecorationKind.head, ''),
      ]);
    });

    test('local branch with slash stays local when not a known remote prefix',
        () {
      final refs =
          parseGitDecorations('(feature/x)', remotePrefixes: {'origin/'});
      expect(refs.single.kind, GitRefDecorationKind.localBranch);
      expect(refs.single.name, 'feature/x');
    });

    test('falls back to origin/ heuristic without prefixes', () {
      final refs = parseGitDecorations('(origin/main)');
      expect(refs.single.kind, GitRefDecorationKind.remoteBranch);
    });

    test('empty or missing parens yield empty list', () {
      expect(parseGitDecorations(''), isEmpty);
      expect(parseGitDecorations('no-decor'), isEmpty);
      expect(parseGitDecorations('()'), isEmpty);
    });
  });
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd client && flutter test test/services/git/parser/git_decoration_parser_test.dart`
Expected: FAIL（找不到 package:teampilot/models/git_graph.dart）

- [ ] **Step 3: 最小实现**

创建 `client/lib/models/git_graph.dart`：

```dart
import 'package:equatable/equatable.dart';

enum GitSearchMode { message, author, hash }

enum GitResetMode { soft, mixed, hard }

enum GitRefDecorationKind { head, localBranch, remoteBranch, tag }

class GitRefDecoration extends Equatable {
  const GitRefDecoration(this.kind, this.name);

  final GitRefDecorationKind kind;
  final String name;

  @override
  List<Object?> get props => [kind, name];
}

/// 图上单个提交节点：所在 lane + 解析期分配的调色板序号。
class GitGraphNode extends Equatable {
  const GitGraphNode(this.lane, this.colorIndex);

  final int lane;
  final int colorIndex;

  @override
  List<Object?> get props => [lane, colorIndex];
}

/// 一行内的一段连线：from=行顶 lane，to=行底 lane；相等画竖线，不等画 S 曲线。
class GitGraphEdge extends Equatable {
  const GitGraphEdge(this.fromLane, this.toLane, this.colorIndex);

  final int fromLane;
  final int toLane;
  final int colorIndex;

  bool get isStraight => fromLane == toLane;

  @override
  List<Object?> get props => [fromLane, toLane, colorIndex];
}

sealed class GitGraphRow extends Equatable {
  const GitGraphRow({required this.edges});

  final List<GitGraphEdge> edges;

  @override
  List<Object?> get props => [edges];
}

/// 有提交记录的行。
class GitCommitRow extends GitGraphRow {
  const GitCommitRow({
    required super.edges,
    required this.node,
    required this.hash,
    required this.parents,
    required this.authorName,
    required this.authorEmail,
    required this.authorDate,
    required this.subject,
    required this.refs,
  });

  final GitGraphNode node;
  final String hash;
  final List<String> parents;
  final String authorName;
  final String authorEmail;
  final DateTime authorDate;
  final String subject;
  final List<GitRefDecoration> refs;

  @override
  List<Object?> get props => [
        ...super.props,
        node,
        hash,
        parents,
        authorName,
        authorEmail,
        authorDate,
        subject,
        refs,
      ];
}

/// 无提交记录的纯拓扑行（merge 的 |\ 、|/ 等），渲染为半高连线。
class GitGraphSpacerRow extends GitGraphRow {
  const GitGraphSpacerRow({required super.edges});
}
```

注意：注释里的 `|\` 在 Dart 注释中无特殊含义，直接写即可。

创建 `client/lib/services/git/parser/git_decoration_parser.dart`：

```dart
import '../../../models/git_graph.dart';

/// 把 `%d` 输出（形如 `(HEAD -> main, origin/main, tag: v1.0)`）解析为装饰列表。
///
/// 远端分支判定优先用 [remotePrefixes]（来自 `git remote` 加 `/` 后缀）；
/// 缺省时退回 `origin/` 启发式。
List<GitRefDecoration> parseGitDecorations(
  String raw, {
  Set<String> remotePrefixes = const {'origin/'},
}) {
  final body = raw.trim();
  if (!body.startsWith('(') || !body.endsWith(')')) return const [];
  final inner = body.substring(1, body.length - 1).trim();
  if (inner.isEmpty) return const [];
  final out = <GitRefDecoration>[];
  for (final part in inner.split(',')) {
    final token = part.trim();
    if (token.isEmpty) continue;
    if (token == 'HEAD') {
      out.add(const GitRefDecoration(GitRefDecorationKind.head, ''));
    } else if (token.startsWith('HEAD -> ')) {
      out.add(GitRefDecoration(
        GitRefDecorationKind.head,
        token.substring('HEAD -> '.length).trim(),
      ));
    } else if (token.startsWith('tag: ')) {
      out.add(GitRefDecoration(
        GitRefDecorationKind.tag,
        token.substring('tag: '.length).trim(),
      ));
    } else if (remotePrefixes.any((p) => token.startsWith(p))) {
      out.add(GitRefDecoration(GitRefDecorationKind.remoteBranch, token));
    } else {
      out.add(GitRefDecoration(GitRefDecorationKind.localBranch, token));
    }
  }
  return out;
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd client && flutter test test/services/git/parser/git_decoration_parser_test.dart`
Expected: PASS（5 tests）

- [ ] **Step 5: Commit**

```bash
git add client/lib/models/git_graph.dart \
  client/lib/services/git/parser/git_decoration_parser.dart \
  client/test/services/git/parser/git_decoration_parser_test.dart
git commit -m "feat(git-graph): add graph models and ref decoration parser"
```

---

### Task 2: ASCII 图解析器

**Files:**
- Create: `client/lib/services/git/parser/git_graph_parser.dart`
- Test: `client/test/services/git/parser/git_graph_parser_test.dart`

**Interfaces:**
- Consumes: Task 1 全部模型。
- Produces:

```dart
class GitGraphParser {
  /// 解析 `git log --graph --pretty=format:%x1e%H%x1f%P%x1f%an%x1f%ae%x1f%at%x1f%d%x1f%s`
  /// 的 stdout 为行模型列表。记录分隔符 \x1e、字段分隔符 \x1f。
  static List<GitGraphRow> parse(String stdout,
      {Set<String> remotePrefixes = const {'origin/'}});
}
```

**解析规则（lane 宽度 = 2 字符）：**
- 字符索引 p 处：`*` → 节点 lane `p ~/ 2`（并补一条同 lane 直通边）；`|` → 直通边 lane `p ~/ 2`；
  `\` → 曲线 `(p ~/ 2) → (p ~/ 2) + 1`；`/` → 曲线 `(p ~/ 2) + 1 → (p ~/ 2)`；
  `-`、`_`、空格 → 跳过；同一行内重复的 (from,to) 去重。
- 不含 `\x1e` 的行为 spacer 行；含则其后按 `\x1f` 切 7 个字段：
  hash / parents(空格分隔) / authorName / authorEmail / authorTimestamp(%at) / %d / %s；
  字段数 < 7 或时间戳非法 → 跳过该行。
- 颜色：跨行维护 `Map<int,int>`，lane 首次触达（节点/直通/曲线端点）时分配递增 palette 序号。

- [ ] **Step 1: 写失败测试**

```dart
// client/test/services/git/parser/git_graph_parser_test.dart
import 'package:flutter_test/flutter_test.dart';
import 'package:teampilot/models/git_graph.dart';
import 'package:teampilot/services/git/parser/git_graph_parser.dart';

const String sep = '\x1e'; // record 分隔
const String f = '\x1f'; // 字段分隔

String row(String graph, List<String> fields) => '$graph$sep${fields.join(f)}';

void main() {
  group('GitGraphParser.parse', () {
    test('linear history: straight edges, node lanes, parents', () {
      final rows = GitGraphParser.parse([
        row('* ', ['c2', 'c1', 'A', 'a@x', '1000', '', 'second']),
        row('* ', ['c1', '', 'A', 'a@x', '900', '', 'first']),
      ].join('\n'));
      expect(rows, hasLength(2));
      final first = rows[0] as GitCommitRow;
      expect(first.node.lane, 0);
      expect(first.parents, ['c1']);
      expect(first.subject, 'second');
      expect(first.edges.single.isStraight, isTrue);
      expect(rows[1], isA<GitCommitRow>());
    });

    test('fork with backslash spacer carries curve 0->1; decorations on root',
        () {
      final rows = GitGraphParser.parse(
        [
          row('* ', ['m', 'a b', 'M', 'm@x', '1000', '', 'merge']),
          '|\\',
          row('| * ', ['b', 'r', 'B', 'b@x', '990', '', 'branch']),
          row('* | ', ['a', 'r', 'A', 'a@x', '995', '', 'side']),
          '|/',
          row('* ', ['r', '', 'R', 'r@x', '900', '(HEAD -> main)', 'root']),
        ].join('\n'),
        remotePrefixes: {'origin/'},
      );
      expect(rows.whereType<GitGraphSpacerRow>(), hasLength(2));
      final fork = rows[1];
      expect(fork.edges.any((e) => e.fromLane == 0 && e.toLane == 1), isTrue);

      final branch = rows[2] as GitCommitRow;
      expect(branch.node.lane, 1);
      final side = rows[3] as GitCommitRow;
      expect(side.node.lane, 0);

      final root = rows[5] as GitCommitRow;
      expect(root.refs.single.kind, GitRefDecorationKind.head);
      expect(root.refs.single.name, 'main');
    });

    test('merge-in |/ curves from lane1 down to lane0', () {
      final rows = GitGraphParser.parse('|/');
      expect(rows.single, isA<GitGraphSpacerRow>());
      expect(
        rows.single.edges.any((e) => e.fromLane == 1 && e.toLane == 0),
        isTrue,
      );
    });

    test('timestamp becomes UTC DateTime; malformed rows skipped', () {
      final rows = GitGraphParser.parse([
        row('* ', ['h1', '', 'N', 'n@x', '1700000000', '', 's']),
        '*$sep broken',
      ].join('\n'));
      expect(rows, hasLength(1));
      expect(
        (rows.single as GitCommitRow).authorDate,
        DateTime.fromMillisecondsSinceEpoch(1700000000 * 1000, isUtc: true),
      );
    });

    test('lane colors assigned in first-touch order across rows', () {
      final rows = GitGraphParser.parse(
        [
          row('* ', ['m', 'a b', 'M', 'm@x', '10', '', 'm']),
          '|\\',
          row('| * ', ['b', 'r', 'B', 'b@x', '9', '', 'b']),
          row('* | ', ['a', 'r', 'A', 'a@x', '8', '', 'a']),
          row('* ', ['r', '', 'R', 'r@x', '7', '', 'r']),
        ].join('\n'),
      );
      final merge = rows[0] as GitCommitRow;
      expect(merge.node.colorIndex, 0);
      final curve =
          rows[1].edges.firstWhere((e) => !e.isStraight);
      expect(curve.colorIndex, 1);
    });

    test('octopus row parses multiple outgoing curves without crash', () {
      final rows = GitGraphParser.parse(
        [
          row('*-.   ', ['m', 'a b c', 'M', 'm@x', '10', '', 'octopus']),
          '|\\ \\',
          row('| | * ', ['c', 'r', 'C', 'c@x', '9', '', 'c']),
        ].join('\n'),
      );
      final curves = rows
          .expand((r) => r.edges)
          .where((e) => !e.isStraight)
          .toList();
      expect(curves.map((e) => (e.fromLane, e.toLane)),
          containsAll([(0, 1), (2, 3)]));
    });
  });
}
```

注意：测试中含反斜杠的图行一律用普通字符串双反斜杠写法（`|\\`、`|\\ \\`）；Dart raw string 结尾紧跟引号的反斜杠不合法，不要用。

- [ ] **Step 2: 跑测试确认失败**

Run: `cd client && flutter test test/services/git/parser/git_graph_parser_test.dart`
Expected: FAIL（找不到 GitGraphParser）

- [ ] **Step 3: 最小实现**

创建 `client/lib/services/git/parser/git_graph_parser.dart`：

```dart
import 'dart:convert';

import '../../../models/git_graph.dart';
import 'git_decoration_parser.dart';

/// `git log --graph --pretty=format:%x1e%H%x1f%P%x1f%an%x1f%ae%x1f%at%x1f%d%x1f%s`
/// 输出的逐行解析器。
///
/// git 的 graph 列每 lane 占 2 个字符：`|`/`*` 在偶数位（lane = index ~/ 2），
/// `\`/`/` 曲线在奇数位。拓扑正确性由 git 保证，这里只做"字符 → 几何"映射。
class GitGraphParser {
  static const String recordSep = '\x1e';
  static const String fieldSep = '\x1f';

  static List<GitGraphRow> parse(
    String stdout, {
    Set<String> remotePrefixes = const {'origin/'},
  }) {
    final state = _LayoutState();
    final rows = <GitGraphRow>[];
    for (final line in const LineSplitter().convert(stdout)) {
      final sepIndex = line.indexOf(recordSep);
      final graphPart =
          line.substring(0, sepIndex < 0 ? line.length : sepIndex);
      final edges = _parseGeometry(graphPart, state);
      if (sepIndex < 0) {
        if (graphPart.trim().isEmpty) continue;
        rows.add(GitGraphSpacerRow(edges: edges));
        continue;
      }
      final fields = line.substring(sepIndex + recordSep.length).split(fieldSep);
      if (fields.length < 7) continue;
      final ts = int.tryParse(fields[4]);
      if (ts == null) continue;
      rows.add(GitCommitRow(
        edges: edges,
        node: GitGraphNode(state.nodeLane ?? 0, state.nodeColor),
        hash: fields[0],
        parents: fields[1].trim().isEmpty
            ? const []
            : fields[1].trim().split(' '),
        authorName: fields[2],
        authorEmail: fields[3],
        authorDate:
            DateTime.fromMillisecondsSinceEpoch(ts * 1000, isUtc: true),
        subject: fields[6],
        refs: parseGitDecorations(fields[5], remotePrefixes: remotePrefixes),
      ));
    }
    return rows;
  }

  /// 扫描一行的 ASCII 前缀，产出边列表；节点 lane 记录在 [state]。
  static List<GitGraphEdge> _parseGeometry(String g, _LayoutState state) {
    state.nodeLane = null;
    state.nodeColor = 0;
    final out = <GitGraphEdge>[];
    void addEdge(int from, int to, int color) {
      if (out.any((e) => e.fromLane == from && e.toLane == to)) return;
      out.add(GitGraphEdge(from, to, color));
    }

    for (var i = 0; i < g.length; i++) {
      switch (g[i]) {
        case '*':
          final lane = i ~/ 2;
          state.nodeLane = lane;
          state.nodeColor = state.colorFor(lane);
          addEdge(lane, lane, state.nodeColor);
          break;
        case '|':
          final lane = i ~/ 2;
          addEdge(lane, lane, state.colorFor(lane));
          break;
        case '\\':
          final lane = i ~/ 2;
          // 分叉曲线取新分支（下方新出现的 lane）的颜色。
          addEdge(lane, lane + 1, state.colorFor(lane + 1));
          break;
        case '/':
          final lane = i ~/ 2;
          // 合流曲线保留侧支 lane 的颜色直到汇入。
          addEdge(lane + 1, lane, state.colorFor(lane + 1));
          break;
        default:
          break; // 空格、'-'、'_' 等填充符
      }
    }
    return out;
  }
}

/// 跨行的调色板分配状态：lane 首次触达时取下一个颜色序号。
class _LayoutState {
  final Map<int, int> _colors = {};
  int _nextColor = 0;
  int? nodeLane;
  int nodeColor = 0;

  int colorFor(int lane) => _colors.putIfAbsent(lane, () => _nextColor++);
}
```

注意实现里 `case '\\'` 是普通字符串转义写法。

- [ ] **Step 4: 跑测试确认通过**

Run: `cd client && flutter test test/services/git/parser/git_graph_parser_test.dart`
Expected: PASS（6 tests）

- [ ] **Step 5: Commit**

```bash
git add client/lib/services/git/parser/git_graph_parser.dart \
  client/test/services/git/parser/git_graph_parser_test.dart
git commit -m "feat(git-graph): parse git log --graph ascii into row model"
```

---

### Task 3: GitHistoryService（只读历史查询）

**Files:**
- Create: `client/lib/services/git/git_history_service.dart`
- Modify: `client/lib/models/git_graph.dart`（追加 detail/branch/tag/stash 模型）
- Test: `client/test/services/git/git_history_service_test.dart`

**Interfaces:**
- Consumes: `GitCommandRunner`（`runInDirectory(dir,args)` → `GitCommandResult{exitCode,stdout,stderr}`）、`GitException`（git_service.dart 导出）、Task 1/2 模型与解析器。
- Produces:

```dart
class GitHistoryService {
  GitHistoryService({GitCommandRunner? runner});
  factory GitHistoryService.forContext(RuntimeContext ctx);
  static GitHistoryService Function()? debugOverrideFactory; // 测试缝，模式同 GitService

  static const int initialLoadCommits = 300;
  static const int loadMoreCommits = 100;

  Future<List<String>> remotes(String dir);
  Future<List<GitGraphRow>> graphRows(String dir,
      {int limit = initialLoadCommits, int skip = 0,
       String query = '', GitSearchMode mode = GitSearchMode.message});
  Future<GitCommitDetail> commitDetail(String dir, String hash);
  Future<String> commitFileDiff(String dir,
      {required String hash, String? parent, required String path});
  Future<List<GitBranchInfo>> branches(String dir);   // 本地+远程，current 标记
  Future<List<GitTagInfo>> tags(String dir);
  Future<List<GitStashEntry>> stashList(String dir);
}

class GitCommitDetail extends Equatable {
  const GitCommitDetail({required this.hash, required this.parents,
    required this.authorName, required this.authorEmail,
    required this.authorDate, required this.subject, required this.body,
    required this.files});
  final String hash; final List<String> parents;
  final String authorName; final String authorEmail; final DateTime authorDate;
  final String subject; final String body;
  final List<GitCommitFileChange> files;
}

enum GitCommitFileStatus { added, modified, deleted, renamed, typeChanged }
class GitCommitFileChange extends Equatable {
  const GitCommitFileChange(this.path, this.status, {this.previousPath});
  final String path; final GitCommitFileStatus status; final String? previousPath;
}
class GitBranchInfo extends Equatable {
  const GitBranchInfo(this.name, this.hash, {required this.isRemote, required this.isCurrent});
}
class GitTagInfo extends Equatable { const GitTagInfo(this.name, this.hash); }
class GitStashEntry extends Equatable {
  const GitStashEntry(this.selector, this.hash, this.subject); // selector 如 stash@{0}
}
```

- [ ] **Step 1: 在 models/git_graph.dart 追加上述模型**（含 Equatable props；`GitCommitFileChange` 的 status 由 diff-tree 字母映射 A→added/M→modified/D→deleted/R→renamed/T→typeChanged）。

先写模型再写测试——模型是纯数据无行为，测试价值在服务层。

- [ ] **Step 2: 写失败测试**

```dart
// client/test/services/git/git_history_service_test.dart
import 'dart:convert';
import 'dart:io';

import 'package:flutter_test/flutter_test.dart';
import 'package:teampilot/models/git_graph.dart';
import 'package:teampilot/services/git/git_command_runner.dart';
import 'package:teampilot/services/git/git_history_service.dart';

/// 与 test/services/git/git_service_test.dart 的 _FakeRunner 同款：
/// 记录 `-C <dir>` 之后的子命令，按前缀匹配返回 ProcessResult。
class _FakeRunner {
  _FakeRunner(this.responses);
  final Map<String, ProcessResult> responses;
  final List<List<String>> calls = [];

  Future<ProcessResult> call(
    String executable,
    List<String> arguments, {
    Encoding? stdoutEncoding,
    Encoding? stderrEncoding,
  }) async {
    final cIdx = arguments.indexOf('-C');
    if (cIdx < 0) return ProcessResult(0, 0, '/usr/bin/git\n', '');
    final cmd = arguments.sublist(cIdx + 2);
    calls.add(cmd);
    final key = cmd.join(' ');
    for (final e in responses.entries) {
      if (key.startsWith(e.key)) return e.value;
    }
    return ProcessResult(0, 0, '', '');
  }
}

ProcessResult ok([String stdout = '']) => ProcessResult(0, 0, stdout, '');

void main() {
  setUp(GitService.debugResetExecutableCache);

  test('graphRows builds log args with paging and default --all', () async {
    final fake = _FakeRunner({'log': ok('')});
    final svc = GitHistoryService(runner: LocalGitCommandRunner(runner: fake.call));
    await svc.graphRows('/repo', limit: 100, skip: 300);
    final cmd = fake.calls.single;
    expect(cmd.first, 'log');
    expect(cmd, containsAll(['--all', '--date-order', '--graph']));
    expect(cmd[cmd.indexOf('--max-count') + 1], '100');
    expect(cmd[cmd.indexOf('--skip') + 1], '300');
    expect(cmd.last.startsWith('--pretty=format:\x1e%H\x1f%P'), isTrue);
  });

  test('graphRows message search appends grep flags', () async {
    final fake = _FakeRunner({'log': ok('')});
    final svc = GitHistoryService(runner: LocalGitCommandRunner(runner: fake.call));
    await svc.graphRows('/repo', query: 'fix bug', mode: GitSearchMode.message);
    final cmd = fake.calls.single;
    expect(cmd.contains('--grep=fix bug'), isTrue);
    expect(cmd, contains('-i'));
  });

  test('graphRows author search appends author flag', () async {
    final fake = _FakeRunner({'log': ok('')});
    final svc = GitHistoryService(runner: LocalGitCommandRunner(runner: fake.call));
    await svc.graphRows('/repo', query: 'Alice', mode: GitSearchMode.author);
    expect(fake.calls.single.contains('--author=Alice'), isTrue);
  });

  test('graphRows hash mode adds no server-side filter (client filters)', () async {
    final fake = _FakeRunner({'log': ok('')});
    final svc = GitHistoryService(runner: LocalGitCommandRunner(runner: fake.call));
    await svc.graphRows('/repo', query: 'abc12', mode: GitSearchMode.hash);
    expect(fake.calls.single.any((a) => a.startsWith('--grep=')), isFalse);
  });

  test('commitDetail parses show + diff-tree name-status', () async {
    const sep = '\x1e';
    const f = '\x1f';
    final fake = _FakeRunner({
      'show': ok('$sep h1${f}h0${f}Ann${f}ann@x${f}1700000000${f}subj'
          '${f}full body\n'),
      'diff-tree': ok('M\tsrc/a.dart\nR100\tsrc/b.dart\tsrc/old.dart\n'),
    });
    final svc = GitHistoryService(runner: LocalGitCommandRunner(runner: fake.call));
    final d = await svc.commitDetail('/repo', 'h1');
    expect(d.parents, ['h0']);
    expect(d.subject, 'subj');
    expect(d.body.trim(), 'full body');
    expect(d.files.map((e) => e.path),
        ['src/a.dart', 'src/b.dart']);
    expect(d.files[1].status, GitCommitFileStatus.renamed);
    expect(d.files[1].previousPath, 'src/old.dart');
  });

  test('commitFileDiff uses diff-tree --root for root commit', () async {
    final fake = _FakeRunner({});
    final svc = GitHistoryService(runner: LocalGitCommandRunner(runner: fake.call));
    await svc.commitFileDiff('/repo', hash: 'r1', path: 'a.dart');
    expect(fake.calls.single,
        containsAll(['diff-tree', '-p', '--root', '--no-commit-id']));
  });

  test('branches merges local+remote with current flag', () async {
    final f = '\x1f';
    final fake = _FakeRunner({
      'for-each-ref refs/heads': ok('main${f}h1${f}*\n'
          'dev${f}h2${f} \n'),
      'for-each-ref refs/remotes': ok('origin/main${f}h1${f} \n'),
    });
    final svc = GitHistoryService(runner: LocalGitCommandRunner(runner: fake.call));
    final list = await svc.branches('/repo');
    expect(list.length, 3);
    expect(list.firstWhere((b) => b.isCurrent).name, 'main');
    expect(
        list.firstWhere((b) => b.isRemote).name, 'origin/main');
  });

  test('stashList parses selectors; empty output yields empty list', () async {
    final f = '\x1f';
    final fake = _FakeRunner({
      'stash list': ok('stash@{0}${f}abc${f}WIP\nstash@{1}${f}def${f}tmp\n'),
    });
    final svc = GitHistoryService(runner: LocalGitCommandRunner(runner: fake.call));
    final list = await svc.stashList('/repo');
    expect(list.first.selector, 'stash@{0}');
    expect(list, hasLength(2));

    final emptyFake = _FakeRunner({'stash': ok('\n')});
    final svc2 =
        GitHistoryService(runner: LocalGitCommandRunner(runner: emptyFake.call));
    expect(await svc2.stashList('/repo'), isEmpty);
  });
}
```

注意：`_FakeRunner` 前缀匹配按 `cmd.join(' ')`，因此响应 key 必须写成 `'stash list'`、`'for-each-ref refs/heads'` 这类带空格的完整前缀。

- [ ] **Step 3: 跑测试确认失败**

Run: `cd client && flutter test test/services/git/git_history_service_test.dart`
Expected: FAIL（GitHistoryService 未定义）

- [ ] **Step 4: 实现 GitHistoryService**

创建 `client/lib/services/git/git_history_service.dart`：

```dart
import '../../models/git_graph.dart';
import '../../utils/logging/logger.dart';
import '../storage/runtime_context.dart';
import 'git_command_runner.dart';
import 'git_service.dart' show GitException;
import 'parser/git_graph_parser.dart';

class GitHistoryService {
  GitHistoryService({GitCommandRunner? runner})
    : _runner = runner ?? LocalGitCommandRunner();

  factory GitHistoryService.forContext(RuntimeContext ctx) =>
      GitHistoryService(runner: gitCommandRunnerForContext(ctx));

  /// 测试缝（同 [GitService.debugOverrideFactory] 模式）。
  static GitHistoryService Function()? debugOverrideFactory;

  static const int initialLoadCommits = 300;
  static const int loadMoreCommits = 100;

  static const String _recordSep = '\x1e';
  static const String _fieldSep = '\x1f';
  static final String _commitFormat =
      '%x1e${['%H', '%P', '%an', '%ae', '%at', '%d', '%s'].join(_fieldSep)}';
  static final String _showFormat =
      ['%H', '%P', '%an', '%ae', '%at', '%s', '%B'].join(_fieldSep);

  final GitCommandRunner _runner;

  Future<String> _run(String dir, List<String> args) async {
    final result = await _runner.runInDirectory(dir, args);
    if (result.exitCode != 0) {
      final detail = result.stderr.trim().isEmpty
          ? result.stdout.trim()
          : result.stderr.trim();
      appLogger.d('[GitHistory] ${args.join(' ')} exit ${result.exitCode}: $detail');
      throw GitException(detail.isEmpty ? 'git ${args.first} failed' : detail);
    }
    return result.stdout;
  }

  Future<List<String>> remotes(String dir) async => (await _run(dir, [
        'remote',
      ]))
          .split('\n')
          .map((e) => e.trim())
          .where((e) => e.isNotEmpty)
          .toList(growable: false);

  Set<String> remotePrefixes(List<String> remotes) =>
      {for (final r in remotes) '$r/'};

  /// 提交拓扑行。query/mode 组成搜索过滤：
  /// message → `--grep=<q> -i`；author → `--author=<q>`；hash → 客户端过滤。
  Future<List<GitGraphRow>> graphRows(
    String dir, {
    int limit = initialLoadCommits,
    int skip = 0,
    String query = '',
    GitSearchMode mode = GitSearchMode.message,
  }) async {
    final args = <String>[
      'log',
      '--all',
      '--date-order',
      if (skip > 0) ...['--skip', '$skip'],
      '--max-count',
      '$limit',
      '--pretty=format:$_commitFormat',
      '--graph',
      if (query.isNotEmpty && mode == GitSearchMode.message) ...[
        '--grep=$query',
        '-i',
      ],
      if (query.isNotEmpty && mode == GitSearchMode.author)
        '--author=$query',
    ];
    return GitGraphParser.parse(await _run(dir, args),
        remotePrefixes: remotePrefixes(await remotes(dir)));
  }

  Future<GitCommitDetail> commitDetail(String dir, String hash) async {
    final meta =
        (await _run(dir, ['show', '-s', "--pretty=format:$_showFormat", hash]))
            .trim();
    final fields = meta.startsWith(_recordSep)
        ? meta.substring(1).split(_fieldSep)
        : meta.split(_fieldSep);
    if (fields.length < 7) {
      throw GitException('unexpected git show output for $hash');
    }
    final ts = int.tryParse(fields[4]) ?? 0;
    final files = await commitFiles(dir, hash);
    return GitCommitDetail(
      hash: fields[0],
      parents: fields[1].trim().isEmpty ? const [] : fields[1].trim().split(' '),
      authorName: fields[2],
      authorEmail: fields[3],
      authorDate: DateTime.fromMillisecondsSinceEpoch(ts * 1000, isUtc: true),
      subject: fields[5],
      body: fields.sublist(6).join(_fieldSep),
      files: files,
    );
  }

  Future<List<GitCommitFileChange>> commitFiles(String dir, String hash) async {
    final out = await _run(dir, [
      'diff-tree',
      '--no-commit-id',
      '--name-status',
      '-r',
      '--root',
      hash,
    ]);
    final files = <GitCommitFileChange>[];
    for (final line in out.split('\n')) {
      final trimmed = line.trim();
      if (trimmed.isEmpty) continue;
      final parts = trimmed.split('\t');
      if (parts.isEmpty) continue;
      final statusLetter = parts.first.trim();
      final status = switch (statusLetter.isEmpty
          ? ''
          : statusLetter[0]) {
        'A' => GitCommitFileStatus.added,
        'M' => GitCommitFileStatus.modified,
        'D' => GitCommitFileStatus.deleted,
        'R' => GitCommitFileStatus.renamed,
        'T' => GitCommitFileStatus.typeChanged,
        _ => GitCommitFileStatus.modified,
      };
      // R100\t<old>\t<new> / C100\t<old>\t<new>
      if ((status == GitCommitFileStatus.renamed) && parts.length >= 3) {
        files.add(GitCommitFileChange(parts[2], status,
            previousPath: parts[1]));
      } else if (parts.length >= 2) {
        files.add(GitCommitFileChange(parts[1], status));
      }
    }
    return files;
  }

  Future<String> commitFileDiff(
    String dir, {
    required String hash,
    String? parent,
    required String path,
  }) =>
      parent == null
          ? _run(dir, [
              'diff-tree',
              '-p',
              '--root',
              '--no-commit-id',
              hash,
              '--',
              path,
            ])
          : _run(dir, ['diff', parent, hash, '--', path]);

  Future<List<GitBranchInfo>> branches(String dir) async {
    final local = await _run(dir, [
      'for-each-ref',
      'refs/heads',
      '--format=%(refname:short)\x1f%(objectname)\x1f%(HEAD)',
    ]);
    final remote = await _run(dir, [
      'for-each-ref',
      'refs/remotes',
      '--format=%(refname:short)\x1f%(objectname)',
    ]);
    GitBranchInfo parse(String line, {required bool isRemote}) {
      final p = line.split(_fieldSep);
      return GitBranchInfo(p[0].trim(), p.length > 1 ? p[1].trim() : '',
          isRemote: isRemote,
          isCurrent: !isRemote && (p.length > 2 ? p[2].trim() == '*' : false));
    }

    return [
      for (final l in local.split('\n'))
        if (l.trim().isNotEmpty) parse(l, isRemote: false),
      for (final l in remote.split('\n'))
        if (l.trim().isNotEmpty) parse(l, isRemote: true),
    ];
  }

  Future<List<GitTagInfo>> tags(String dir) async {
    final out = await _run(dir, [
      'for-each-ref',
      'refs/tags',
      '--format=%(refname:short)\x1f%(objectname)',
    ]);
    return [
      for (final l in out.split('\n'))
        if (l.trim().isNotEmpty)
          () {
            final p = l.split(_fieldSep);
            return GitTagInfo(p[0].trim(),
                p.length > 1 ? p[1].trim() : '');
          }(),
    ];
  }

  Future<List<GitStashEntry>> stashList(String dir) async {
    final out = await _run(dir, [
      'stash',
      'list',
      '--pretty=format:%gd\x1f%H\x1f%s',
    ]);
    return [
      for (final l in out.split('\n'))
        if (l.trim().isNotEmpty)
          () {
            final p = l.split(_fieldSep);
            return GitStashEntry(p[0].trim(),
                p.length > 1 ? p[1].trim() : '',
                p.length > 2 ? p[2].trim() : '');
          }(),
    ];
  }
}
```

注意：测试的 `_FakeRunner` 需要导入 `package:teampilot/services/git/git_service.dart`（取 `GitService.debugResetExecutableCache`）。

- [ ] **Step 5: 跑测试确认通过**

Run: `cd client && flutter test test/services/git/git_history_service_test.dart`
Expected: PASS（8 tests）

- [ ] **Step 6: Commit**

```bash
git add client/lib/models/git_graph.dart \
  client/lib/services/git/git_history_service.dart \
  client/test/services/git/git_history_service_test.dart
git commit -m "feat(git-graph): add read-only git history service"
```

---

### Task 4: GitHistoryActions（历史写操作）

**Files:**
- Create: `client/lib/services/git/git_history_actions.dart`
- Test: `client/test/services/git/git_history_actions_test.dart`

**Interfaces:**
- Consumes: `GitCommandRunner`、`GitException`、`GitResetMode`。
- Produces:

```dart
class GitHistoryActions {
  GitHistoryActions({GitCommandRunner? runner});
  factory GitHistoryActions.forContext(RuntimeContext ctx);

  Future<void> createBranchAt(String dir, String name, {required String startPoint});
  Future<void> renameBranch(String dir, String oldName, String newName);
  Future<void> deleteBranch(String dir, String name, {bool force = false});
  Future<void> checkoutBranch(String dir, String name);
  Future<void> checkoutCommit(String dir, String hash);      // detach
  Future<void> mergeIntoCurrent(String dir, String refName); // --no-edit
  Future<void> createTag(String dir, String name, {String? at, String? message}); // message!=null → 附注
  Future<void> deleteTag(String dir, String name);
  Future<void> pushTag(String dir, String name, {String remote = 'origin'});
  Future<void> cherryPick(String dir, String hash);
  Future<void> revert(String dir, String hash);              // --no-edit
  Future<void> resetTo(String dir, String ref, {required GitResetMode mode});
  Future<void> stashPop(String dir, {String? ref});
  Future<void> stashApply(String dir, {String? ref});
  Future<void> stashDrop(String dir, {String? ref});
  Future<void> fetchAll(String dir);                          // fetch --all --prune
}
```

- [ ] **Step 1: 写失败测试**

```dart
// client/test/services/git/git_history_actions_test.dart
import 'dart:convert';
import 'dart:io';

import 'package:flutter_test/flutter_test.dart';
import 'package:teampilot/models/git_graph.dart';
import 'package:teampilot/services/git/git_command_runner.dart';
import 'package:teampilot/services/git/git_history_actions.dart';
import 'package:teampilot/services/git/git_service.dart';

class _FakeRunner {
  _FakeRunner(this.responses);
  final Map<String, ProcessResult> responses;
  final List<List<String>> calls = [];

  Future<ProcessResult> call(
    String executable,
    List<String> arguments, {
    Encoding? stdoutEncoding,
    Encoding? stderrEncoding,
  }) async {
    final cIdx = arguments.indexOf('-C');
    if (cIdx < 0) return ProcessResult(0, 0, '/usr/bin/git\n', '');
    final cmd = arguments.sublist(cIdx + 2);
    calls.add(cmd);
    for (final e in responses.entries) {
      if (cmd.join(' ').startsWith(e.key)) return e.value;
    }
    return ProcessResult(0, 0, '', '');
  }
}

void main() {
  setUp(GitService.debugResetExecutableCache);
  late _FakeRunner fake;
  late GitHistoryActions actions;

  setUp(() {
    fake = _FakeRunner({});
    actions = GitHistoryActions(runner: LocalGitCommandRunner(runner: fake.call));
  });

  test('resetTo maps mode to flag', () async {
    await actions.resetTo('/r', 'main', mode: GitResetMode.hard);
    expect(fake.calls.single.sublist(0, 3), ['reset', '--hard', 'main']);
    await actions.resetTo('/r', 'h1', mode: GitResetMode.soft);
    expect(fake.calls.last[1], '--soft');
  });

  test('createTag annotated includes -a -m and optional start point',
      () async {
    await actions.createTag('/r', 'v2', at: 'h1', message: 'release');
    expect(fake.calls.single,
        ['tag', '-a', 'v2', '-m', 'release', 'h1']);
  });

  test('deleteBranch uses -d unless force', () async {
    await actions.deleteBranch('/r', 'dev');
    expect(fake.calls.single, ['branch', '-d', 'dev']);
    await actions.deleteBranch('/r', 'dev', force: true);
    expect(fake.calls.last, ['branch', '-D', 'dev']);
  });

  test('revert/cherry-pick/checkout-commit arg shapes', () async {
    await actions.revert('/r', 'h1');
    expect(fake.calls.single, ['revert', '--no-edit', 'h1']);
    await actions.cherryPick('/r', 'h2');
    expect(fake.calls[1], ['cherry-pick', 'h2']);
    await actions.checkoutCommit('/r', 'h3');
    expect(fake.calls.last, ['checkout', 'h3']);
  });

  test('stash pop/apply/drop with optional ref', () async {
    await actions.stashPop('/r');
    expect(fake.calls.single, ['stash', 'pop']);
    await actions.stashApply('/r', ref: 'stash@{1}');
    expect(fake.calls.last, ['stash', 'apply', 'stash@{1}']);
    await actions.stashDrop('/r', ref: 'stash@{0}');
    expect(fake.calls.last, ['stash', 'drop', 'stash@{0}']);
  });
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd client && flutter test test/services/git/git_history_actions_test.dart`
Expected: FAIL（GitHistoryActions 未定义）

- [ ] **Step 3: 实现 GitHistoryActions**

创建 `client/lib/services/git/git_history_actions.dart`：

```dart
import '../../models/git_graph.dart';
import '../../utils/logging/logger.dart';
import '../storage/runtime_context.dart';
import 'git_command_runner.dart';
import 'git_service.dart' show GitException;

/// 历史写操作（分支/标签/提交级/stash/fetch）。全部经 [GitCommandRunner] 执行，
/// 非零退出抛 [GitException]；调用方（GitGraphCubit）负责刷新与错误呈现。
class GitHistoryActions {
  GitHistoryActions({GitCommandRunner? runner})
    : _runner = runner ?? LocalGitCommandRunner();

  factory GitHistoryActions.forContext(RuntimeContext ctx) =>
      GitHistoryActions(runner: gitCommandRunnerForContext(ctx));

  final GitCommandRunner _runner;

  Future<void> _run(String dir, List<String> args) async {
    final result = await _runner.runInDirectory(dir, args);
    if (result.exitCode != 0) {
      final detail = result.stderr.trim().isEmpty
          ? result.stdout.trim()
          : result.stderr.trim();
      appLogger.d('[GitActions] ${args.join(' ')} exit ${result.exitCode}: $detail');
      throw GitException(detail.isEmpty ? 'git ${args.first} failed' : detail);
    }
  }

  Future<void> createBranchAt(
    String dir,
    String name, {
    required String startPoint,
  }) =>
      _run(dir, ['branch', name, startPoint]);

  Future<void> renameBranch(String dir, String oldName, String newName) =>
      _run(dir, ['branch', '-m', oldName, newName]);

  Future<void> deleteBranch(String dir, String name, {bool force = false}) =>
      _run(dir, ['branch', force ? '-D' : '-d', name]);

  Future<void> checkoutBranch(String dir, String name) =>
      _run(dir, ['checkout', name]);

  Future<void> checkoutCommit(String dir, String hash) =>
      _run(dir, ['checkout', hash]);

  Future<void> mergeIntoCurrent(String dir, String refName) =>
      _run(dir, ['merge', '--no-edit', refName]);

  /// [message] 非空创建附注标签，否则轻量标签；[at] 为空指向 HEAD。
  Future<void> createTag(
    String dir,
    String name, {
    String? at,
    String? message,
  }) =>
      message != null && message.isNotEmpty
          ? _run(dir, [
              'tag',
              '-a',
              name,
              '-m',
              message,
              if (at != null && at.isNotEmpty) at,
            ])
          : _run(dir, [
              'tag',
              name,
              if (at != null && at.isNotEmpty) at,
            ]);

  Future<void> deleteTag(String dir, String name) =>
      _run(dir, ['tag', '-d', name]);

  Future<void> pushTag(
    String dir,
    String name, {
    String remote = 'origin',
  }) =>
      _run(dir, ['push', remote, name]);

  Future<void> cherryPick(String dir, String hash) =>
      _run(dir, ['cherry-pick', hash]);

  Future<void> revert(String dir, String hash) =>
      _run(dir, ['revert', '--no-edit', hash]);

  Future<void> resetTo(
    String dir,
    String ref, {
    required GitResetMode mode,
  }) =>
      _run(dir, [
        'reset',
        switch (mode) {
          GitResetMode.soft => '--soft',
          GitResetMode.mixed => '--mixed',
          GitResetMode.hard => '--hard',
        },
        ref,
      ]);

  Future<void> stashPop(String dir, {String? ref}) => _run(dir, [
        'stash',
        'pop',
        if (ref != null && ref.isNotEmpty) ref,
      ]);

  Future<void> stashApply(String dir, {String? ref}) => _run(dir, [
        'stash',
        'apply',
        if (ref != null && ref.isNotEmpty) ref,
      ]);

  Future<void> stashDrop(String dir, {String? ref}) => _run(dir, [
        'stash',
        'drop',
        if (ref != null && ref.isNotEmpty) ref,
      ]);

  Future<void> fetchAll(String dir) =>
      _run(dir, ['fetch', '--all', '--prune']);
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd client && flutter test test/services/git/git_history_actions_test.dart`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add client/lib/services/git/git_history_actions.dart \
  client/test/services/git/git_history_actions_test.dart
git commit -m "feat(git-graph): add history mutation service"
```

---

### Task 5: GitGraphCubit 核心（刷新/分页/选中/搜索）

**Files:**
- Create: `client/lib/cubits/git_graph_cubit.dart`
- Test: `client/test/cubits/git_graph_cubit_test.dart`

**Interfaces:**
- Consumes: `GitHistoryService`、`GitService`（status）、Task 3 模型。
- Produces:

```dart
class GitGraphState extends Equatable {
  const GitGraphState({
    this.repoRoot = '', this.rows = const [], this.hasMore = false,
    this.isLoadingMore = false, this.isRefreshing = false,
    this.selectedHash, this.commitDetail, this.detailLoading = false,
    this.openFilePath, this.fileDiffText, this.fileDiffLoading = false,
    this.branches = const [], this.tags = const [], this.stashList = const [],
    this.currentBranch = '', this.ahead = 0, this.behind = 0,
    this.dirtyCount = 0, this.searchQuery = '',
    this.searchMode = GitSearchMode.message,
    this.errorMessage, this.gitAvailable = true,
  });
  // 全部 final 字段；copyWith 支持 clearError / clearSelection /
  // clearDetail / clearFileDiff 命名哨兵参数。
  /// hash 搜索模式对已加载行做前缀过滤；其余模式原样返回 rows。
  List<GitGraphRow> get visibleRows;
}

class GitGraphCubit extends Cubit<GitGraphState> {
  GitGraphCubit({required GitHistoryService history, required GitService git});
  Future<void> setRepoRoot(String root); // 重置状态并 refresh
  Future<void> refresh();                // coalesced：首页图+refs/tags/stash/status
  Future<void> loadMore();               // skip=rows.length 追加下一页
  void selectCommit(String? hash);       // 触发详情懒加载，null 取消选中
  Future<void> openCommitFile(GitCommitFileChange file);
  void closeFileDiff();
  Future<void> search(String query, GitSearchMode mode);
  Future<void> clearSearch();
}
```

**关键行为约定：**
- `refresh()` 复制 GitCubit 的 coalescing 模式（参照 `git_cubit.dart:228`：`_refreshInFlight`/`_refreshQueued`）。
- `hasMore = 本次 graphRows 返回行数 == limit`。
- `selectCommit` 异步加载 detail；emit 前检查 `state.selectedHash == hash && !isClosed`，否则丢弃（stale guard）。
- status 来自 `GitService.status(root)`：提供 currentBranch/ahead/behind/dirtyCount（staged+unstaged 数）与 notARepository 判定。
- 所有错误进 `errorMessage` 并经 `AppLogger` 记录，不抛出到 UI。

- [ ] **Step 1: 写失败测试**

测试文件 `client/test/cubits/git_graph_cubit_test.dart`。fake 服务用 `implements + noSuchMethod` 只覆盖所需成员：

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:teampilot/cubits/git_graph_cubit.dart';
import 'package:teampilot/models/git_graph.dart';
import 'package:teampilot/models/git_status.dart';
import 'package:teampilot/services/git/git_history_service.dart';
import 'package:teampilot/services/git/git_service.dart';

class _FakeHistory implements GitHistoryService {
  _FakeHistory({required this.rows, this.detail});
  final List<GitGraphRow> rows;
  final GitCommitDetail? detail;
  int graphCalls = 0;
  Map<String, Object?> lastArgs = {};

  @override
  dynamic noSuchMethod(Invocation invocation) => super.noSuchMethod(invocation);

  @override
  Future<List<GitGraphRow>> graphRows(String dir,
      {int limit = GitHistoryService.initialLoadCommits,
      int skip = 0,
      String query = '',
      GitSearchMode mode = GitSearchMode.message}) async {
    graphCalls++;
    lastArgs = {'limit': limit, 'skip': skip, 'query': query, 'mode': mode};
    return rows;
  }

  @override
  Future<GitCommitDetail> commitDetail(String dir, String hash) async =>
      detail!;
}

class _FakeGit implements GitService {
  _FakeGit(this.statusResult);
  final GitRepoStatus statusResult;

  @override
  dynamic noSuchMethod(Invocation invocation) => super.noSuchMethod(invocation);

  @override
  Future<GitRepoStatus> status(String dir) async => statusResult;
}
```

公共构造器与用例：

```dart
GitCommitRow commitRow(String hash) => GitCommitRow(
      edges: const [GitGraphEdge(0, 0, 0)],
      node: const GitGraphNode(0, 0),
      hash: hash,
      parents: const ['p0'],
      authorName: 'A',
      authorEmail: 'a@x',
      authorDate: DateTime.fromMillisecondsSinceEpoch(0, isUtc: true),
      subject: 's-$hash',
      refs: const [],
    );

// GitRepoStatus/GitFileChange 构造签名以 client/lib/models/git_status.dart
// 实际定义为准——实现者先读该文件再写此 helper。
GitRepoStatus repoStatus({bool dirty = false}) => GitRepoStatus(/* … */);

void main() {
  test('setRepoRoot loads first page + metadata', () async {
    final history = _FakeHistory(rows: [commitRow('c1')], detail: null);
    final cubit = GitGraphCubit(history: history, git: _FakeGit(repoStatus()));
    await cubit.setRepoRoot('/repo');
    expect(cubit.state.rows.single.hash, 'c1');
    expect(cubit.state.currentBranch, 'main');
    expect(cubit.state.ahead, 1);
    expect(cubit.state.hasMore, isFalse); // 1 行 < limit 300
    await cubit.close();
  });

  test('loadMore requests skip=rows.length and appends', () async {
    final history =
        _FakeHistory(rows: [commitRow('c0'), commitRow('c1')], detail: null);
    final cubit = GitGraphCubit(history: history, git: _FakeGit(repoStatus()));
    await cubit.setRepoRoot('/repo');
    await cubit.loadMore();
    expect(history.lastArgs['skip'], 2);
    expect(cubit.state.rows.length, 4); // fake 每次返回同 2 行
    expect(cubit.state.hasMore, isTrue); // 2 行满页 → 视为还有更多
    await cubit.close();
  });

  test('selectCommit lazily loads detail; null clears selection', () async {
    final history =
        _FakeHistory(rows: [commitRow('c1')], detail: _detailFor('c1'));
    final cubit = GitGraphCubit(history: history, git: _FakeGit(repoStatus()));
    await cubit.setRepoRoot('/repo');
    cubit.selectCommit('c1');
    await cubit.stream.firstWhere((s) => s.commitDetail != null);
    expect(cubit.state.commitDetail!.hash, 'c1');
    cubit.selectCommit(null);
    expect(cubit.state.selectedHash, isNull);
    expect(cubit.state.commitDetail, isNull);
    await cubit.close();
  });

  test('search message mode passes query server-side', () async {
    final history = _FakeHistory(rows: [commitRow('abc12')], detail: null);
    final cubit = GitGraphCubit(history: history, git: _FakeGit(repoStatus()));
    await cubit.setRepoRoot('/repo');
    await cubit.search('fix', GitSearchMode.message);
    expect(history.lastArgs['query'], 'fix');
    expect(history.lastArgs['mode'], GitSearchMode.message);
    expect(cubit.state.visibleRows, hasLength(1));
    await cubit.close();
  });

  test('hash mode filters loaded rows client-side; clearSearch restores',
      () async {
    final history = _FakeHistory(
        rows: [commitRow('abcdef1'), commitRow('1234567')], detail: null);
    final cubit = GitGraphCubit(history: history, git: _FakeGit(repoStatus()));
    await cubit.setRepoRoot('/repo');
    await cubit.search('abc', GitSearchMode.hash);
    expect(cubit.state.visibleRows.single.hash, 'abcdef1');
    await cubit.clearSearch();
    expect(cubit.state.visibleRows, hasLength(2));
    await cubit.close();
  });

  test('service error lands in errorMessage without losing rows', () async {
    final failing = _FailingHistory();
    final cubit = GitGraphCubit(history: failing, git: _FakeGit(repoStatus()));
    await cubit.setRepoRoot('/ok'); // 第一次成功载入 1 行
    await cubit.refresh(); // 第二次 graphRows 抛 GitException
    expect(cubit.state.errorMessage, isNotNull);
    expect(cubit.state.rows, isNotEmpty); // 旧行保留
    await cubit.close();
  });
}
```

`_detailFor(hash)` 构造一个 `GitCommitDetail`（字段同 Task 3 模型）。

```dart
class _FailingHistory implements GitHistoryService {
  bool failNext = false;
  int calls = 0;

  @override
  dynamic noSuchMethod(Invocation invocation) => super.noSuchMethod(invocation);

  @override
  Future<List<GitGraphRow>> graphRows(String dir,
      {int limit = GitHistoryService.initialLoadCommits,
      int skip = 0,
      String query = '',
      GitSearchMode mode = GitSearchMode.message}) async {
    calls++;
    if (failNext) throw GitException('boom');
    if (calls > 1) failNext = true; // 第二次开始失败
    return [commitRow('c$calls')];
  }
}
```

注意：`GitException` 从 `git_service.dart` 导入；`_FakeHistory` 与 `_FailingHistory` 都要实现 `graphRows`/`commitDetail` 并用 `noSuchMethod` 兜底其余成员。

- [ ] **Step 2: 跑测试确认失败**

Run: `cd client && flutter test test/cubits/git_graph_cubit_test.dart`
Expected: FAIL（GitGraphCubit 未定义）

- [ ] **Step 3: 实现 GitGraphState + GitGraphCubit**

创建 `client/lib/cubits/git_graph_cubit.dart`（骨架如下，方法体按 Step 1 用例实现；copyWith 哨兵用 `Object? x = _unset, {Object? clearError = false}` 模式）：

```dart
import 'dart:async';

import 'package:flutter_bloc/flutter_bloc.dart';
import 'package:equatable/equatable.dart';

import '../models/git_graph.dart';
import '../services/git/git_history_service.dart';
import '../services/git/git_service.dart' show GitException;
import '../utils/logging/logger.dart';

class GitGraphState extends Equatable {
  const GitGraphState({
    this.repoRoot = '',
    this.rows = const [],
    this.hasMore = false,
    this.isLoadingMore = false,
    this.isRefreshing = false,
    this.selectedHash,
    this.commitDetail,
    this.detailLoading = false,
    this.openFilePath,
    this.fileDiffText,
    this.fileDiffLoading = false,
    this.branches = const [],
    this.tags = const [],
    this.stashList = const [],
    this.currentBranch = '',
    this.ahead = 0,
    this.behind = 0,
    this.dirtyCount = 0,
    this.searchQuery = '',
    this.searchMode = GitSearchMode.message,
    this.errorMessage,
    this.gitAvailable = true,
  });
  // …字段声明…

  static const Object _unset = Object();

  GitGraphState copyWith({
    String? repoRoot,
    List<GitGraphRow>? rows,
    bool? hasMore,
    bool? isLoadingMore,
    bool? isRefreshing,
    Object? selectedHash = _unset,
    Object? commitDetail = _unset,
    bool? detailLoading,
    Object? openFilePath = _unset,
    Object? fileDiffText = _unset,
    bool? fileDiffLoading,
    List<GitBranchInfo>? branches,
    List<GitTagInfo>? tags,
    List<GitStashEntry>? stashList,
    String? currentBranch,
    int? ahead,
    int? behind,
    int? dirtyCount,
    String? searchQuery,
    GitSearchMode? searchMode,
    Object? errorMessage = _unset,
    bool? gitAvailable,
  }) {
    final isUnset = (Object? v) => identical(v, _unset);
    return GitGraphState(
      // …逐字段：isUnset(v) ? this.v : v as T?…
      errorMessage: isUnset(errorMessage)
          ? this.errorMessage
          : errorMessage as String?,
      selectedHash:
          isUnset(selectedHash) ? this.selectedHash : selectedHash as String?,
      commitDetail: isUnset(commitDetail)
          ? this.commitDetail
          : commitDetail as GitCommitDetail?,
      openFilePath:
          isUnset(openFilePath) ? this.openFilePath : openFilePath as String?,
      fileDiffText: isUnset(fileDiffText)
          ? this.fileDiffText
          : fileDiffText as String?,
      // …其余直传…
    );
  }

  /// hash 搜索模式对已加载行做前缀过滤（大小写不敏感）。
  List<GitGraphRow> get visibleRows {
    if (searchMode != GitSearchMode.hash || searchQuery.isEmpty) return rows;
    final q = searchQuery.toLowerCase();
    return rows
        .where((r) =>
            r is GitCommitRow && r.hash.toLowerCase().startsWith(q))
        .toList(growable: false);
  }

  @override
  List<Object?> get props => [
        repoRoot, rows, hasMore, isLoadingMore, isRefreshing, selectedHash,
        commitDetail, detailLoading, openFilePath, fileDiffText,
        fileDiffLoading, branches, tags, stashList, currentBranch, ahead,
        behind, dirtyCount, searchQuery, searchMode, errorMessage,
        gitAvailable,
      ];
}

class GitGraphCubit extends Cubit<GitGraphState> {
  GitGraphCubit({required GitHistoryService history, required GitService git})
      : _history = history,
        _git = git,
        super(const GitGraphState());

  final GitHistoryService _history;
  final GitService _git;

  bool _refreshInFlight = false;
  bool _refreshQueued = false;

  Future<void> setRepoRoot(String root) async {
    emit(GitGraphState(repoRoot: root));
    await refresh();
  }

  Future<void> refresh() async {
    if (_refreshInFlight) {
      _refreshQueued = true;
      return;
    }
    _refreshInFlight = true;
    try {
      await _runRefresh();
    } finally {
      _refreshInFlight = false;
      if (_refreshQueued && !isClosed) {
        _refreshQueued = false;
        await refresh();
      }
    }
  }

  Future<void> _runRefresh() async {
    final dir = state.repoRoot;
    if (dir.isEmpty || isClosed) return;
    try {
      final rows = await _history.graphRows(dir,
          query: state.searchQuery, mode: state.searchMode);
      final status = dir.isEmpty ? null : await _git.status(dir);
      if (isClosed || state.repoRoot != dir) return;
      emit(state.copyWith(
        rows: rows,
        hasMore: rows.length == GitHistoryService.initialLoadCommits,
        branches: await _history.branches(dir),
        tags: await _history.tags(dir),
        stashList: await _history.stashList(dir),
        currentBranch: status?.branch ?? '',
        ahead: status?.ahead ?? 0,
        behind: status?.behind ?? 0,
        dirtyCount: status == null ? 0 : status.staged.length + status.unstaged.length,
        gitAvailable: status?.isRepository ?? true,
        clearError: true,
      ));
    } on GitException catch (e) {
      if (isClosed) return;
      appLogger.e('[GitGraph] refresh failed: ${e.message}');
      emit(state.copyWith(errorMessage: e.message));
    }
  }
  // loadMore / selectCommit / openCommitFile / search / clearSearch 按
  // Step 1 用例补齐——loadMore 复制 refresh 的 coalescing（独立
  // _loadMoreInFlight 标志）；selectCommit 见下。
}
```

`loadMore`（独立 coalescing）与 `selectCommit` 的完整实现：

```dart
bool _loadMoreInFlight = false;

Future<void> loadMore() async {
  if (_loadMoreInFlight || !state.hasMore || state.isLoadingMore) return;
  _loadMoreInFlight = true;
  emit(state.copyWith(isLoadingMore: true));
  final dir = state.repoRoot;
  try {
    final more = await _history.graphRows(
      dir,
      skip: state.rows.length,
      limit: GitHistoryService.loadMoreCommits,
      query: state.searchQuery,
      mode: state.searchMode,
    );
    if (isClosed || state.repoRoot != dir) return;
    emit(state.copyWith(
      rows: [...state.rows, ...more],
      hasMore: more.length == GitHistoryService.loadMoreCommits,
      isLoadingMore: false,
    ));
  } on GitException catch (e) {
    if (isClosed) return;
    emit(state.copyWith(isLoadingMore: false, errorMessage: e.message));
  } finally {
    _loadMoreInFlight = false;
  }
}

Future<void> search(String query, GitSearchMode mode) async {
  emit(state.copyWith(searchQuery: query, searchMode: mode));
  await refresh();
}

Future<void> clearSearch() async {
  emit(state.copyWith(searchQuery: '', clearError: true));
  await refresh();
}

/// null → 清除选中与详情；非 null → 设置并懒加载详情。
Future<void> selectCommit(String? hash) async {
  final root = state.repoRoot;
  emit(state.copyWith(
    selectedHash: hash,
    commitDetail: hash == null ? null : state.commitDetail,
    detailLoading: hash != null,
    clearFileDiff: true,
  ));
  if (hash == null) return;
  try {
    final detail = await _history.commitDetail(root, hash);
    // stale guard：选中项或仓库已变化则丢弃
    if (isClosed || state.selectedHash != hash || state.repoRoot != root) {
      return;
    }
    emit(state.copyWith(commitDetail: detail, detailLoading: false));
  } on GitException catch (e) {
    if (isClosed) return;
    appLogger.e('[GitGraph] detail failed: ${e.message}');
    emit(state.copyWith(detailLoading: false, errorMessage: e.message));
  }
}

Future<void> openCommitFile(GitCommitFileChange file) async {
  final detail = state.commitDetail;
  if (detail == null) return;
  emit(state.copyWith(
    openFilePath: file.path,
    fileDiffLoading: true,
    fileDiffText: null,
  ));
  try {
    final text = await _history.commitFileDiff(
      state.repoRoot,
      hash: detail.hash,
      parent: detail.parents.isNotEmpty ? detail.parents.first : null,
      path: file.previousPath ?? file.path,
    );
    if (isClosed || state.openFilePath != file.path) return;
    emit(state.copyWith(fileDiffText: text, fileDiffLoading: false));
  } on GitException catch (e) {
    if (isClosed) return;
    emit(state.copyWith(fileDiffLoading: false, errorMessage: e.message));
  }
}

void closeFileDiff() =>
    emit(state.copyWith(openFilePath: null, fileDiffText: null,
        fileDiffLoading: false));
```

注意 `copyWith` 需要补 `clearFileDiff` 命名哨兵（`bool clearFileDiff = false`）。

- [ ] **Step 4: 跑测试确认通过**

Run: `cd client && flutter test test/cubits/git_graph_cubit_test.dart`
Expected: PASS（6 tests）

- [ ] **Step 5: Commit**

```bash
git add client/lib/cubits/git_graph_cubit.dart \
  client/test/cubits/git_graph_cubit_test.dart
git commit -m "feat(git-graph): add graph cubit with paging/selection/search"
```

---

### Task 6: 写操作扩展 + GitRepoStore 托管

**Files:**
- Create: `client/lib/cubits/git_graph_actions_controller.dart`
- Modify: `client/lib/cubits/git_graph_cubit.dart`（构造器加可选 actions 参数 + surfaceError + visibleForTesting getter）
- Modify: `client/lib/services/git/git_repo_store.dart`（graphCubitFor 托管）
- Modify: `client/lib/services/git/git_history_actions.dart`（debugOverrideFactory 测试缝）
- Create: `client/test/support/git_graph_test_fakes.dart`（共享 fake；Task 5 测试同步改为 import）
- Test: `client/test/cubits/git_graph_actions_controller_test.dart`

**Interfaces:**
- Consumes: `GitGraphCubit`/`GitGraphState`、`GitHistoryService`、`GitService`、`GitHistoryActions`。
- Produces:

```dart
class GitGraphActionsController {
  GitGraphActionsController({required GitGraphCubit cubit});
  // 每个方法：执行 → cubit.refresh()；GitException → surfaceError + 返回 false
  Future<bool> createBranch(String name, {required String atHash});
  Future<bool> renameBranch(String oldName, String newName);
  Future<bool> deleteBranch(String name, {bool force = false});
  Future<bool> checkoutBranch(String name);
  Future<bool> checkoutCommit(String hash);
  Future<bool> mergeIntoCurrent(String refName);
  Future<bool> createTag(String name, {String? at, String? message});
  Future<bool> deleteTag(String name);
  Future<bool> pushTag(String name);
  Future<bool> cherryPick(String hash);
  Future<bool> revert(String hash);
  Future<bool> resetTo(String ref, {required GitResetMode mode});
  Future<bool> stashPop({String? ref});
  Future<bool> stashApply({String? ref});
  Future<bool> stashDrop({String? ref});
  Future<bool> fetchAll();
  Future<bool> pull(); // 委托 GitService.pull(dir)
  Future<bool> push(); // 委托 GitService.push(dir)
}

// GitGraphCubit 新增成员：
//   GitGraphCubit({required history, required git, GitHistoryActions? actions})
//   @visibleForTesting GitHistoryActions get actions;
//   @visibleForTesting GitService get gitService;
//   void surfaceError(String message);

// GitRepoStore 新增：
//   GitGraphCubit graphCubitFor(String root, {required RuntimeContext workContext});
```

- [ ] **Step 1: 写失败测试**

先创建共享 fake `client/test/support/git_graph_test_fakes.dart`（把 Task 5 测试里的 `_FakeHistory`/`_FakeGit`/`commitRow` 移进来公开为 `FakeHistoryForGraph`/`FakeGitForGraph`/`graphCommitRow`，并新增 `repoStatus()` 与 `RecordingGraphActions`）：

```dart
import 'package:teampilot/models/git_graph.dart';
import 'package:teampilot/models/git_status.dart';
import 'package:teampilot/services/git/git_history_actions.dart';
import 'package:teampilot/services/git/git_history_service.dart';
import 'package:teampilot/services/git/git_service.dart';

class FakeHistoryForGraph implements GitHistoryService {
  FakeHistoryForGraph({this.rows = const [], this.detail});
  final List<GitGraphRow> rows;
  final GitCommitDetail? detail;
  int graphCalls = 0;
  Map<String, Object?> lastArgs = {};

  @override
  dynamic noSuchMethod(Invocation invocation) => super.noSuchMethod(invocation);

  @override
  Future<List<GitGraphRow>> graphRows(String dir,
      {int limit = GitHistoryService.initialLoadCommits,
      int skip = 0,
      String query = '',
      GitSearchMode mode = GitSearchMode.message}) async {
    graphCalls++;
    lastArgs = {'limit': limit, 'skip': skip, 'query': query, 'mode': mode};
    return rows;
  }

  @override
  Future<GitCommitDetail> commitDetail(String dir, String hash) async =>
      detail!;
}

class FakeGitForGraph implements GitService {
  FakeGitForGraph(this.statusResult);
  final GitRepoStatus statusResult;

  @override
  dynamic noSuchMethod(Invocation invocation) => super.noSuchMethod(invocation);

  @override
  Future<GitRepoStatus> status(String dir) async => statusResult;
}

class RecordingGraphActions implements GitHistoryActions {
  final List<List<String>> calls = [];
  Object? throwNext;

  @override
  dynamic noSuchMethod(Invocation invocation) => super.noSuchMethod(invocation);

  void _maybeThrow() {
    final t = throwNext;
    if (t != null) throw t;
  }

  @override
  Future<void> createBranchAt(String dir, String name,
      {required String startPoint}) async {
    calls.add(['branch', name, startPoint]);
    _maybeThrow();
  }

  @override
  Future<void> deleteBranch(String dir, String name,
      {bool force = false}) async {
    calls.add(['branch', force ? '-D' : '-d', name]);
    _maybeThrow();
  }

  @override
  Future<void> resetTo(String dir, String ref,
      {required GitResetMode mode}) async {
    calls.add([
      'reset',
      switch (mode) {
        GitResetMode.soft => '--soft',
        GitResetMode.mixed => '--mixed',
        GitResetMode.hard => '--hard',
      },
      ref,
    ]);
    _maybeThrow();
  }
}

GitCommitRow graphCommitRow(String hash) => GitCommitRow(
      edges: const [GitGraphEdge(0, 0, 0)],
      node: const GitGraphNode(0, 0),
      hash: hash,
      parents: const ['p0'],
      authorName: 'A',
      authorEmail: 'a@x',
      authorDate: DateTime.fromMillisecondsSinceEpoch(0, isUtc: true),
      subject: 's-$hash',
      refs: const [],
    );

// 字段以 client/lib/models/git_status.dart 实际定义为准，实现者先读该文件。
GitRepoStatus repoStatus() => GitRepoStatus(
      isRepository: true,
      branch: 'main',
      upstream: 'origin/main',
      ahead: 1,
      behind: 0,
      hasCommits: true,
    );
```

测试文件 `client/test/cubits/git_graph_actions_controller_test.dart`：

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:teampilot/cubits/git_graph_actions_controller.dart';
import 'package:teampilot/cubits/git_graph_cubit.dart';
import 'package:teampilot/models/git_graph.dart';
import 'package:teampilot/services/git/git_service.dart';

import '../support/git_graph_test_fakes.dart';

void main() {
  late RecordingGraphActions actions;

  setUp(() => actions = RecordingGraphActions());

  Future<(GitGraphCubit, GitGraphActionsController)> build() async {
    final cubit = GitGraphCubit(
      history: FakeHistoryForGraph(rows: [graphCommitRow('c1')]),
      git: FakeGitForGraph(repoStatus()),
      actions: actions,
    );
    await cubit.setRepoRoot('/repo');
    return (cubit, GitGraphActionsController(cubit: cubit));
  }

  test('createBranch delegates then refreshes without error', () async {
    final (cubit, controller) = await build();
    expect(await controller.createBranch('dev', atHash: 'c1'), isTrue);
    expect(actions.calls.single, ['branch', 'dev', 'c1']);
    expect(cubit.state.errorMessage, isNull);
    expect(cubit.state.rows.single.hash, 'c1'); // 刷新后数据仍在
    await cubit.close();
  });

  test('failure surfaces error and returns false', () async {
    actions.throwNext = GitException('conflict');
    final (cubit, controller) = await build();
    expect(await controller.deleteBranch('dev'), isFalse);
    expect(cubit.state.errorMessage, contains('conflict'));
    await cubit.close();
  });

  test('resetTo hard maps mode flag', () async {
    final (cubit, controller) = await build();
    expect(
        await controller.resetTo('main', mode: GitResetMode.hard), isTrue);
    expect(actions.calls.single, ['reset', '--hard', 'main']);
    await cubit.close();
  });
}
```

注意：测试 import 路径按仓库实际 test/ 目录相对关系写（若 `test/support` 不在包内路径，用 `package:teampilot/test/support/...` 形式——以现有测试文件引用 post_frame_test_harness 的写法为准）。

- [ ] **Step 2: 跑测试确认失败**

Run: `cd client && flutter test test/cubits/git_graph_actions_controller_test.dart`
Expected: FAIL（controller 未定义）

- [ ] **Step 3: 实现**

创建 `client/lib/cubits/git_graph_actions_controller.dart`：

```dart
import 'package:meta/meta.dart';

import '../models/git_graph.dart';
import '../services/git/git_history_actions.dart';
import '../services/git/git_service.dart' show GitException;
import '../utils/logging/logger.dart';
import 'git_graph_cubit.dart';

/// Git Graph 面板的写操作入口。每个操作成功后触发 cubit.refresh()，
/// 失败时把 [GitException.message] 写入状态并返回 false（UI 弹提示）。
class GitGraphActionsController {
  GitGraphActionsController({required this.cubit});

  final GitGraphCubit cubit;

  GitHistoryActions get _actions => cubit.actions;
  String get _dir => cubit.state.repoRoot;

  Future<bool> createBranch(String name, {required String atHash}) =>
      _run(() => _actions.createBranchAt(_dir, name, startPoint: atHash));

  Future<bool> renameBranch(String oldName, String newName) =>
      _run(() => _actions.renameBranch(_dir, oldName, newName));

  Future<bool> deleteBranch(String name, {bool force = false}) =>
      _run(() => _actions.deleteBranch(_dir, name, force: force));

  Future<bool> checkoutBranch(String name) =>
      _run(() => _actions.checkoutBranch(_dir, name));

  Future<bool> checkoutCommit(String hash) =>
      _run(() => _actions.checkoutCommit(_dir, hash));

  Future<bool> mergeIntoCurrent(String refName) =>
      _run(() => _actions.mergeIntoCurrent(_dir, refName));

  Future<bool> createTag(String name, {String? at, String? message}) =>
      _run(() => _actions.createTag(_dir, name, at: at, message: message));

  Future<bool> deleteTag(String name) =>
      _run(() => _actions.deleteTag(_dir, name));

  Future<bool> pushTag(String name) => _run(() => _actions.pushTag(_dir, name));

  Future<bool> cherryPick(String hash) =>
      _run(() => _actions.cherryPick(_dir, hash));

  Future<bool> revert(String hash) => _run(() => _actions.revert(_dir, hash));

  /// [ref] 为目标提交或分支名；hard 由确认对话框把关后才进入这里。
  Future<bool> resetTo(String ref, {required GitResetMode mode}) =>
      _run(() => _actions.resetTo(_dir, ref, mode: mode));

  Future<bool> stashPop({String? ref}) =>
      _run(() => _actions.stashPop(_dir, ref: ref));

  Future<bool> stashApply({String? ref}) =>
      _run(() => _actions.stashApply(_dir, ref: ref));

  Future<bool> stashDrop({String? ref}) =>
      _run(() => _actions.stashDrop(_dir, ref: ref));

  Future<bool> fetchAll() => _run(() => _actions.fetchAll(_dir));

  Future<bool> pull() => _run(() => cubit.gitService.pull(_dir));
  Future<bool> push() => _run(() => cubit.gitService.push(_dir));

  Future<bool> _run(Future<void> Function() action) async {
    try {
      await action();
      await cubit.refresh();
      return true;
    } on GitException catch (e) {
      appLogger.e('[GitGraph] action failed: ${e.message}');
      cubit.surfaceError(e.message);
      await cubit.refresh(); // 失败也可能部分生效（如冲突），刷新对齐真实状态
      return false;
    }
  }
}
```

修改 `client/lib/cubits/git_graph_cubit.dart`：

```dart
// 构造函数追加可选参数：
GitGraphCubit({
  required GitHistoryService history,
  required GitService git,
  GitHistoryActions? actions,
})  : _history = history,
      _git = git,
      _actions = actions ?? GitHistoryActions(),
      super(const GitGraphState());

// 新增字段与成员：
final GitHistoryActions _actions;

@visibleForTesting
GitHistoryActions get actions => _actions;

@visibleForTesting
GitService get gitService => _git;

void surfaceError(String message) =>
    emit(state.copyWith(errorMessage: message));
```

（`@visibleForTesting` 来自 `package:meta/meta.dart`，flutter 项目可直接用。）

修改 `client/lib/services/git/git_history_actions.dart` 加测试缝（同 GitService 模式）：

```dart
static GitHistoryActions Function()? debugOverrideFactory;
```

修改 `client/lib/services/git/git_repo_store.dart`：

```dart
// 构造器加 factory 参数（默认 _defaultGraphFactory），便于 store 级测试注入：
GitRepoStore({
  GitCubit Function(String root, RuntimeContext workContext)? cubitFactory,
  GitGraphCubit Function(String root, RuntimeContext workContext)?
      graphCubitFactory,
  int maxRetained = 8,
})  : _cubitFactory = cubitFactory ?? _defaultFactory,
      _graphFactory = graphCubitFactory ?? _defaultGraphFactory,
      _maxRetained = maxRetained;

static GitGraphCubit _defaultGraphFactory(String root, RuntimeContext ctx) {
  final history = GitHistoryService.debugOverrideFactory?.call() ??
      GitHistoryService.forContext(ctx);
  final git =
      GitService.debugOverrideFactory?.call() ?? GitService.forContext(ctx);
  final actions = GitHistoryActions.debugOverrideFactory?.call() ??
      GitHistoryActions.forContext(ctx);
  return GitGraphCubit(history: history, git: git, actions: actions);
}

final GitGraphCubit Function(String root, RuntimeContext workContext)
    _graphFactory;
final Map<String, GitGraphCubit> _graphCubits = <String, GitGraphCubit>{};

/// 返回保留的 graph cubit（独立 LRU，≤ maxRetained），首次访问异步预热。
GitGraphCubit graphCubitFor(
  String root, {
  required RuntimeContext workContext,
}) {
  final key = _cacheKey(root, workContext);
  final existing = _graphCubits.remove(key);
  if (existing != null) {
    _graphCubits[key] = existing;
    return existing;
  }
  final cubit = _graphFactory(_ctx.normalize(root), workContext);
  unawaited(cubit.setRepoRoot(root));
  _graphCubits[key] = cubit;
  _evict();
  return cubit;
}

void _evict() {
  while (_cubits.length > _maxRetained) {
    final oldestKey = _cubits.keys.first;
    _cubits.remove(oldestKey)?.close();
  }
  while (_graphCubits.length > _maxRetained) {
    final oldestKey = _graphCubits.keys.first;
    _graphCubits.remove(oldestKey)?.close();
  }
}
```

`dispose()` 补关闭 `_graphCubits.values`。文件头加 `import 'dart:async';`（unawaited）与 `git_graph_cubit.dart`、`git_history_service.dart`、`git_history_actions.dart` 的 import。

- [ ] **Step 4: 跑全部相关测试确认通过**

Run: `cd client && flutter test test/cubits/git_graph_cubit_test.dart test/cubits/git_graph_actions_controller_test.dart`
Expected: PASS（Task 5 用例改用共享 fakes 后仍全绿）

- [ ] **Step 5: Commit**

```bash
git add client/lib/cubits/git_graph_cubit.dart \
  client/lib/cubits/git_graph_actions_controller.dart \
  client/lib/services/git/git_repo_store.dart \
  client/lib/services/git/git_history_actions.dart \
  client/test/support/git_graph_test_fakes.dart \
  client/test/cubits/git_graph_actions_controller_test.dart \
  client/test/cubits/git_graph_cubit_test.dart
git commit -m "feat(git-graph): wire mutation controller and store retention"
```


### Task 7: WorkbenchTabKind + FloatingSurface 接入

**Files:**
- Modify: `client/lib/cubits/workbench/workbench_tab.dart`（enum + surfaceIdFor + WorkbenchTabId factory）
- Create: `client/lib/services/floating_workspace/surfaces/git_graph_floating_surface.dart`
- Modify: `client/lib/services/floating_workspace/floating_surface_registry.dart`（withDefaults 加可选参数）
- Modify: `client/lib/app/app_shell.dart`（装配，约 L2103 的 `FloatingSurfaceRegistry.withDefaults(...)`）
- Modify: `client/lib/pages/floating_workspace/floating_workspace_tab_bar.dart`（floatingTabMenuIdentity + _iconFor 两个 switch 各加 'gitGraph' 分支）
- Test: `client/test/cubits/workbench/workbench_tab_git_graph_test.dart`

**Interfaces:**
- Consumes: `FloatingSurface` 抽象（id/emptyAction/allowMultipleTabs/createTab/build/activate/canClose/onTabClosed）、`FloatingTab{id,surfaceId,title,payload}`、`WorkspaceToolsScope.maybeOf(context)`。
- Produces:

```dart
// workbench_tab.dart
enum WorkbenchTabKind { session, file, diff, shell, run, htmlPreview, gitGraph }
String? surfaceIdFor(WorkbenchTabKind kind) => switch (kind) {
  // …现有分支…
  WorkbenchTabKind.gitGraph => 'gitGraph',
};
factory WorkbenchTabId.gitGraph(String repoRoot) =>
    WorkbenchTabId._(WorkbenchTabKind.gitGraph, repoRoot);

// git_graph_floating_surface.dart
class GitGraphFloatingSurface extends FloatingSurface {
  GitGraphFloatingSurface();
  static const String surfaceId = 'gitGraph';
}
```

- [ ] **Step 1: 写失败测试**

```dart
// client/test/cubits/workbench/workbench_tab_git_graph_test.dart
import 'package:flutter_test/flutter_test.dart';
import 'package:teampilot/cubits/workbench/workbench_tab.dart';

void main() {
  test('gitGraph kind maps to gitGraph floating surface id', () {
    expect(surfaceIdFor(WorkbenchTabKind.gitGraph), 'gitGraph');
    expect(isCenterStripWorkbenchTab(WorkbenchTabKind.gitGraph), isFalse);
  });

  test('WorkbenchTabId.gitGraph carries repoRoot as id', () {
    final id = WorkbenchTabId.gitGraph('/repo');
    expect(id.kind, WorkbenchTabKind.gitGraph);
    expect(id.id, '/repo');
  });
}
```

- [ ] **Step 2: 跑测试确认失败** → Run: `cd client && flutter test test/cubits/workbench/workbench_tab_git_graph_test.dart` → Expected: FAIL

- [ ] **Step 3: 实现**

1) `workbench_tab.dart`：enum 追加 `gitGraph`；`surfaceIdFor` switch 加 `WorkbenchTabKind.gitGraph => 'gitGraph',`；新增 factory：

```dart
/// Git Graph 浮动页：id 即仓库根绝对路径。
factory WorkbenchTabId.gitGraph(String repoRoot) =>
    WorkbenchTabId._(WorkbenchTabKind.gitGraph, repoRoot);
```

2) 创建 `client/lib/services/floating_workspace/surfaces/git_graph_floating_surface.dart`：

```dart
import 'package:flutter/material.dart';
import 'package:path/path.dart' as p;

import '../../../models/floating_workspace_tab.dart';
import '../../../pages/git_graph/git_graph_pane.dart';
import '../floating_surface.dart';

/// 浮动面板中的 Git Graph 页。payload = 仓库根绝对路径；
/// 每仓库一个标签页（allowMultipleTabs）。
class GitGraphFloatingSurface extends FloatingSurface {
  GitGraphFloatingSurface();

  static const String surfaceId = 'gitGraph';

  @override
  String get id => surfaceId;

  @override
  FloatingEmptyAction? get emptyAction => null;

  @override
  bool get allowMultipleTabs => true;

  @override
  FloatingTab createTab({required String workspaceId, Object? payload}) {
    final root = payload is String ? payload.trim() : '';
    return FloatingTab(
      id: root.isEmpty ? '$surfaceId:' : '$surfaceId:$root',
      surfaceId: surfaceId,
      title: root.isEmpty ? 'Git Graph' : '${p.basename(root)} · Graph',
      payload: root.isEmpty ? null : root,
    );
  }

  @override
  Widget build(BuildContext context, FloatingTab tab) {
    final root = tab.payload;
    if (root is! String || root.isEmpty) return const SizedBox.shrink();
    final workspaceId = tab.id.split(':').first; // 见下方说明
    return GitGraphPane(workspaceId: workspaceId, repoRoot: root);
  }

  @override
  Future<void> activate(FloatingTab tab) async {}
}
```

说明：workspaceId 不在 payload 里——参照 HtmlPreviewFloatingSurface 的做法从 `context.read<FloatingWorkspaceCubit>().state.activeWorkspaceId` 取。把 build 改为：

```dart
@override
Widget build(BuildContext context, FloatingTab tab) {
  final root = tab.payload;
  if (root is! String || root.isEmpty) return const SizedBox.shrink();
  final workspaceId =
      context.read<FloatingWorkspaceCubit>().state.activeWorkspaceId;
  if (workspaceId.isEmpty) return const SizedBox.shrink();
  return GitGraphPane(workspaceId: workspaceId, repoRoot: root);
}
```

（需要 import `cubits/floating_workspace/floating_workspace_cubit.dart` 与 `package:flutter_bloc/flutter_bloc.dart`；删除上面示意里的 split 行。）

3) `floating_surface_registry.dart`：`withDefaults` 增加命名参数 `FloatingSurface? gitGraph` 并追加进列表。

4) `app_shell.dart` L2103 处追加：

```dart
gitGraph: GitGraphFloatingSurface(),
```

5) `floating_workspace_tab_bar.dart` 两处 switch：
   - `floatingTabMenuIdentity`：`'gitGraph' => (WorkbenchTabKind.gitGraph, tab.payload is String ? tab.payload as String : null),`
   - `_iconFor`：`'gitGraph' => Icons.account_tree_outlined,`

- [ ] **Step 4: 跑测试确认通过**

Run: `cd client && flutter analyze --no-fatal-infos --no-fatal-warnings && flutter test test/cubits/workbench/workbench_tab_git_graph_test.dart`
Expected: PASS（analyze 无新错误）

- [ ] **Step 5: Commit**

```bash
git add client/lib/cubits/workbench/workbench_tab.dart \
  client/lib/services/floating_workspace/surfaces/git_graph_floating_surface.dart \
  client/lib/services/floating_workspace/floating_surface_registry.dart \
  client/lib/app/app_shell.dart \
  client/lib/pages/floating_workspace/floating_workspace_tab_bar.dart \
  client/test/cubits/workbench/workbench_tab_git_graph_test.dart
git commit -m "feat(git-graph): register git graph floating surface"
```

---

### Task 8: Lane Painter + 行片元

**Files:**
- Create: `client/lib/pages/git_graph/git_graph_lane_painter.dart`
- Create: `client/lib/pages/git_graph/git_graph_row_tile.dart`
- Test: `client/test/pages/git_graph/git_graph_row_tile_test.dart`

**Interfaces:**
- Consumes: `GitGraphRow`/`GitCommitRow`/`GitGraphEdge`/`GitRefDecoration`。
- Produces:

```dart
/// 单行图形画笔：在宽 laneWidth*maxLane 的画布内画节点与连线。
/// rounded 风格：直通竖线 + 三次贝塞尔 S 曲线。angular 后续可另实现同接口。
class GitGraphLanePainter extends CustomPainter {
  GitGraphLanePainter({required this.edges, this.node, required this.palette});
  final List<GitGraphEdge> edges;
  final GitGraphNode? node;      // spacer 行传 null
  final List<Color> palette;     // colorIndex → Color，调用方由 TpTheme 派生
  // paint：laneX(lane) = laneWidth/2 + lane*laneWidth；node 半径 4.5；
  // 曲线 cubic control points: (x0,y0)+(0,h/2) / (x1,y1)-(0,h/2)
}

/// 一行 tile：左侧固定宽 graph 区（CustomPaint），右侧 subject/refs/author/date。
class GitGraphRowTile extends StatelessWidget {
  const GitGraphRowTile({
    super.key,
    required this.row,
    required this.selected,
    required this.onTap,
    this.onSecondaryTapUp,   // 桌面右键
    this.onLongPress,        // 移动端长按（Android SSH 场景）
    this.dateFormat,         // 默认 MM/dd HH:mm
  });
}
```

- [ ] **Step 1: 写失败测试**

```dart
// client/test/pages/git_graph/git_graph_row_tile_test.dart
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:teampilot/models/git_graph.dart';
import 'package:teampilot/pages/git_graph/git_graph_row_tile.dart';

GitCommitRow makeRow(String hash, {List<GitRefDecoration> refs = const []}) =>
    GitCommitRow(
      edges: const [GitGraphEdge(0, 0, 0)],
      node: const GitGraphNode(0, 0),
      hash: hash,
      parents: const ['p'],
      authorName: 'Ann',
      authorEmail: 'ann@x',
      authorDate: DateTime.utc(2026, 8, 25, 10, 30),
      subject: 'add feature',
      refs: refs,
    );

Future<void> pump(WidgetTester tester, Widget child) async {
  await tester.pumpWidget(MaterialApp(home: Scaffold(body: child)));
}

void main() {
  testWidgets('commit row renders subject, author and date', (tester) async {
    await pump(
      tester,
      SizedBox(
        height: 26,
        width: 600,
        child: GitGraphRowTile(row: makeRow('abc'), selected: false, onTap: () {}),
      ),
    );
    expect(find.text('add feature'), findsOneWidget);
    expect(find.textContaining('Ann'), findsOneWidget);
    expect(find.textContaining('08/25'), findsOneWidget);
  });

  testWidgets('ref decorations render as chips', (tester) async {
    await pump(
      tester,
      SizedBox(
        height: 26,
        width: 600,
        child: GitGraphRowTile(
          row: makeRow('abc', refs: [
            const GitRefDecoration(GitRefDecorationKind.head, 'main'),
            const GitRefDecoration(GitRefDecorationKind.tag, 'v1'),
          ]),
          selected: false,
          onTap: () {},
        ),
      ),
    );
    expect(find.text('main'), findsOneWidget);
    expect(find.text('v1'), findsOneWidget);
  });

  testWidgets('tap fires callback', (tester) async {
    var tapped = false;
    await pump(
      tester,
      SizedBox(
        height: 26,
        width: 600,
        child: GitGraphRowTile(
          row: makeRow('abc'),
          selected: false,
          onTap: () => tapped = true,
        ),
      ),
    );
    await tester.tap(find.text('add feature'));
    expect(tapped, isTrue);
  });
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd client && flutter test test/pages/git_graph/git_graph_row_tile_test.dart` → Expected: FAIL

- [ ] **Step 3: 实现 painter 与 tile**

```dart
// client/lib/pages/git_graph/git_graph_lane_painter.dart
import 'package:flutter/material.dart';
import '../../models/git_graph.dart';

/// rounded 风格 lane 绘制策略。后续 angular 实现同一签名即可替换。
class GitGraphLanePainter extends CustomPainter {
  GitGraphLanePainter({
    required this.edges,
    this.node,
    required this.palette,
    this.laneWidth = 14,
  });

  static const double nodeRadius = 4.5;

  final List<GitGraphEdge> edges;
  final GitGraphNode? node;
  final List<Color> palette;
  final double laneWidth;

  Color _colorOf(int index) => palette[index % palette.length];

  double _x(int lane) => laneWidth / 2 + lane * laneWidth;

  @override
  void paint(Canvas canvas, Size size) {
    final h = size.height;
    for (final edge in edges) {
      final paint = Paint()
        ..color = _colorOf(edge.colorIndex)
        ..style = PaintingStyle.stroke
        ..strokeWidth = 2
        ..strokeCap = StrokeCap.round;
      final x0 = _x(edge.fromLane);
      final x1 = _x(edge.toLane);
      if (edge.isStraight) {
        canvas.drawLine(Offset(x0, 0), Offset(x1, h), paint);
      } else {
        final path = Path()
          ..moveTo(x0, 0)
          ..cubicTo(x0, h / 2, x1, h / 2, x1, h);
        canvas.drawPath(path, paint);
      }
    }
    final n = node;
    if (n != null) {
      final fill = Paint()..color = _colorOf(n.colorIndex);
      canvas.drawCircle(Offset(_x(n.lane), h / 2), nodeRadius, fill);
      final ring = Paint()
        ..color = _colorOf(n.colorIndex).withValues(alpha: 0.35)
        ..style = PaintingStyle.stroke
        ..strokeWidth = 2;
      canvas.drawCircle(Offset(_x(n.lane), h / 2), nodeRadius + 1.5, ring);
    }
  }

  @override
  bool shouldRepaint(covariant GitGraphLanePainter old) =>
      old.node != node ||
      old.edges.length != edges.length ||
      _sameEdges(old) == false;

  bool _sameEdges(GitGraphLanePainter old) {
    for (var i = 0; i < edges.length; i++) {
      if (old.edges[i] != edges[i]) return true;
    }
    return false;
  }
}
```

```dart
// client/lib/pages/git_graph/git_graph_row_tile.dart
import 'dart:io' show Platform;

import 'package:flutter/gestures.dart';
import 'package:flutter/material.dart';
import 'package:intl/intl.dart';

import '../../models/git_graph.dart';
import 'git_graph_lane_painter.dart';

/// 提交图单行：graph 片元 + 文本列。spacer 行用 [GitGraphSpacerTile]。
class GitGraphRowTile extends StatelessWidget {
  const GitGraphRowTile({
    super.key,
    required this.row,
    required this.selected,
    required this.onTap,
    this.onSecondaryTapUp,
    this.onLongPress,
    this.palette = defaultPalette,
    this.dateFormat,
  });

  static const List<Color> defaultPalette = [
    Color(0xFF4C8DFF), Color(0xFF54C08A), Color(0xFFE0A63F),
    Color(0xFFD96C6C), Color(0xFF9B7BE0), Color(0xFF4FB6C9),
    Color(0xFFC97BB5), Color(0xFF8FA83F), Color(0xFFB08B5C),
    Color(0xFF7C93F0), Color(0xFF5FBF9E), Color(0xFFDD8A4D),
  ];

  final GitCommitRow row;
  final bool selected;
  final VoidCallback onTap;
  final GestureTapCallback? onLongPress;
  final void Function(TapDownDetails)? onSecondaryTapUp;
  final List<Color> palette;
  final DateFormat? dateFormat;

  @override
  Widget build(BuildContext context) {
    final theme = Theme.of(context);
    final cs = theme.colorScheme;
    final bg = selected ? cs.primary.withValues(alpha: 0.12) : null;
    return GestureDetector(
      behavior: HitTestBehavior.opaque,
      onTap: onTap,
      onLongPress: onLongPress,
      onSecondaryTapUp: onSecondaryTapUp,
      child: Container(
        height: 28,
        color: bg,
        padding: const EdgeInsets.only(right: 12),
        child: Row(
          children: [
            SizedBox(
              width: _graphWidth(),
              child: CustomPaint(
                size: const Size(double.infinity, 28),
                painter: GitGraphLanePainter(
                  edges: row.edges,
                  node: row.node,
                  palette: palette,
                ),
              ),
            ),
            const SizedBox(width: 8),
            ...[for (final r in row.refs) _RefChip(decoration: r)],
            Expanded(
              child: Text(
                row.subject,
                maxLines: 1,
                overflow: TextOverflow.ellipsis,
                style: TpTextStyles.of(context).sm,
              ),
            ),
            const SizedBox(width: 8),
            Text(
              row.authorName,
              maxLines: 1,
              overflow: TextOverflow.ellipsis,
              style:
                  TpTextStyles.of(context).xsColored(cs.onSurfaceVariant),
            ),
            const SizedBox(width: 10),
            Text(
              (dateFormat ?? DateFormat('MM/dd HH:mm')).format(
                  row.authorDate.toLocal()),
              style: TpTextStyles.of(context)
                  .xsColored(cs.onSurfaceVariant),
            ),
          ],
        ),
      ),
    );
  }

  double _graphWidth() {
    final maxLane = [
      for (final e in row.edges) e.fromLane,
      for (final e in row.edges) e.toLane,
      row.node.lane,
    ].reduce((a, b) => a > b ? a : b);
    return (maxLane + 1) * GitGraphLanePainter().laneWidth;
  }
}

class _RefChip extends StatelessWidget {
  const _RefChip({required this.decoration});
  final GitRefDecoration decoration;

  @override
  Widget build(BuildContext context) {
    final colors = switch (decoration.kind) {
      GitRefDecorationKind.head => Colors.amber,
      GitRefDecorationKind.localBranch => Colors.lightBlueAccent,
      GitRefDecorationKind.remoteBranch => Colors.tealAccent.shade200,
      GitRefDecorationKind.tag => Colors.purpleAccent,
    };
    return Padding(
      padding: const EdgeInsets.only(right: 4),
      child: Container(
        padding: const EdgeInsets.symmetric(horizontal: 5, vertical: 1),
        decoration: BoxDecoration(
          color: colors.withValues(alpha: 0.18),
          borderRadius: BorderRadius.circular(4),
          border: Border.all(color: colors.withValues(alpha: 0.55)),
        ),
        child: Row(mainAxisSize: MainAxisSize.min, children: [
          Icon(
            switch (decoration.kind) {
              GitRefDecorationKind.head => Icons.push_pin_outlined,
              GitRefDecorationKind.tag => Icons.sell_outlined,
              _ => Icons.call_split_rounded,
            },
            size: 9,
            color: colors,
          ),
          const SizedBox(width: 2),
          Text(decoration.name.isEmpty ? 'HEAD' : decoration.name,
              style: TextStyle(fontSize: 10, color: colors)),
        ]),
      ),
    );
  }
}

/// 无提交的纯连线行（半高）。
class GitGraphSpacerTile extends StatelessWidget {
  const GitGraphSpacerTile({
    super.key,
    required this.row,
    this.palette = GitGraphRowTile.defaultPalette,
  });

  final GitGraphRow row;
  final List<Color> palette;

  @override
  Widget build(BuildContext context) {
    return IgnorePointer(
      child: SizedBox(
        height: 16,
        child: CustomPaint(
          size: const Size(double.infinity, 16),
          painter: GitGraphLanePainter(edges: row.edges, palette: palette),
        ),
      ),
    );
  }
}
```

注意：
- `withValues(alpha:)` 是当前 Flutter Color API；若仓库 SDK 版本较旧改用 `withOpacity(...)`（以 `flutter analyze` 结果为准，全库统一即可）。
- `TpTextStyles.of(context)` 用法参照 `git_source_control_panel.dart:721`。
- `intl` 已是依赖（pubspec 中 DateFormat 可用；若未引入则手写 `MM/dd HH:mm` 格式化 helper 放本文件内，不新增依赖——先查 `client/pubspec.yaml`）。
- `dart:io Platform` import 若未使用则删除（tile 不做平台分支时）。

- [ ] **Step 4: 跑测试确认通过**

Run: `cd client && flutter test test/pages/git_graph/git_graph_row_tile_test.dart`
Expected: PASS（3 tests）

- [ ] **Step 5: Commit**

```bash
git add client/lib/pages/git_graph/git_graph_lane_painter.dart \
  client/lib/pages/git_graph/git_graph_row_tile.dart \
  client/test/pages/git_graph/git_graph_row_tile_test.dart
git commit -m "feat(git-graph): lane painter and row tiles"
```

---

### Task 9: GitGraphPane 骨架（工具条 + 列表 + 加载更多 + 未提交行 + 状态栏）

**Files:**
- Create: `client/lib/pages/git_graph/git_graph_toolbar.dart`
- Create: `client/lib/pages/git_graph/git_graph_pane.dart`
- Modify: `client/lib/l10n/app_en.arb`、`app_zh.arb`（本任务与 Task 10/11 一起补齐所需 key）
- Test: `client/test/pages/git_graph/git_graph_pane_test.dart`

**Interfaces:**
- Consumes: `GitGraphCubit`/`GitGraphState`、`GitGraphActionsController`、Task 8 tiles。
- Produces:

```dart
/// 浮动面板主体。内部：
/// BlocProvider.value(value: store.graphCubitFor(repoRoot, workContext))
/// 由 surface 保证——pane 自身假定祖先已提供 GitGraphCubit。
class GitGraphPane extends StatefulWidget {
  const GitGraphPane({super.key, required this.workspaceId, required this.repoRoot});
}
// 布局：Column[ Toolbar, Expanded(Row[ 图列表 | 可拖分隔 | 详情占位 ]),
//              _StatusBar(errorMessage/currentBranch/ahead-behind) ]
```

**工具条内容：**
- 分支过滤 `SegmentedButton<GitGraphScope>`（all / current）：all → `--all`；current → cubit 增加 `showOnlyCurrentBranch(bool)`（refresh 时传 revision range `HEAD` 替代 `--all`，`graphRows` 增加 `String? revisionRange` 参数替代硬编码 `--all`）。
- `[Fetch][Pull][Push]` 图标按钮。
- `[Stash▾]` 弹层（列表 + pop/apply/drop，Task 11 实装菜单项）。
- 搜索框（TpInput，回车提交 search(query, mode)；模式下拉三选）。
- 刷新按钮。

- [ ] **Step 1: l10n key 先行**（en/zh 同步添加；后续任务复用）

```json
"gitGraphTitle": "Git Graph", "gitGraphTitleZh": "提交图",
"gitGraphFetch": "Fetch", "gitGraphPull": "Pull", "gitGraphPush": "Push",
"gitGraphStash": "Stash", "gitGraphRefresh": "Refresh",
"gitGraphSearchHint": "Search commits (message / author / hash)",
"gitGraphAllBranches": "All branches", "gitGraphCurrentBranch": "Current branch",
"gitGraphUncommittedChanges": "Uncommitted changes",
"gitGraphNoCommits": "No commits found", "gitGraphNotARepository": "Not a git repository",
"gitGraphLoadMore": "Load more", "gitGraphLoadingMore": "Loading…"
```
zh 对应："提交图"、"抓取"、"拉取"、"推送"、…（实现时两份 arb 都加齐并跑 `flutter gen-l10n`）。

- [ ] **Step 2: 写失败测试**

```dart
// client/test/pages/git_graph/git_graph_pane_test.dart
import 'package:flutter/material.dart';
import 'package:flutter_bloc/flutter_bloc.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:teampilot/cubits/git_graph_cubit.dart';
import 'package:teampilot/pages/git_graph/git_graph_pane.dart';
import '../../support/git_graph_test_fakes.dart';

Widget host(GitGraphCubit cubit) => MaterialApp(
      home: BlocProvider.value(
        value: cubit,
        child: const Scaffold(
          body: GitGraphPane(workspaceId: 'ws', repoRoot: '/repo'),
        ),
      ),
    );

void main() {
  testWidgets('renders commit rows and uncommitted pseudo row when dirty',
      (tester) async {
    final history = FakeHistoryForGraph(rows: [graphCommitRow('c1')]);
    final cubit = GitGraphCubit(history: history, git: FakeGitForGraph(dirtyStatus()));
    await cubit.setRepoRoot('/repo');
    await tester.pumpWidget(host(cubit));
    await tester.pumpAndSettle();
    expect(find.byKey(const ValueKey('git-graph-row-c1')), findsOneWidget);
    expect(find.textContaining('Uncommitted'), findsOneWidget);
    await cubit.close();
  });

  testWidgets('not-a-repo state shows hint', (tester) async {
    final cubit = GitGraphCubit(
      history: FakeHistoryForGraph(),
      git: FakeGitForGraph(notRepoStatus()),
    );
    await cubit.setRepoRoot('/repo');
    await tester.pumpWidget(host(cubit));
    await tester.pumpAndSettle();
    expect(find.byType(TextField), findsNothing); // 工具条隐藏
    await cubit.close();
  });
}
```

说明：`dirtyStatus()`/`notRepoStatus()` 加入共享 fakes 文件；行 widget 需要 `key: ValueKey('git-graph-row-${row.hash}')`。`GitGraphCubit` 需暴露 `showOnlyCurrentBranch` 与 `graphRows(revisionRange)` 支撑（见 Step 3 的 service 小改）。

- [ ] **Step 3: 实现**

1) `GitHistoryService.graphRows` 把硬编码 `'--all'` 改为参数：

```dart
Future<List<GitGraphRow>> graphRows(String dir,
    {int limit = initialLoadCommits, int skip = 0, String query = '',
     GitSearchMode mode = GitSearchMode.message,
     String? revisionRange}) async {
  // revisionRange == null → ['--all']；否则 [revisionRange]（如 'HEAD'）
}
```

2) `GitGraphCubit` 增加字段与方法：

```dart
bool showCurrentOnly = false;
void toggleCurrentOnly() { showCurrentOnly = !showCurrentOnly; /* emit + refresh */ }
```
（`_runRefresh` 里把 revisionRange 传入 graphRows；state 增加 `currentOnly` 字段供 UI 高亮。）

3) `git_graph_pane.dart` 核心（骨架完整版）：

```dart
import 'package:flutter/material.dart';
import 'package:flutter_bloc/flutter_bloc.dart';

import '../../cubits/git_graph_actions_controller.dart';
import '../../cubits/git_graph_cubit.dart';
import '../../l10n/l10n_extensions.dart';
import '../../models/git_graph.dart';
import 'git_graph_detail_pane.dart';   // Task 10
import 'git_graph_row_tile.dart';
import 'git_graph_toolbar.dart';

class GitGraphPane extends StatefulWidget {
  const GitGraphPane({super.key, required this.workspaceId, required this.repoRoot});

  final String workspaceId;
  final String repoRoot;

  @override
  State<GitGraphPane> createState() => _GitGraphPaneState();
}

`GitGraphPane.build`（解析仓库所属后端上下文并创建/复用 cubit）：

- `GitGraphPane.build`：

```dart
@override
Widget build(BuildContext context) {
  final scope = WorkspaceToolsScope.maybeOf(context);
  RuntimeContext? ctx;
  for (final slice in scope?.targetSlices ?? const []) {
    if (slice.roots.contains(widget.repoRoot)) ctx ??= slice.tools.context;
  }
  ctx ??= scope?.tools?.context;
  if (ctx == null) return const SizedBox.shrink();
  return BlocProvider(
    create: (_) => context.read<GitRepoStore>()
        .graphCubitFor(widget.repoRoot, workContext: ctx!)
      ..selectCommit(null),
    child: const _PaneBody(),
  );
}
```

- `_PaneBody`：`BlocBuilder<GitGraphCubit, GitGraphState>`；非 repo 空态；否则 Column：`GitGraphToolbar(state)` + `Expanded(_GraphList)` + 错误条（l10n + 自动 4s 清除可后置）。
- `_GraphList`：`ListView.builder` 虚拟化；itemCount = dirty 行(0/1) + visibleRows.length + (hasMore ? 1 : 0)；index0 为未提交伪行（`_UncommittedTile`：Icons.edit_note + l10n.gitGraphUncommittedChanges + dirtyCount 徽标，点击行为 v1 为仅展示（不跳转）；后续版本可复用 Source Control 面板的 diff 入口）；滚动监听 `ScrollController.position.pixels > maxScrollExtent - 200` 触发 `context.read<GitGraphCubit>().loadMore()`；底部 item 是 loading 指示或 "加载更多" 按钮（fallback 点击触发 loadMore）。每行包 BlocProvider.value + controller 右键菜单入口（Task 11 接入，先留 `onSecondaryTapUp` 空实现）。
- 行点击 → `cubit.selectCommit(hash)`；选中态来自 `state.selectedHash`。

4) `git_graph_toolbar.dart`：无状态 widget，参数 `(GitGraphState state)` + 通过 `context.read<GitGraphCubit>()` 回调；控件布局一行 `Row`：SegmentedButton(all/current)、三个 IconButton(fetch/pull/push)、Stash PopupMenuButton(渲染 state.stashList；菜单项 Task 11 实装)、Expanded(TpInput 搜索框+模式下拉 DropdownButton<GitSearchMode>)、IconButton(refresh)。所有回调走 cubit/controller，不做 IO。

- [ ] **Step 4: 跑测试确认通过**

Run: `cd client && flutter test test/pages/git_graph/git_graph_pane_test.dart && flutter analyze --no-fatal-infos --no-fatal-warnings`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add client/lib/pages/git_graph/git_graph_toolbar.dart \
  client/lib/pages/git_graph/git_graph_pane.dart \
  client/lib/services/git/git_history_service.dart \
  client/lib/cubits/git_graph_cubit.dart \
  client/lib/l10n/app_en.arb client/lib/l10n/app_zh.arb \
  client/test/pages/git_graph/git_graph_pane_test.dart \
  client/test/support/git_graph_test_fakes.dart
git commit -m "feat(git-graph): pane shell with toolbar list loadmore statusbar"
```

---

### Task 10: 详情栏 + 内嵌 Diff

**Files:**
- Create: `client/lib/pages/git_graph/git_graph_detail_pane.dart`
- Test: `client/test/pages/git_graph/git_graph_detail_pane_test.dart`

**Interfaces:**
- Consumes: `GitGraphState`（commitDetail/fileDiffText/openFilePath/detailLoading）、`DiffViewer.fromUnifiedDiff`、Task 9 的 pane 布局。
- Produces:

```dart
/// 右侧详情栏：选中提交的元数据 + 文件列表；点击文件切换内嵌 diff 视图，
/// diff 文本由 cubit.openCommitFile 加载，渲染复用 widgets/diff/diff_viewer.dart。
class GitGraphDetailPane extends StatelessWidget {
  const GitGraphDetailPane({super.key, required this.onBack});
}
```

- [ ] **Step 1: 写失败测试**

```dart
// client/test/pages/git_graph/git_graph_detail_pane_test.dart
import 'package:flutter/material.dart';
import 'package:flutter_bloc/flutter_bloc.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:teampilot/cubits/git_graph_cubit.dart';
import 'package:teampilot/models/git_graph.dart';
import 'package:teampilot/pages/git_graph/git_graph_detail_pane.dart';
import '../../support/git_graph_test_fakes.dart';

GitCommitDetail detail() => GitCommitDetail(
GitCommitDetail detail() => GitCommitDetail(
      hash: 'c1',
      parents: const ['p0'],
      authorName: 'Ann',
      authorEmail: 'ann@x',
      authorDate: DateTime.utc(2026, 8, 25),
      subject: 'subj',
      body: 'body',
      files: const [
        GitCommitFileChange('a.dart', GitCommitFileStatus.modified),
      ],
    );
```

```dart
void main() {
  testWidgets('shows metadata and file list; tapping file opens embedded diff',
      (tester) async {
    final history = FakeHistoryForGraph(
      rows: [graphCommitRow('c1')],
      detail: detail(),
    );
    final cubit = GitGraphCubit(history: history, git: FakeGitForGraph(repoStatus()));
    await cubit.setRepoRoot('/repo');
    await cubit.selectCommit('c1');

    await tester.pumpWidget(MaterialApp(
      home: BlocProvider.value(
        value: cubit,
        child: Scaffold(body: GitGraphDetailPane(onBack: () {})),
      ),
    ));
    expect(find.text('subj'), findsWidgets);
    expect(find.text('a.dart'), findsOneWidget);

    // fake 无法产出真实 diff：给 FakeHistoryForGraph 增加 fileDiff 字段
    // （实现时在共享 fakes 里补：
    //   String fileDiff = 'diff --git a/a.dart b/a.dart\\n--- a/a.dart\\n+++ b/a.dart\\n@@ -1 +1 @@\\n-a\\n+b\\n';）
    await tester.tap(find.text('a.dart'));
    await tester.pumpAndSettle();
    expect(cubit.state.openFilePath, 'a.dart');
    expect(find.byType(DiffViewer), findsOneWidget);
    await cubit.close();
  });
}
```

（同步修改 `client/test/support/git_graph_test_fakes.dart`：`FakeHistoryForGraph` 增加 `String fileDiff` 字段并覆盖 `commitFileDiff` 返回它。）

- [ ] **Step 2: 跑测试确认失败**

Run: `cd client && flutter test test/pages/git_graph/git_graph_detail_pane_test.dart` → Expected: FAIL

- [ ] **Step 3: 实现 git_graph_detail_pane.dart**

要点（完整实现）：

- 顶部：subject（加粗）、作者 + 本地化时间、hash 行带 copy 按钮（Clipboard.setData + SnackBar l10n 提示）。
- 中部文件列表：每行 status 字母徽标(A/M/D/R/T 颜色区分) + 路径（renamed 显示 `old → new`）；点击 `context.read<GitGraphCubit>().openCommitFile(file)`。
- `openFilePath != null` 时整栏切换为：返回按钮(closeFileDiff) + 文件名头 + `fileDiffLoading ? spinner : DiffViewer.fromUnifiedDiff(diffText: state.fileDiffText ?? '', filePath: state.openFilePath)`。
- `detailLoading` 时显示居中 CircularProgressIndicator。
- 未选中提交（commitDetail == null）显示引导文案「选择一个提交查看详情」（l10n key `gitGraphSelectCommit`）。
- 宽度约束：pane 内以固定 380dp 起、可拖分隔由 Task 9 的 Row 布局承载（v1 固定宽度即可，拖拽为可选增强）。

- [ ] **Step 4: 跑测试确认通过**

Run: `cd client && flutter test test/pages/git_graph/git_graph_detail_pane_test.dart`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add client/lib/pages/git_graph/git_graph_detail_pane.dart \
  client/lib/l10n/app_en.arb client/lib/l10n/app_zh.arb \
  client/test/support/git_graph_test_fakes.dart \
  client/test/pages/git_graph/git_graph_detail_pane_test.dart
git commit -m "feat(git-graph): commit detail pane with embedded diff"
```

---

### Task 11: 右键菜单 + 确认对话框（分支/标签/提交级/stash）

**Files:**
- Create: `client/lib/pages/git_graph/git_graph_menus.dart`
- Modify: `client/lib/pages/git_graph/git_graph_pane.dart`（行右键/长按接入菜单；工具条 stash 弹层实装）
- Test: `client/test/pages/git_graph/git_graph_menus_test.dart`

**Interfaces:**
- Consumes: `GitGraphActionsController`、`GitGraphCubit`、`showDialog`。
- Produces:

```dart
/// 行级菜单：checkout / 在此创建分支 / 在此创建标签 / cherry-pick / revert /
/// reset(soft|mixed|hard) / copy hash / copy subject / checkout 分支(装饰)。
Future<void> showCommitContextMenu(BuildContext context, Offset position,
    GitCommitRow row, GitGraphActionsController actions, GitGraphState state);

/// 危险操作确认。reset hard 要求输入当前分支名方可启用确认按钮。
Future<bool> confirmDangerAction(BuildContext context,
    {required String title, required String body, String? typeToConfirm});
```

- [ ] **Step 1: 写失败测试**

```dart
// client/test/pages/git_graph/git_graph_menus_test.dart
import 'package:flutter/material.dart';
import 'package:flutter_bloc/flutter_bloc.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:teampilot/cubits/git_graph_actions_controller.dart';
import 'package:teampilot/cubits/git_graph_cubit.dart';
import 'package:teampilot/models/git_graph.dart';
import 'package:teampilot/pages/git_graph/git_graph_menus.dart';
import '../../support/git_graph_test_fakes.dart';

void main() {
  testWidgets('confirmDangerAction blocks until typed branch matches',
      (tester) async {
    late bool result;
    await tester.pumpWidget(MaterialApp(home: Builder(builder: (context) {
      return Center(
        child: TextButton(
          onPressed: () async {
            result = await confirmDangerAction(context,
                title: 'Reset', body: 'hard reset main',
                typeToConfirm: 'main');
          },
          child: const Text('go'),
        ),
      );
    })));
    await tester.tap(find.text('go'));
    await tester.pumpAndSettle();

    expect(tester.widget<ElevatedButton>(find.byType(ElevatedButton).last)
        .onPressed, isNull); // 未输入时禁用
    await tester.enterText(find.byType(TextField), 'wrong');
    await tester.pump();
    expect(
        tester.widget<ElevatedButton>(find.byType(ElevatedButton).last)
            .onPressed, isNull);
    await tester.enterText(find.byType(TextField), 'main');
    await tester.pump();
    await tester.tap(find.byType(ElevatedButton).last);
    await tester.pumpAndSettle();
    expect(result, isTrue);
  });

  testWidgets('commit menu triggers cherryPick through controller',
      (tester) async {
    final actions = RecordingGraphActions();
    final cubit = GitGraphCubit(
      history: FakeHistoryForGraph(rows: [graphCommitRow('c1')]),
      git: FakeGitForGraph(repoStatus()),
      actions: actions,
    );
    await cubit.setRepoRoot('/repo');
    final controller = GitGraphActionsController(cubit: cubit);

    await tester.pumpWidget(MaterialApp(home: Builder(builder: (context) {
      return BlocProvider.value(
        value: cubit,
        child: Center(
          child: TextButton(
            onPressed: () => showCommitContextMenu(
                context,
                const Offset(200, 200),
                graphCommitRow('c1'),
                controller,
                cubit.state),
            child: const Text('open'),
          ),
        ),
      );
    })));
    await tester.tap(find.text('open'));
    await tester.pumpAndSettle();

    await tester.tap(find.text('Cherry-pick'));
    await tester.pumpAndSettle();
    expect(actions.calls.single, ['cherry-pick', 'c1']);
    await cubit.close();
  });
}
```

菜单项文案用 l10n key（en: `gitGraphCheckout/...`；zh: 检出/在此创建分支/在此创建标签/摘取(Cherry-pick)/回退(Revert)/重置(Reset)/复制哈希/复制说明）。测试里 `find.text('Cherry-pick')` 对应 en 文案——widget 测试默认 l10n 为 en。

- [ ] **Step 2: 跑测试确认失败**

Run: `cd client && flutter test test/pages/git_graph/git_graph_menus_test.dart` → Expected: FAIL

- [ ] **Step 3: 实现 git_graph_menus.dart**

要点：

- `showCommitContextMenu`：`showMenu(position: RelativeRect.fromLTRB(pos.dx, pos.dy, pos.dx+1, pos.dy+1), items: [...])`，菜单项按序：若 row.refs 有 localBranch → Checkout <branch>；Create branch here（弹 `TpInput` 风格 dialog 输入名字 → `actions.createBranch(name, atHash: row.hash)`）；Create tag here（dialog：name + 可选 message → createTag）；Cherry-pick；Revert（confirmDangerAction 无 typeToConfirm）；Reset 子项 soft/mixed/hard（hard 先 `confirmDangerAction(typeToConfirm: state.currentBranch)`）；Divider；Copy hash / Copy subject。
- `confirmDangerAction`：`showDialog<bool>` 返回 true/false；有 `typeToConfirm` 时 TextField 与之严格相等才 enable 确认按钮（红色 destructive 样式）。
- 所有 dialog 用仓库现有 Tp 组件风格（参照 `pages/my_teams/` 或 `worktree_create_dialog` 的对话框写法）。
- 工具条 Stash 弹层：PopupMenuButton 列出 `state.stashList`（selector+subject），每项子菜单 Pop/Applly/Drop（drop 走 confirmDangerAction），底部 "Stash all changes"→ v1 不做（非本期范围，stash 创建仍走终端）。
- pane 的 `_GraphList` 行回调接上 `onSecondaryTapUp: (d) => showCommitContextMenu(context, d.globalPosition, row, controller, state)` 与 `onLongPress` 同路径。

- [ ] **Step 4: 跑测试确认通过**

Run: `cd client && flutter test test/pages/git_graph/ && flutter analyze --no-fatal-infos --no-fatal-warnings`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add client/lib/pages/git_graph/git_graph_menus.dart \
  client/lib/pages/git_graph/git_graph_pane.dart \
  client/lib/l10n/app_en.arb client/lib/l10n/app_zh.arb \
  client/test/pages/git_graph/git_graph_menus_test.dart
git commit -m "feat(git-graph): context menus and danger confirmations"
```

---

### Task 12: Source Control 入口按钮 + 全量验证

**Files:**
- Create: `client/lib/pages/git_graph/open_git_graph.dart`
- Modify: `client/lib/widgets/git/git_source_control_panel.dart`（_Header 加按钮并透传回调）
- Test: `client/test/pages/git_graph/open_git_graph_test.dart`

**Interfaces:**
- Produces:

```dart
void openGitGraphTab(BuildContext context,
    {required String workspaceId, required String repoRoot}) {
  final floating = context.read<FloatingWorkspaceCubit>();
  floating.ensureOpen();
  floating.setActiveWorkspace(workspaceId);
  context
      .read<WorkbenchCubit>()
      .openFloating(workspaceId, WorkbenchTabId.gitGraph(repoRoot));
}
```

- [ ] **Step 1: 写失败测试**

```dart
import 'package:flutter/material.dart';
import 'package:flutter_bloc/flutter_bloc.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:teampilot/cubits/floating_workspace/floating_workspace_cubit.dart';
import 'package:teampilot/cubits/workbench/workbench_cubit.dart';
import 'package:teampilot/cubits/workbench/workbench_tab.dart';
import 'package:teampilot/pages/git_graph/open_git_graph.dart';

void main() {
  testWidgets('openGitGraphTab opens floating gitGraph tab for repo',
      (tester) async {
    final workbench = WorkbenchCubit();
    final floating = FloatingWorkspaceCubit();
    floating.ensureOpen();
    floating.setActiveWorkspace('ws');
    await tester.pumpWidget(MultiRepositoryProvider(
      providers: [
        RepositoryProvider.value(value: workbench),
        RepositoryProvider.value(value: floating),
      ],
      child: MaterialApp(home: Builder(builder: (context) {
        return Center(
          child: TextButton(
            onPressed: () => openGitGraphTab(context,
                workspaceId: 'ws', repoRoot: '/repo'),
            child: const Text('open'),
          ),
        );
      })),
    ));
    await tester.tap(find.text('open'));
    await tester.pump();
    expect(
      workbench.state.bar('ws').floating.order.map((t) => t.kind),
      contains(WorkbenchTabKind.gitGraph),
    );
  });
}
```

注意：`WorkbenchCubit()`/`FloatingWorkspaceCubit()` 构造签名以实际类为准（实现者先读两个 cubit 文件的构造器；如需依赖注入则按现有测试先例 mock）。

- [ ] **Step 2: 跑测试确认失败**

Run: `cd client && flutter test test/pages/git_graph/open_git_graph_test.dart` → Expected: FAIL

- [ ] **Step 3: 实现**

创建 `client/lib/pages/git_graph/open_git_graph.dart`（接口签名见上，imports：floating_workspace_cubit、workbench_cubit、workbench_tab、flutter_bloc、material）。

修改 `git_source_control_panel.dart`：
- `_Header` 增加 `required this.onOpenGraph` 回调与按钮：在 branch chip 之后插入 `TpHover` 图标按钮（Icons.account_tree_outlined，tooltip l10n.gitGraphTitle）。
- `_Header` 的调用点（`_GitRepoBody.build` 内）传入 `onOpenGraph: () => openGitGraphTab(context, workspaceId: workspaceId, repoRoot: root)`（root 为当前激活仓库根）。

- [ ] **Step 4: 全量验证**

Run:
```bash
cd client
flutter analyze --no-fatal-infos --no-fatal-warnings
flutter test test/services/git/ test/cubits/git_graph_cubit_test.dart \
  test/cubits/git_graph_actions_controller_test.dart test/pages/git_graph/
dart run tool/run_tests.dart
```
Expected: analyze 零错误；全部测试 PASS。

手工冒烟（桌面 debug 运行）：打开带多分支仓库的工作区 → Source Control 头部点 Graph 按钮 → 浮动面板出现图 → 点击 merge 提交看详情/diff → 右键 checkout 一个远程分支 → fetch/pull/push → stash drop 二次确认 → hash 搜索过滤 → 滚动加载更多 → 关闭标签后重开缓存仍在。

- [ ] **Step 5: Commit**

```bash
git add client/lib/pages/git_graph/open_git_graph.dart \
  client/lib/widgets/git/git_source_control_panel.dart \
  client/test/pages/git_graph/open_git_graph_test.dart
git commit -m "feat(git-graph): source control entry button and e2e verify"
```

---

## Self-Review 结论

- Spec coverage：可视化(Task 8/9)、详情+diff(Task 10)、分支/标签/提交级/stash 操作(Task 4/6/11)、fetch/pull/push(Task 9/6)、三种搜索(Task 5/9)、未提交节点(Task 9)、浮动工作区接入(Task 7)、入口(Task 12)。l10n 分散在各任务 Step 1/3 并在 Task 12 汇总核对。
- Placeholder scan：无 TBD；所有代码块可直接落地（个别标注"以实际文件为准"的是防御性提示而非占位）。
- Type consistency：`graphRows(revisionRange)` 在 Task 3 定义、Task 9 使用；`GitGraphActionsController` 方法名在 Task 6 定义、Task 11 调用一致；`surfaceIdFor(gitGraph)='gitGraph'` Task 7 两处一致。
