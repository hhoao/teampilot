name: toastification
description: Vendored toastification overlay toast engine for TeamPilot.
version: 3.0.3
publish_to: none

environment:
  sdk: ^3.8.1
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter
  collection: ^1.19.0
  equatable: ^2.0.5
  pausable_timer: ^3.1.0+3
  uuid: ^4.5.1
