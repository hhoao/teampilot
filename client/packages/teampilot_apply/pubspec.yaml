name: teampilot_apply
description: ApplyPlan + in-process work-plane applier.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.8.1

dependencies:
  crypto: ^3.0.7
  path: ^1.9.0
  teampilot_fs:
    path: ../teampilot_fs

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
