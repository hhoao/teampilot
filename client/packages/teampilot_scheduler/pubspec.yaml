name: teampilot_scheduler
description: Session work-plane init to spawn spec.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.8.1

dependencies:
  path: ^1.9.0
  teampilot_fs:
    path: ../teampilot_fs
  teampilot_apply:
    path: ../teampilot_apply

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
