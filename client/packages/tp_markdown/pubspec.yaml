name: tp_markdown
description: Semantic GFM markdown — compile to IR, kind-based layout, Flutter MarkdownView.
version: 0.2.0
publish_to: none

environment:
  sdk: ^3.8.1
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter
  markdown: ^7.3.0
  flutter_html: 3.0.0
  html: 0.15.6

dev_dependencies:
  flutter_test:
    sdk: flutter
