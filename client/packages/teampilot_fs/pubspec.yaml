name: teampilot_fs
description: Filesystem interface and local/memory backends for TeamPilot.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.8.1

dependencies:
  path: ^1.9.0
  ffi: ^2.2.0
  synchronized: ^3.3.1

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
