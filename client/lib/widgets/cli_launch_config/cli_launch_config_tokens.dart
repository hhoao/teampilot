/// Shared sentinel tokens for CLI launch / preset configure dialogs.
abstract final class CliLaunchConfigTokens {
  static const presetCustom = '__custom__';
  static const presetInherit = '__inherit__';
  static const cliInherit = '__inherit__';
  static const dropdownMinWidth = 180.0;
}
