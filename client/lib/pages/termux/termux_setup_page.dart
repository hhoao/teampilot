import 'package:flutter/material.dart';

/// Guided Termux OpenSSH setup — filled in by Task 9.
class TermuxSetupPage extends StatelessWidget {
  const TermuxSetupPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Termux setup')),
      body: const Center(
        child: Padding(
          padding: EdgeInsets.all(24),
          child: Text('Coming soon — Task 9'),
        ),
      ),
    );
  }
}
