import '../../registry/capabilities/shared_tool_call_resolvers.dart';

/// Flashskyai tool-call resolvers (shared configuration, no CLI deltas).
class FlashskyaiToolCallResolvers extends SharedToolCallResolvers {
  const FlashskyaiToolCallResolvers();
}
