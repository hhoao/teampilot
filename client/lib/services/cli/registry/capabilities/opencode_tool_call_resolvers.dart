import 'shared_tool_call_resolvers.dart';

/// Opencode tool-call resolvers (shared configuration, no CLI deltas).
class OpencodeToolCallResolvers extends SharedToolCallResolvers {
  const OpencodeToolCallResolvers();
}
